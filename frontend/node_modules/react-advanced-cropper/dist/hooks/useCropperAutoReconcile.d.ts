interface CropperInstance {
    hasInteractions: () => boolean;
    reconcileState: () => void;
}
export declare function useCropperAutoReconcile(cropper: CropperInstance, enabled?: boolean): {
    pause(): void;
    resume(): void;
};
export {};
