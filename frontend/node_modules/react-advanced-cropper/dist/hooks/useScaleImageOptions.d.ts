import { ScaleImageOptions } from '../types';
interface DefinedScaleImageOptions {
    touch: boolean;
    wheel: boolean | {
        ratio: number;
    };
}
export declare function useScaleImageOptions(scaleImage: ScaleImageOptions | boolean): DefinedScaleImageOptions;
export {};
