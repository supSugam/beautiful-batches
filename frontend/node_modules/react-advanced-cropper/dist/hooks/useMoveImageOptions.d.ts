import { MoveImageOptions } from '../types';
interface DefinedMoveImageOptions {
    touch: boolean;
    mouse: boolean;
}
export declare function useMoveImageOptions(moveImage: MoveImageOptions | boolean): DefinedMoveImageOptions;
export {};
