import { CSSProperties } from 'react';
import { CropperTransitions, CropperImage, CropperState, Size } from 'advanced-cropper';
interface DesiredCropperRef {
    getState: () => CropperState | null;
    getTransitions: () => CropperTransitions;
    getImage: () => CropperImage | null;
}
export interface CropperPreviewBackgroundProps {
    className?: string;
    cropper: DesiredCropperRef;
    crossOrigin?: 'anonymous' | 'use-credentials' | boolean;
    size?: Size | null;
    style?: CSSProperties;
}
export declare const CropperPreviewBackground: ({ className, cropper, crossOrigin, size, style, }: CropperPreviewBackgroundProps) => JSX.Element | null;
export {};
