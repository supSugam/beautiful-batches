import { CSSProperties, FC, ReactNode } from 'react';
interface Props {
    visible?: unknown;
    className?: string;
    style?: CSSProperties;
    children?: ReactNode;
}
export declare const CropperFade: FC<Props>;
export {};
