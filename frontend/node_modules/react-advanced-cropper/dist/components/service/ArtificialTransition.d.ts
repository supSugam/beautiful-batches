import { FC, ReactNode } from 'react';
import { CropperTransitions } from 'advanced-cropper';
interface Props {
    className?: string;
    transitions?: CropperTransitions;
    width?: number;
    height?: number;
    left?: number;
    top?: number;
    children?: ReactNode;
}
export declare const ArtificialTransition: FC<Props>;
export {};
