/// <reference types="react" />
import { CardinalDirection, MoveDirections } from 'advanced-cropper';
interface Props {
    defaultClassName?: string;
    hoverClassName?: string;
    wrapperClassName?: string;
    position?: CardinalDirection;
    disabled?: boolean;
    onMove?: (directions: MoveDirections, event: TouchEvent | MouseEvent) => void;
    onMoveEnd?: () => void;
}
export declare const SimpleLine: ({ position, hoverClassName, wrapperClassName, defaultClassName, disabled, onMove, onMoveEnd, }: Props) => JSX.Element;
export {};
