import {
  require_react
} from "./chunk-YHPANKLD.js";
import {
  __commonJS,
  __toESM
} from "./chunk-PR4QN5HX.js";

// node_modules/classnames/index.js
var require_classnames = __commonJS({
  "node_modules/classnames/index.js"(exports, module) {
    (function() {
      "use strict";
      var hasOwn = {}.hasOwnProperty;
      function classNames() {
        var classes = "";
        for (var i = 0; i < arguments.length; i++) {
          var arg = arguments[i];
          if (arg) {
            classes = appendClass(classes, parseValue(arg));
          }
        }
        return classes;
      }
      function parseValue(arg) {
        if (typeof arg === "string" || typeof arg === "number") {
          return arg;
        }
        if (typeof arg !== "object") {
          return "";
        }
        if (Array.isArray(arg)) {
          return classNames.apply(null, arg);
        }
        if (arg.toString !== Object.prototype.toString && !arg.toString.toString().includes("[native code]")) {
          return arg.toString();
        }
        var classes = "";
        for (var key in arg) {
          if (hasOwn.call(arg, key) && arg[key]) {
            classes = appendClass(classes, key);
          }
        }
        return classes;
      }
      function appendClass(value, newClass) {
        if (!newClass) {
          return value;
        }
        if (value) {
          return value + " " + newClass;
        }
        return value + newClass;
      }
      if (typeof module !== "undefined" && module.exports) {
        classNames.default = classNames;
        module.exports = classNames;
      } else if (typeof define === "function" && typeof define.amd === "object" && define.amd) {
        define("classnames", [], function() {
          return classNames;
        });
      } else {
        window.classNames = classNames;
      }
    })();
  }
});

// node_modules/tslib/tslib.es6.mjs
var extendStatics = function(d, b) {
  extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
    d2.__proto__ = b2;
  } || function(d2, b2) {
    for (var p in b2) if (Object.prototype.hasOwnProperty.call(b2, p)) d2[p] = b2[p];
  };
  return extendStatics(d, b);
};
function __extends(d, b) {
  if (typeof b !== "function" && b !== null)
    throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
  extendStatics(d, b);
  function __() {
    this.constructor = d;
  }
  d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
}
var __assign = function() {
  __assign = Object.assign || function __assign2(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
    }
    return t;
  };
  return __assign.apply(this, arguments);
};
function __rest(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
}
function __awaiter(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}
function __generator(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
  return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
}
function __read(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
}
function __spreadArrays() {
  for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
  for (var r = Array(s), k = 0, i = 0; i < il; i++)
    for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
      r[k] = a[j];
  return r;
}
function __spreadArray(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
}

// node_modules/react-advanced-cropper/dist/index.esm-bundler.js
var import_react = __toESM(require_react());

// node_modules/advanced-cropper/utils/index.js
function getDirectionNames(hDirection, vDirection) {
  var camelCase, snakeCase;
  if (hDirection && vDirection) {
    camelCase = "" + hDirection + vDirection[0].toUpperCase() + vDirection.slice(1);
    snakeCase = hDirection + "-" + vDirection;
  } else {
    camelCase = hDirection || vDirection;
    snakeCase = hDirection || vDirection;
  }
  return { camelCase, snakeCase };
}
function isBlob(url) {
  return /^blob:/.test(url);
}
function isDataUrl(url) {
  return /^data:/.test(url);
}
function isLocal(url) {
  return isBlob(url) || isDataUrl(url);
}
function isCrossOriginURL(url) {
  if (isLocal(url)) {
    return false;
  }
  var pageLocation = window.location;
  var URL_HOST_PATTERN = /(\w+:)?(?:\/\/)([\w.-]+)?(?::(\d+))?\/?/;
  var urlMatch = URL_HOST_PATTERN.exec(url) || [];
  var urlparts = {
    protocol: urlMatch[1] || "",
    host: urlMatch[2] || "",
    port: urlMatch[3] || ""
  };
  var defaultPort = function(protocol) {
    if (protocol === "http") {
      return 80;
    } else {
      return 433;
    }
  };
  var portOf = function(location) {
    return location.port || defaultPort(location.protocol || pageLocation.protocol);
  };
  return !(!urlparts.protocol && !urlparts.host && !urlparts.port || Boolean(urlparts.protocol && urlparts.protocol == pageLocation.protocol && urlparts.host && urlparts.host == pageLocation.host && urlparts.host && portOf(urlparts) == portOf(pageLocation)));
}
function isArray(value) {
  return Array.isArray(value);
}
function isFunction(value) {
  return typeof value === "function";
}
function isUndefined(obj) {
  return obj === void 0;
}
var isObject = function(term) {
  return term !== null && typeof term === "object";
};
function getOptions(options, defaultScheme, falseScheme) {
  if (falseScheme === void 0) {
    falseScheme = {};
  }
  var result = {};
  if (isObject(options)) {
    Object.keys(defaultScheme).forEach(function(key) {
      if (isUndefined(options[key])) {
        result[key] = defaultScheme[key];
      } else if (isObject(defaultScheme[key])) {
        if (isObject(options[key])) {
          result[key] = getOptions(options[key], defaultScheme[key], falseScheme[key]);
        } else {
          result[key] = options[key] ? defaultScheme[key] : falseScheme[key];
        }
      } else if (defaultScheme[key] === true || defaultScheme[key] === false) {
        result[key] = Boolean(options[key]);
      } else {
        result[key] = options[key];
      }
    });
    return result;
  } else {
    if (options) {
      return defaultScheme;
    } else {
      return falseScheme;
    }
  }
}
function parseNumber(number) {
  var parsedNumber = Number(number);
  if (Number.isNaN(parsedNumber)) {
    return number;
  } else {
    return parsedNumber;
  }
}
function isBoolean(value) {
  return value === true || value === false;
}
function isNumber(value) {
  return typeof value === "number";
}
function isString(value) {
  return typeof value === "string";
}
function isNaN(value) {
  return value !== value;
}
function isNumeric(value) {
  return isNumber(value) && !isNaN(value) || isString(value) && !Number.isNaN(parseFloat(value)) && isFinite(parseFloat(value));
}
function distance(firstPoint, secondPoint) {
  return Math.sqrt(Math.pow(firstPoint.left - secondPoint.left, 2) + Math.pow(firstPoint.top - secondPoint.top, 2));
}
function isRoughlyEqual(a, b, tolerance) {
  if (tolerance === void 0) {
    tolerance = 1e-3;
  }
  return Math.abs(b - a) < tolerance;
}
function isGreater(a, b, tolerance) {
  return isRoughlyEqual(a, b, tolerance) ? false : a > b;
}
function isLower(a, b, tolerance) {
  return isRoughlyEqual(a, b, tolerance) ? false : a < b;
}
function isArrayBufferLike(value) {
  return value instanceof ArrayBuffer;
}
function getCloserAngle(reference, angle) {
  var turnsCount = Math.floor(reference / 360);
  var firstCandidate = turnsCount * 360 + angle;
  var secondCandidate = (turnsCount + 1) * 360 + angle;
  if (Math.abs(firstCandidate - reference) < Math.abs(secondCandidate - reference)) {
    return firstCandidate;
  } else {
    return secondCandidate;
  }
}
function sign(value) {
  var number = +value;
  if (number === 0 || isNaN(number)) {
    return number;
  }
  return number > 0 ? 1 : -1;
}
function promiseTimeout(timeout) {
  return new Promise(function(resolve) {
    setTimeout(function() {
      resolve();
    }, timeout);
  });
}
function deepClone(obj) {
  if (!isObject(obj)) {
    return obj;
  }
  var result = {};
  if (Array.isArray(obj)) {
    result = obj.map(function(item) {
      return deepClone(item);
    });
  } else {
    Object.keys(obj).forEach(function(key) {
      return result[key] = deepClone(obj[key]);
    });
  }
  return result;
}
function deepCompare(a, b, tolerance) {
  if (tolerance === void 0) {
    tolerance = 1e-3;
  }
  if (isNumber(a) && isNumber(b) && isRoughlyEqual(a, b, tolerance))
    return true;
  if (a === b)
    return true;
  if (a && b && typeof a == "object" && typeof b == "object") {
    if (a.constructor !== b.constructor)
      return false;
    var length_1, i = void 0;
    if (Array.isArray(a)) {
      length_1 = a.length;
      if (length_1 != b.length)
        return false;
      for (i = length_1; i-- !== 0; )
        if (!deepCompare(a[i], b[i], tolerance))
          return false;
      return true;
    }
    if (a.valueOf !== Object.prototype.valueOf)
      return a.valueOf() === b.valueOf();
    if (a.toString !== Object.prototype.toString)
      return a.toString() === b.toString();
    var keys = Object.keys(a);
    length_1 = keys.length;
    if (length_1 !== Object.keys(b).length)
      return false;
    for (i = length_1; i-- !== 0; )
      if (!Object.prototype.hasOwnProperty.call(b, keys[i]))
        return false;
    for (i = length_1; i-- !== 0; ) {
      var key = keys[i];
      if (!deepCompare(a[key], b[key], tolerance))
        return false;
    }
    return true;
  }
  return a !== a && b !== b;
}
function isWheelEvent(event) {
  return "deltaX" in event;
}
function isTouchEvent(event) {
  return "touches" in event;
}
function isMouseEvent(event) {
  return "buttons" in event;
}
function emptyCoordinates() {
  return {
    left: 0,
    top: 0,
    width: 0,
    height: 0
  };
}
function isCardinalDirection(value) {
  return value === "west" || value === "south" || value === "north" || value === "east";
}
function isOrdinalDirection(value) {
  return isCardinalDirection(value) || value === "westNorth" || value === "westSouth" || value === "eastNorth" || value === "eastSouth";
}
function debounce(callback, delay) {
  var timestamp;
  var timeout;
  function later() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    var last = Date.now() - timestamp;
    var delayValue = isFunction(delay) ? delay() : delay || 0;
    if (last < delayValue && last >= 0) {
      timeout = setTimeout(function() {
        return later.apply(void 0, args);
      }, delayValue - last);
    } else {
      callback.apply(void 0, args);
    }
  }
  function result() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    timestamp = Date.now();
    timeout = setTimeout(function() {
      return later.apply(void 0, args);
    }, isFunction(delay) ? delay() : delay || 0);
  }
  result.clear = function() {
    clearTimeout(timeout);
  };
  return result;
}

// node_modules/advanced-cropper/constants/index.js
var ALL_DIRECTIONS = ["left", "right", "top", "bottom"];
var HORIZONTAL_DIRECTIONS = ["left", "right"];
var VERTICAL_DIRECTIONS = ["top", "bottom"];

// node_modules/advanced-cropper/service/utils.js
function diff(firstObject, secondObject) {
  return {
    left: firstObject.left - secondObject.left,
    top: firstObject.top - secondObject.top
  };
}
function getCenter(object) {
  return {
    left: object.left + object.width / 2,
    top: object.top + object.height / 2
  };
}
function getOppositeSide(side) {
  return {
    left: "right",
    top: "bottom",
    right: "left",
    bottom: "top"
  }[side];
}
function sizeDistance(a, b) {
  return Math.pow(a.width - b.width, 2) + Math.pow(a.height - b.height, 2);
}
function applyDirections(coordinates, directions) {
  return {
    left: coordinates.left - directions.left,
    top: coordinates.top - directions.top,
    width: coordinates.width + directions.left + directions.right,
    height: coordinates.height + directions.top + directions.bottom
  };
}
function inverseMove(directions) {
  return {
    left: -directions.left,
    top: -directions.top
  };
}
function applyMove(object, move) {
  return __assign(__assign({}, object), { left: object.left + move.left, top: object.top + move.top });
}
function coordinatesToPositionRestrictions(coordinates) {
  return {
    left: coordinates.left,
    top: coordinates.top,
    right: coordinates.left + coordinates.width,
    bottom: coordinates.top + coordinates.height
  };
}
function applyScale(object, factor, center, progress) {
  if (factor !== 1) {
    if ("left" in object || "top" in object) {
      if (center) {
        var currentCenter = getCenter(object);
        return {
          width: object.width * factor,
          height: object.height * factor,
          left: object.left + object.width * (1 - factor) / 2 + (center.left - currentCenter.left) * (progress || 1 - factor),
          top: object.top + object.height * (1 - factor) / 2 + (center.top - currentCenter.top) * (progress || 1 - factor)
        };
      } else {
        return {
          width: object.width * factor,
          height: object.height * factor,
          left: object.left + object.width * (1 - factor) / 2,
          top: object.top + object.height * (1 - factor) / 2
        };
      }
    } else {
      return {
        width: object.width * factor,
        height: object.height * factor
      };
    }
  } else {
    return object;
  }
}
function ratio(object) {
  return object.width / object.height;
}
function maxScale(size, restrictions) {
  return Math.min(restrictions.maxWidth ? restrictions.maxWidth / size.width : Infinity, restrictions.maxHeight ? restrictions.maxHeight / size.height : Infinity);
}
function minScale(size, restrictions) {
  return Math.max(restrictions.minWidth ? restrictions.minWidth / size.width : 0, restrictions.minHeight ? restrictions.minHeight / size.height : 0);
}
function getBrokenRatio(currentAspectRatio, aspectRatio2) {
  var ratioBroken;
  if (aspectRatio2.minimum && isLower(currentAspectRatio, aspectRatio2.minimum)) {
    ratioBroken = aspectRatio2.minimum;
  } else if (aspectRatio2.maximum && isGreater(currentAspectRatio, aspectRatio2.maximum)) {
    ratioBroken = aspectRatio2.maximum;
  }
  return ratioBroken;
}
function fitToSizeRestrictions(coordinates, sizeRestrictions2) {
  var aspectRatio2 = ratio(coordinates);
  var scale = 1;
  if (sizeRestrictions2.minWidth > 0 && sizeRestrictions2.minHeight > 0) {
    if (aspectRatio2 > sizeRestrictions2.minWidth / sizeRestrictions2.minHeight) {
      if (coordinates.height < sizeRestrictions2.minHeight) {
        scale = sizeRestrictions2.minHeight / coordinates.height;
      }
    } else {
      if (coordinates.width < sizeRestrictions2.minWidth) {
        scale = sizeRestrictions2.minWidth / coordinates.width;
      }
    }
  } else if (sizeRestrictions2.minWidth > 0) {
    if (coordinates.width < sizeRestrictions2.minWidth) {
      scale = sizeRestrictions2.minWidth / coordinates.width;
    }
  } else if (sizeRestrictions2.minHeight > 0) {
    if (coordinates.height < sizeRestrictions2.minHeight) {
      scale = sizeRestrictions2.minHeight / coordinates.height;
    }
  }
  if (sizeRestrictions2.maxWidth < Infinity && sizeRestrictions2.maxHeight < Infinity) {
    if (aspectRatio2 > sizeRestrictions2.maxWidth / sizeRestrictions2.maxHeight) {
      if (coordinates.width > sizeRestrictions2.maxWidth) {
        scale = sizeRestrictions2.maxWidth / coordinates.width;
      }
    } else {
      if (coordinates.height > sizeRestrictions2.maxHeight) {
        scale = sizeRestrictions2.maxHeight / coordinates.height;
      }
    }
  } else if (sizeRestrictions2.maxWidth < Infinity) {
    if (coordinates.width > sizeRestrictions2.maxWidth) {
      scale = sizeRestrictions2.maxWidth / coordinates.width;
    }
  } else if (sizeRestrictions2.maxHeight < Infinity) {
    if (coordinates.height > sizeRestrictions2.maxHeight) {
      scale = sizeRestrictions2.maxHeight / coordinates.height;
    }
  }
  return scale;
}
function getIntersections(object, positionRestrictions) {
  var intersections = {
    left: 0,
    top: 0,
    right: 0,
    bottom: 0
  };
  ALL_DIRECTIONS.forEach(function(direction) {
    var areaLimit = positionRestrictions[direction];
    var objectLimit = coordinatesToPositionRestrictions(object)[direction];
    if (areaLimit !== void 0 && objectLimit !== void 0) {
      if (direction === "left" || direction === "top") {
        intersections[direction] = Math.max(0, areaLimit - objectLimit);
      } else {
        intersections[direction] = Math.max(0, objectLimit - areaLimit);
      }
    } else {
      intersections[direction] = 0;
    }
  });
  return intersections;
}
function resizeToSizeRestrictions(coordinates, sizeRestrictions2) {
  return applyScale(coordinates, fitToSizeRestrictions(coordinates, sizeRestrictions2));
}
function rotateSize(size, angle) {
  var radians = angle * Math.PI / 180;
  return {
    width: Math.abs(size.width * Math.cos(radians)) + Math.abs(size.height * Math.sin(radians)),
    height: Math.abs(size.width * Math.sin(radians)) + Math.abs(size.height * Math.cos(radians))
  };
}
function rotatePoint(point, angle, anchor) {
  var radians = angle * Math.PI / 180;
  if (anchor) {
    return {
      left: (point.left - anchor.left) * Math.cos(radians) - (point.top - anchor.top) * Math.sin(radians) + anchor.left,
      top: (point.left - anchor.left) * Math.sin(radians) + (point.top - anchor.top) * Math.cos(radians) + anchor.top
    };
  } else {
    return {
      left: point.left * Math.cos(radians) - point.top * Math.sin(radians),
      top: point.left * Math.sin(radians) + point.top * Math.cos(radians)
    };
  }
}
function positionToSizeRestrictions(positionRestrictions) {
  return {
    minWidth: 0,
    minHeight: 0,
    maxWidth: positionRestrictions.right !== void 0 && positionRestrictions.left !== void 0 ? positionRestrictions.right - positionRestrictions.left : Infinity,
    maxHeight: positionRestrictions.bottom !== void 0 && positionRestrictions.top !== void 0 ? positionRestrictions.bottom - positionRestrictions.top : Infinity
  };
}
function mergePositionRestrictions(a, b) {
  var restrictions = {};
  ALL_DIRECTIONS.forEach(function(direction) {
    var firstDirection = a[direction];
    var secondDirection = b[direction];
    if (firstDirection !== void 0 && secondDirection !== void 0) {
      if (direction === "left" || direction === "top") {
        restrictions[direction] = Math.max(firstDirection, secondDirection);
      } else {
        restrictions[direction] = Math.min(firstDirection, secondDirection);
      }
    } else if (secondDirection !== void 0) {
      restrictions[direction] = secondDirection;
    } else if (firstDirection !== void 0) {
      restrictions[direction] = firstDirection;
    }
  });
  return restrictions;
}
function fitToPositionRestrictions(coordinates, positionRestrictions) {
  var directions = {
    left: 0,
    top: 0
  };
  var intersection = getIntersections(coordinates, positionRestrictions);
  if (intersection.left && intersection.left > 0) {
    directions.left = intersection.left;
  } else if (intersection.right && intersection.right > 0) {
    directions.left = -intersection.right;
  }
  if (intersection.top && intersection.top > 0) {
    directions.top = intersection.top;
  } else if (intersection.bottom && intersection.bottom > 0) {
    directions.top = -intersection.bottom;
  }
  return directions;
}
function moveToPositionRestrictions(coordinates, positionRestrictions) {
  return applyMove(coordinates, fitToPositionRestrictions(coordinates, positionRestrictions));
}
function aspectRatioIntersection(main, subset) {
  if (!subset) {
    return main;
  } else if (!main) {
    return subset;
  } else {
    return {
      minimum: Math.min(main.maximum, Math.max(main.minimum, subset.minimum)),
      maximum: Math.max(main.minimum, Math.min(main.maximum, subset.maximum))
    };
  }
}
function createAspectRatio(aspectRatio2) {
  if (aspectRatio2 === void 0) {
    aspectRatio2 = {};
  }
  if (isNumber(aspectRatio2)) {
    return {
      minimum: aspectRatio2,
      maximum: aspectRatio2
    };
  } else {
    return {
      minimum: isNumeric(aspectRatio2.minimum) ? aspectRatio2.minimum : 0,
      maximum: isNumeric(aspectRatio2.maximum) ? aspectRatio2.maximum : Infinity
    };
  }
}
function getTransitionStyle(transitions) {
  return transitions ? transitions.timingFunction + " " + (transitions.active ? transitions.duration : 0) + "ms" : "none";
}
function isConsistentSize(size, restrictions) {
  return (isUndefined(restrictions.maxWidth) || !isGreater(size.width, restrictions.maxWidth)) && (isUndefined(restrictions.maxHeight) || !isGreater(size.height, restrictions.maxHeight)) && (isUndefined(restrictions.minHeight) || !isLower(size.height, restrictions.minHeight)) && (isUndefined(restrictions.minWidth) || !isLower(size.width, restrictions.minWidth));
}
function isConsistentPosition(coordinates, restrictions) {
  return (isUndefined(restrictions.left) || !isLower(coordinates.left, restrictions.left)) && (isUndefined(restrictions.top) || !isLower(coordinates.top, restrictions.top)) && (isUndefined(restrictions.right) || !isGreater(coordinates.left + coordinates.width, restrictions.right)) && (isUndefined(restrictions.bottom) || !isGreater(coordinates.top + coordinates.height, restrictions.bottom));
}
function getCloserSize(candidates, reference, sizeRestrictions2, aspectRatio2) {
  var traverse = function(ignoreMinimum) {
    return candidates.reduce(function(minimum, size) {
      var _a = isFunction(sizeRestrictions2) ? sizeRestrictions2(size) : sizeRestrictions2, maxHeight = _a.maxHeight, maxWidth = _a.maxWidth, minWidth = _a.minWidth, minHeight = _a.minHeight;
      var preparedAspectRatio = createAspectRatio(isFunction(aspectRatio2) ? aspectRatio2(size) : aspectRatio2);
      var preparedSizeRestrictions = ignoreMinimum ? { maxWidth, maxHeight } : { maxWidth, maxHeight, minWidth, minHeight };
      if (isConsistentSize(size, preparedSizeRestrictions) && !getBrokenRatio(ratio(size), preparedAspectRatio) && size.width && size.height) {
        return !minimum || isLower(sizeDistance(size, reference), sizeDistance(minimum, reference)) ? size : minimum;
      } else {
        return minimum;
      }
    }, null);
  };
  return traverse() || traverse(true);
}

// node_modules/advanced-cropper/service/helpers.js
function isInitializedState(state) {
  return Boolean(state && state.visibleArea && state.coordinates);
}
function getAreaSizeRestrictions(state, settings) {
  return calculateAreaSizeRestrictions(state, settings);
}
function getAreaPositionRestrictions(state, settings) {
  return isFunction(settings.areaPositionRestrictions) ? settings.areaPositionRestrictions(state, settings) : settings.areaPositionRestrictions;
}
function getSizeRestrictions(state, settings) {
  return calculateSizeRestrictions(state, settings);
}
function getPositionRestrictions(state, settings) {
  return isFunction(settings.positionRestrictions) ? settings.positionRestrictions(state, settings) : settings.positionRestrictions;
}
function getCoefficient(state) {
  return state.visibleArea ? state.visibleArea.width / state.boundary.width : 0;
}
function getStencilCoordinates(state) {
  if (isInitializedState(state)) {
    var _a = state.coordinates, width = _a.width, height = _a.height, left = _a.left, top_1 = _a.top;
    var coefficient = getCoefficient(state);
    return {
      width: width / coefficient,
      height: height / coefficient,
      left: (left - state.visibleArea.left) / coefficient,
      top: (top_1 - state.visibleArea.top) / coefficient
    };
  } else {
    return emptyCoordinates();
  }
}
function getAspectRatio(state, settings) {
  return createAspectRatio(isFunction(settings.aspectRatio) ? settings.aspectRatio(state, settings) : settings.aspectRatio);
}
function getDefaultCoordinates(state, settings) {
  return isFunction(settings.defaultCoordinates) ? settings.defaultCoordinates(state, settings) : settings.defaultCoordinates;
}
function getDefaultVisibleArea(state, settings) {
  return isFunction(settings.defaultVisibleArea) ? settings.defaultVisibleArea(state, settings) : settings.defaultVisibleArea;
}
function getDefaultTransforms(state, settings) {
  var transforms = __assign(__assign({}, state.transforms), { flip: __assign({}, state.transforms.flip) });
  if (settings.defaultTransforms) {
    var defaultTransforms = isFunction(settings.defaultTransforms) ? settings.defaultTransforms(state, settings) : settings.defaultTransforms;
    if (isNumber(defaultTransforms.rotate)) {
      transforms.rotate = defaultTransforms.rotate;
    }
    if (defaultTransforms.flip) {
      if (isBoolean(defaultTransforms.flip.horizontal)) {
        transforms.flip.horizontal = defaultTransforms.flip.horizontal;
      }
      if (isBoolean(defaultTransforms.flip.vertical)) {
        transforms.flip.vertical = defaultTransforms.flip.vertical;
      }
    }
  }
  return transforms;
}
function getTransformedImageSize(state) {
  if (state.imageSize && state.imageSize.width && state.imageSize.height) {
    return rotateSize(state.imageSize, state.transforms.rotate);
  } else {
    return {
      width: 0,
      height: 0
    };
  }
}
function getMinimumSize(state) {
  return state.coordinates ? Math.min(state.coordinates.width, state.coordinates.height, 20 * getCoefficient(state)) : 1;
}
function getRoundedCoordinates(state, settings) {
  if (isInitializedState(state)) {
    var sizeRestrictions2 = getSizeRestrictions(state, settings);
    var positionRestrictions = getPositionRestrictions(state, settings);
    var roundCoordinates = {
      width: Math.round(state.coordinates.width),
      height: Math.round(state.coordinates.height),
      left: Math.round(state.coordinates.left),
      top: Math.round(state.coordinates.top)
    };
    if (roundCoordinates.width > sizeRestrictions2.maxWidth) {
      roundCoordinates.width = Math.floor(state.coordinates.width);
    } else if (roundCoordinates.width < sizeRestrictions2.minWidth) {
      roundCoordinates.width = Math.ceil(state.coordinates.width);
    }
    if (roundCoordinates.height > sizeRestrictions2.maxHeight) {
      roundCoordinates.height = Math.floor(state.coordinates.height);
    } else if (roundCoordinates.height < sizeRestrictions2.minHeight) {
      roundCoordinates.height = Math.ceil(state.coordinates.height);
    }
    return moveToPositionRestrictions(roundCoordinates, positionRestrictions);
  } else {
    return null;
  }
}
function isConsistentState(state, settings) {
  if (isInitializedState(state)) {
    return !getBrokenRatio(ratio(state.coordinates), getAspectRatio(state, settings)) && isConsistentSize(state.visibleArea, getAreaSizeRestrictions(state, settings)) && isConsistentSize(state.coordinates, getSizeRestrictions(state, settings)) && isConsistentPosition(state.visibleArea, getAreaPositionRestrictions(state, settings)) && isConsistentPosition(state.coordinates, getPositionRestrictions(state, settings));
  } else {
    return true;
  }
}

// node_modules/advanced-cropper/service/sizeRestrictions.js
function reconcileSizeRestrictions(sizeRestrictions2) {
  var restrictions = __assign({}, sizeRestrictions2);
  if (restrictions.minWidth > restrictions.maxWidth) {
    restrictions.minWidth = restrictions.maxWidth;
  }
  if (restrictions.minHeight > restrictions.maxHeight) {
    restrictions.minHeight = restrictions.maxHeight;
  }
  return restrictions;
}
function mergeSizeRestrictions(a, b) {
  var first = a;
  var second = __assign({ minWidth: 0, minHeight: 0, maxWidth: Infinity, maxHeight: Infinity }, b);
  return reconcileSizeRestrictions({
    maxHeight: Math.min(first.maxHeight, second.maxHeight),
    minHeight: Math.max(first.minHeight, second.minHeight),
    maxWidth: Math.min(first.maxWidth, second.maxWidth),
    minWidth: Math.max(first.minWidth, second.minWidth)
  });
}
function calculateSizeRestrictions(state, settings) {
  var sizeRestrictions2 = isFunction(settings.sizeRestrictions) ? settings.sizeRestrictions(state, settings) : settings.sizeRestrictions;
  var positionRestrictions = getPositionRestrictions(state, settings);
  var restrictions = {
    minWidth: isNumeric(sizeRestrictions2.minWidth) ? parseNumber(sizeRestrictions2.minWidth) : 0,
    minHeight: isNumeric(sizeRestrictions2.minHeight) ? parseNumber(sizeRestrictions2.minHeight) : 0,
    maxWidth: isNumeric(sizeRestrictions2.maxWidth) ? parseNumber(sizeRestrictions2.maxWidth) : Infinity,
    maxHeight: isNumeric(sizeRestrictions2.maxHeight) ? parseNumber(sizeRestrictions2.maxHeight) : Infinity
  };
  if (positionRestrictions.left !== void 0 && positionRestrictions.right !== void 0) {
    restrictions.maxWidth = Math.min(restrictions.maxWidth, positionRestrictions.right - positionRestrictions.left);
  }
  if (positionRestrictions.bottom !== void 0 && positionRestrictions.top !== void 0) {
    restrictions.maxHeight = Math.min(restrictions.maxHeight, positionRestrictions.bottom - positionRestrictions.top);
  }
  return reconcileSizeRestrictions(restrictions);
}
function calculateAreaSizeRestrictions(state, settings) {
  var sizeRestrictions2 = isFunction(settings.areaSizeRestrictions) ? settings.areaSizeRestrictions(state, settings) : settings.areaSizeRestrictions;
  if (sizeRestrictions2.maxWidth < Infinity && sizeRestrictions2.maxHeight < Infinity) {
    if (ratio(state.boundary) > sizeRestrictions2.maxWidth / sizeRestrictions2.maxHeight) {
      sizeRestrictions2.maxHeight = sizeRestrictions2.maxWidth / ratio(state.boundary);
    } else {
      sizeRestrictions2.maxWidth = sizeRestrictions2.maxHeight * ratio(state.boundary);
    }
  }
  return reconcileSizeRestrictions(sizeRestrictions2);
}

// node_modules/advanced-cropper/state/copyState.js
function copyState(state) {
  return deepClone(state);
}

// node_modules/advanced-cropper/types/index.js
var ImageRestriction;
(function(ImageRestriction2) {
  ImageRestriction2["fillArea"] = "fillArea";
  ImageRestriction2["fitArea"] = "fitArea";
  ImageRestriction2["stencil"] = "stencil";
  ImageRestriction2["none"] = "none";
})(ImageRestriction || (ImageRestriction = {}));
var Priority;
(function(Priority2) {
  Priority2["coordinates"] = "coordinates";
  Priority2["visibleArea"] = "visibleArea";
})(Priority || (Priority = {}));

// node_modules/advanced-cropper/service/approximateSize.js
function approximateSize(params) {
  var width = params.width, height = params.height;
  var sizeRestrictions2 = params.sizeRestrictions || {
    minWidth: 0,
    minHeight: 0,
    maxWidth: Infinity,
    maxHeight: Infinity
  };
  var aspectRatio2 = createAspectRatio(params.aspectRatio);
  var coordinates = {
    width: Math.max(sizeRestrictions2.minWidth, Math.min(sizeRestrictions2.maxWidth, width)),
    height: Math.max(sizeRestrictions2.minHeight, Math.min(sizeRestrictions2.maxHeight, height))
  };
  var candidates = [coordinates];
  if (aspectRatio2) {
    [aspectRatio2.minimum, aspectRatio2.maximum].forEach(function(ratio2) {
      if (ratio2) {
        candidates.push({ width: coordinates.width, height: coordinates.width / ratio2 }, { width: coordinates.height * ratio2, height: coordinates.height });
      }
    });
  }
  candidates = candidates.map(function(candidate) {
    return resizeToSizeRestrictions(candidate, sizeRestrictions2);
  });
  return getCloserSize(candidates, { width, height }, sizeRestrictions2, aspectRatio2);
}

// node_modules/advanced-cropper/service/fitVisibleArea.js
function fitVisibleArea(state, settings) {
  var result = copyState(state);
  if (state.visibleArea) {
    var areaSizeRestrictions = getAreaSizeRestrictions(state, settings);
    result.visibleArea = __assign(__assign({}, state.visibleArea), approximateSize({
      width: state.visibleArea.width,
      height: state.visibleArea.height,
      aspectRatio: {
        minimum: ratio(result.boundary),
        maximum: ratio(result.boundary)
      },
      sizeRestrictions: areaSizeRestrictions
    }));
    result.visibleArea = applyMove(result.visibleArea, diff(getCenter(state.visibleArea), getCenter(result.visibleArea)));
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
  }
  return result;
}

// node_modules/advanced-cropper/algorithms/moveCoordinatesAlgorithm.js
function moveCoordinatesAlgorithm(coordinates, directions, positionRestrictions) {
  var movedCoordinates = applyMove(coordinates, directions);
  return positionRestrictions ? moveToPositionRestrictions(movedCoordinates, positionRestrictions) : movedCoordinates;
}

// node_modules/advanced-cropper/state/setCoordinates.js
var SetCoordinatesMode;
(function(SetCoordinatesMode2) {
  SetCoordinatesMode2["limit"] = "limit";
  SetCoordinatesMode2["zoom"] = "zoom";
  SetCoordinatesMode2["unsafe"] = "unsafe";
})(SetCoordinatesMode || (SetCoordinatesMode = {}));
function setCoordinates(state, settings, transform, mode) {
  if (mode === void 0) {
    mode = true;
  }
  var currentMode = mode === false ? SetCoordinatesMode.unsafe : mode === true ? SetCoordinatesMode.zoom : mode;
  var aspectRatio2 = getAspectRatio(state, settings);
  var sizeRestrictions2 = getSizeRestrictions(state, settings);
  if (state.visibleArea && currentMode === SetCoordinatesMode.limit) {
    sizeRestrictions2 = __assign(__assign({}, sizeRestrictions2), { minWidth: Math.min(state.visibleArea.width, sizeRestrictions2.minWidth), minHeight: Math.min(state.visibleArea.height, sizeRestrictions2.minHeight), maxWidth: Math.min(state.visibleArea.width, sizeRestrictions2.maxWidth), maxHeight: Math.min(state.visibleArea.height, sizeRestrictions2.maxHeight) });
  }
  var positionRestrictions = getPositionRestrictions(state, settings);
  if (state.visibleArea && currentMode === SetCoordinatesMode.limit) {
    positionRestrictions = mergePositionRestrictions(positionRestrictions, coordinatesToPositionRestrictions(state.visibleArea));
  }
  var move = function(prevCoordinates, newCoordinates) {
    return moveCoordinatesAlgorithm(prevCoordinates, {
      left: newCoordinates.left - (prevCoordinates.left || 0),
      top: newCoordinates.top - (prevCoordinates.top || 0)
    }, positionRestrictions);
  };
  var resize = function(prevCoordinates, newCoordinates) {
    var coordinates2 = __assign(__assign(__assign({}, prevCoordinates), approximateSize({
      width: newCoordinates.width,
      height: newCoordinates.height,
      sizeRestrictions: sizeRestrictions2,
      aspectRatio: aspectRatio2
    })), { left: 0, top: 0 });
    return move(coordinates2, {
      left: prevCoordinates.left || 0,
      top: prevCoordinates.top || 0
    });
  };
  var coordinates = state.coordinates ? __assign({}, state.coordinates) : emptyCoordinates();
  var transforms = Array.isArray(transform) ? transform : [transform];
  transforms.forEach(function(transform2) {
    var changes;
    if (typeof transform2 === "function") {
      changes = transform2(__assign(__assign({}, state), { coordinates }), settings);
    } else {
      changes = transform2;
    }
    if (changes) {
      if (!isUndefined(changes.width) || !isUndefined(changes.height)) {
        coordinates = resize(coordinates, __assign(__assign({}, coordinates), changes));
      }
      if (!isUndefined(changes.left) || !isUndefined(changes.top)) {
        coordinates = move(coordinates, __assign(__assign({}, coordinates), changes));
      }
    }
  });
  var result = __assign(__assign({}, copyState(state)), { coordinates });
  if (result.visibleArea && currentMode === SetCoordinatesMode.zoom) {
    var widthIntersections = Math.max(0, result.coordinates.width - result.visibleArea.width);
    var heightIntersections = Math.max(0, result.coordinates.height - result.visibleArea.height);
    var areaSizeRestrictions = getAreaSizeRestrictions(state, settings);
    var scale = widthIntersections > heightIntersections ? result.coordinates.width / result.visibleArea.width : result.coordinates.height / result.visibleArea.height;
    if (scale > 1) {
      result.visibleArea = applyScale(result.visibleArea, Math.min(scale, maxScale(result.visibleArea, areaSizeRestrictions)));
    }
    result.visibleArea = applyMove(result.visibleArea, inverseMove(fitToPositionRestrictions(result.coordinates, coordinatesToPositionRestrictions(result.visibleArea))));
    return fitVisibleArea(result, settings);
  } else {
    return result;
  }
}

// node_modules/advanced-cropper/service/fitCoordinates.js
function fitCoordinates(state, settings) {
  if (state.coordinates) {
    var result = copyState(state);
    var aspectRatio2 = getAspectRatio(state, settings);
    var sizeRestrictions2 = getSizeRestrictions(state, settings);
    result.coordinates = __assign(__assign({}, state.coordinates), approximateSize({
      width: state.coordinates.width,
      height: state.coordinates.height,
      aspectRatio: aspectRatio2,
      sizeRestrictions: state.visibleArea ? {
        maxWidth: Math.min(state.visibleArea.width, sizeRestrictions2.maxWidth),
        maxHeight: Math.min(state.visibleArea.height, sizeRestrictions2.maxHeight),
        minHeight: Math.min(state.visibleArea.height, sizeRestrictions2.minHeight),
        minWidth: Math.min(state.visibleArea.width, sizeRestrictions2.minWidth)
      } : sizeRestrictions2
    }));
    result.coordinates = applyMove(result.coordinates, diff(getCenter(state.coordinates), getCenter(result.coordinates)));
    result.coordinates = moveToPositionRestrictions(result.coordinates, state.visibleArea ? mergePositionRestrictions(coordinatesToPositionRestrictions(state.visibleArea), getPositionRestrictions(result, settings)) : getPositionRestrictions(result, settings));
    return result;
  }
  return state;
}

// node_modules/advanced-cropper/state/setVisibleArea.js
function setVisibleArea(state, settings, visibleArea, safe) {
  if (safe === void 0) {
    safe = true;
  }
  var result = __assign(__assign({}, copyState(state)), { visibleArea });
  result = fitVisibleArea(result, settings);
  if (safe) {
    result = fitCoordinates(result, settings);
  }
  return result;
}

// node_modules/advanced-cropper/state/createState.js
function createState(options, settings) {
  var _a, _b, _c, _d, _e;
  var boundary = options.boundary, image = options.image;
  var state = {
    boundary: {
      width: boundary.width,
      height: boundary.height
    },
    imageSize: {
      width: image.width,
      height: image.height
    },
    transforms: {
      rotate: ((_a = image.transforms) === null || _a === void 0 ? void 0 : _a.rotate) || 0,
      flip: {
        horizontal: ((_c = (_b = image.transforms) === null || _b === void 0 ? void 0 : _b.flip) === null || _c === void 0 ? void 0 : _c.horizontal) || false,
        vertical: ((_e = (_d = image.transforms) === null || _d === void 0 ? void 0 : _d.flip) === null || _e === void 0 ? void 0 : _e.vertical) || false
      }
    },
    visibleArea: null,
    coordinates: null
  };
  if (settings.defaultTransforms) {
    state.transforms = getDefaultTransforms(state, settings);
  }
  if (settings.priority === Priority.visibleArea) {
    state = setVisibleArea(state, settings, getDefaultVisibleArea(state, settings), false);
    state = setCoordinates(state, settings, getDefaultCoordinates(state, settings), SetCoordinatesMode.limit);
  } else {
    state = setCoordinates(state, settings, getDefaultCoordinates(state, settings), SetCoordinatesMode.unsafe);
    state = setVisibleArea(state, settings, getDefaultVisibleArea(state, settings), true);
  }
  return state;
}

// node_modules/advanced-cropper/state/moveCoordinates.js
function moveCoordinates(state, settings, directions) {
  if (isInitializedState(state)) {
    var result = copyState(state);
    result.coordinates = applyMove(result.coordinates, directions);
    result.coordinates = moveToPositionRestrictions(result.coordinates, mergePositionRestrictions(coordinatesToPositionRestrictions(result.visibleArea), getPositionRestrictions(result, settings)));
    return result;
  }
  return state;
}

// node_modules/advanced-cropper/algorithms/resizeCoordinatesAlgorithm.js
function fitDirections(_a) {
  var coordinates = _a.coordinates, directions = _a.directions, positionRestrictions = _a.positionRestrictions, sizeRestrictions2 = _a.sizeRestrictions, preserveRatio = _a.preserveRatio, allowedDirections = _a.allowedDirections, _b = _a.compensate, compensate = _b === void 0 ? true : _b;
  var fittedDirections = __assign({}, directions);
  var currentWidth = applyDirections(coordinates, fittedDirections).width;
  var currentHeight = applyDirections(coordinates, fittedDirections).height;
  if (currentWidth < 0) {
    if (fittedDirections.left < 0 && fittedDirections.right < 0) {
      fittedDirections.left = -(coordinates.width - sizeRestrictions2.minWidth) / (fittedDirections.left / fittedDirections.right);
      fittedDirections.right = -(coordinates.width - sizeRestrictions2.minWidth) / (fittedDirections.right / fittedDirections.left);
    } else if (fittedDirections.left < 0) {
      fittedDirections.left = -(coordinates.width - sizeRestrictions2.minWidth);
    } else if (fittedDirections.right < 0) {
      fittedDirections.right = -(coordinates.width - sizeRestrictions2.minWidth);
    }
  }
  if (currentHeight < 0) {
    if (fittedDirections.top < 0 && fittedDirections.bottom < 0) {
      fittedDirections.top = -(coordinates.height - sizeRestrictions2.minHeight) / (fittedDirections.top / fittedDirections.bottom);
      fittedDirections.bottom = -(coordinates.height - sizeRestrictions2.minHeight) / (fittedDirections.bottom / fittedDirections.top);
    } else if (fittedDirections.top < 0) {
      fittedDirections.top = -(coordinates.height - sizeRestrictions2.minHeight);
    } else if (fittedDirections.bottom < 0) {
      fittedDirections.bottom = -(coordinates.height - sizeRestrictions2.minHeight);
    }
  }
  var breaks = getIntersections(applyDirections(coordinates, fittedDirections), positionRestrictions);
  var canBeCompensated = ALL_DIRECTIONS.every(function(direction) {
    return !isGreater(breaks[getOppositeSide(direction)], 0) || allowedDirections[direction];
  });
  if (compensate && canBeCompensated) {
    if (breaks.left && breaks.left > 0 && breaks.right === 0) {
      fittedDirections.right += breaks.left;
      fittedDirections.left -= breaks.left;
    } else if (breaks.right && breaks.right > 0 && breaks.left === 0) {
      fittedDirections.left += breaks.right;
      fittedDirections.right -= breaks.right;
    }
    if (breaks.top && breaks.top > 0 && breaks.bottom === 0) {
      fittedDirections.bottom += breaks.top;
      fittedDirections.top -= breaks.top;
    } else if (breaks.bottom && breaks.bottom > 0 && breaks.top === 0) {
      fittedDirections.top += breaks.bottom;
      fittedDirections.bottom -= breaks.bottom;
    }
    breaks = getIntersections(applyDirections(coordinates, fittedDirections), positionRestrictions);
  }
  var maxResize = {
    width: Infinity,
    height: Infinity,
    left: Infinity,
    right: Infinity,
    top: Infinity,
    bottom: Infinity
  };
  ALL_DIRECTIONS.forEach(function(direction) {
    var intersection = breaks[direction];
    if (intersection && fittedDirections[direction]) {
      maxResize[direction] = Math.max(0, 1 - intersection / fittedDirections[direction]);
    }
  });
  if (preserveRatio) {
    var multiplier_1 = Math.min.apply(Math, ALL_DIRECTIONS.map(function(direction) {
      return maxResize[direction];
    }));
    if (multiplier_1 !== Infinity) {
      ALL_DIRECTIONS.forEach(function(direction) {
        fittedDirections[direction] *= multiplier_1;
      });
    }
  } else {
    ALL_DIRECTIONS.forEach(function(direction) {
      if (maxResize[direction] !== Infinity) {
        fittedDirections[direction] *= maxResize[direction];
      }
    });
  }
  currentWidth = applyDirections(coordinates, fittedDirections).width;
  currentHeight = applyDirections(coordinates, fittedDirections).height;
  if (fittedDirections.right + fittedDirections.left) {
    if (currentWidth > sizeRestrictions2.maxWidth) {
      maxResize.width = (sizeRestrictions2.maxWidth - coordinates.width) / (fittedDirections.right + fittedDirections.left);
    } else if (currentWidth < sizeRestrictions2.minWidth) {
      maxResize.width = (sizeRestrictions2.minWidth - coordinates.width) / (fittedDirections.right + fittedDirections.left);
    }
  }
  if (fittedDirections.bottom + fittedDirections.top) {
    if (currentHeight > sizeRestrictions2.maxHeight) {
      maxResize.height = (sizeRestrictions2.maxHeight - coordinates.height) / (fittedDirections.bottom + fittedDirections.top);
    } else if (currentHeight < sizeRestrictions2.minHeight) {
      maxResize.height = (sizeRestrictions2.minHeight - coordinates.height) / (fittedDirections.bottom + fittedDirections.top);
    }
  }
  if (preserveRatio) {
    var multiplier_2 = Math.min(maxResize.width, maxResize.height);
    if (multiplier_2 !== Infinity) {
      ALL_DIRECTIONS.forEach(function(direction) {
        fittedDirections[direction] *= multiplier_2;
      });
    }
  } else {
    if (maxResize.width !== Infinity) {
      HORIZONTAL_DIRECTIONS.forEach(function(direction) {
        fittedDirections[direction] *= maxResize.width;
      });
    }
    if (maxResize.height !== Infinity) {
      VERTICAL_DIRECTIONS.forEach(function(direction) {
        fittedDirections[direction] *= maxResize.height;
      });
    }
  }
  return fittedDirections;
}
function distributeOverlap(overlap, first, second) {
  if (isRoughlyEqual(first + second, 0)) {
    return overlap / 2;
  } else if (isRoughlyEqual(first, 0)) {
    return 0;
  } else if (isRoughlyEqual(second, 0)) {
    return overlap;
  } else {
    return overlap * Math.abs(first / (first + second));
  }
}
function resizeCoordinatesAlgorithm(coordinates, directions, options, limitations) {
  var aspectRatio2 = limitations.aspectRatio, positionRestrictions = limitations.positionRestrictions, sizeRestrictions2 = limitations.sizeRestrictions;
  var actualCoordinates = __assign({}, coordinates);
  directions = __assign({}, directions);
  var allowedDirections = options.allowedDirections || {
    left: true,
    right: true,
    bottom: true,
    top: true
  };
  if (coordinates.width < sizeRestrictions2.minWidth) {
    directions.left = 0;
    directions.right = 0;
  }
  if (coordinates.height < sizeRestrictions2.minHeight) {
    directions.top = 0;
    directions.bottom = 0;
  }
  ALL_DIRECTIONS.forEach(function(direction) {
    if (!allowedDirections[direction]) {
      directions[direction] = 0;
    }
  });
  directions = fitDirections({
    coordinates: actualCoordinates,
    directions,
    sizeRestrictions: sizeRestrictions2,
    positionRestrictions,
    allowedDirections
  });
  var currentWidth = applyDirections(actualCoordinates, directions).width;
  var currentHeight = applyDirections(actualCoordinates, directions).height;
  var ratioBroken = options.preserveAspectRatio ? ratio(actualCoordinates) : getBrokenRatio(currentWidth / currentHeight, aspectRatio2);
  if (ratioBroken) {
    var respectDirection = options.respectDirection;
    if (respectDirection !== "width" && respectDirection !== "height") {
      if (actualCoordinates.width >= actualCoordinates.height || ratioBroken === 1) {
        respectDirection = "width";
      } else {
        respectDirection = "height";
      }
    }
    if (respectDirection === "width") {
      var overlapHeight = currentWidth / ratioBroken - actualCoordinates.height;
      if (allowedDirections.top && allowedDirections.bottom) {
        var top_1 = directions.top, bottom = directions.bottom;
        directions.bottom = distributeOverlap(overlapHeight, bottom, top_1);
        directions.top = distributeOverlap(overlapHeight, top_1, bottom);
      } else if (allowedDirections.bottom) {
        directions.bottom = overlapHeight;
      } else if (allowedDirections.top) {
        directions.top = overlapHeight;
      } else if (allowedDirections.right) {
        directions.right = 0;
      } else if (allowedDirections.left) {
        directions.left = 0;
      }
    } else if (respectDirection === "height") {
      var overlapWidth = actualCoordinates.width - currentHeight * ratioBroken;
      if (allowedDirections.left && allowedDirections.right) {
        var left = directions.left, right = directions.right;
        directions.left = -distributeOverlap(overlapWidth, left, right);
        directions.right = -distributeOverlap(overlapWidth, right, left);
      } else if (allowedDirections.left) {
        directions.left = -overlapWidth;
      } else if (allowedDirections.right) {
        directions.right = -overlapWidth;
      } else if (allowedDirections.top) {
        directions.top = 0;
      } else if (allowedDirections.bottom) {
        directions.bottom = 0;
      }
    }
    directions = fitDirections({
      directions,
      coordinates: actualCoordinates,
      sizeRestrictions: sizeRestrictions2,
      positionRestrictions,
      preserveRatio: true,
      compensate: options.compensate,
      allowedDirections
    });
  }
  currentWidth = applyDirections(actualCoordinates, directions).width;
  currentHeight = applyDirections(actualCoordinates, directions).height;
  ratioBroken = options.preserveAspectRatio ? ratio(actualCoordinates) : getBrokenRatio(currentWidth / currentHeight, aspectRatio2);
  if (ratioBroken && isGreater(Math.abs(ratioBroken - currentWidth / currentHeight), 0)) {
    ALL_DIRECTIONS.forEach(function(direction) {
      directions[direction] = 0;
    });
  }
  return moveCoordinatesAlgorithm({
    width: coordinates.width + directions.right + directions.left,
    height: coordinates.height + directions.top + directions.bottom,
    left: coordinates.left,
    top: coordinates.top
  }, {
    left: -directions.left,
    top: -directions.top
  }, positionRestrictions);
}
function anchorToMassPoint(coordinates, anchor) {
  var plainAnchor = anchor.toLowerCase();
  return {
    left: coordinates.left + coordinates.width * (plainAnchor.indexOf("west") !== -1 ? 1 : plainAnchor.indexOf("east") !== -1 ? 0 : 0.5),
    top: coordinates.top + coordinates.height * (plainAnchor.indexOf("north") !== -1 ? 1 : plainAnchor.indexOf("south") !== -1 ? 0 : 0.5)
  };
}
function anchorToAllowedDirections(anchor) {
  var plainAnchor = anchor.toLowerCase();
  return {
    left: plainAnchor.indexOf("east") === -1,
    top: plainAnchor.indexOf("south") === -1,
    right: plainAnchor.indexOf("west") === -1,
    bottom: plainAnchor.indexOf("north") === -1
  };
}
function anchorMoveToResizeDirections(anchor, directions) {
  var plainAnchor = anchor.toLowerCase();
  var normalizedDirections = {
    left: plainAnchor.indexOf("west") === -1 ? directions.left : -directions.left,
    top: -directions.top,
    right: directions.left,
    bottom: directions.top
  };
  if (["north", "south", "center"].every(function(el) {
    return plainAnchor.indexOf(el) === -1;
  })) {
    normalizedDirections.top = 0;
    normalizedDirections.bottom = 0;
  }
  if (["west", "east", "center"].every(function(el) {
    return plainAnchor.indexOf(el) === -1;
  })) {
    normalizedDirections.left = 0;
    normalizedDirections.right = 0;
  }
  var allowedDirections = anchorToAllowedDirections(anchor);
  ALL_DIRECTIONS.forEach(function(direction) {
    if (!allowedDirections[direction]) {
      normalizedDirections[direction] = 0;
    }
  });
  return normalizedDirections;
}
function anchoredResizeCoordinatesAlgorithm(coordinates, anchor, directions, options, limitations) {
  var reference = options.reference;
  var resizeDirections = anchorMoveToResizeDirections(anchor, directions);
  var allowedDirections = anchorToAllowedDirections(anchor);
  var result = resizeCoordinatesAlgorithm(coordinates, resizeDirections, __assign(__assign({}, options), { allowedDirections }), limitations);
  if (reference) {
    result = applyMove(result, diff(anchorToMassPoint(reference, anchor), anchorToMassPoint(result, anchor)));
  }
  return moveToPositionRestrictions(result, limitations.positionRestrictions);
}

// node_modules/advanced-cropper/state/resizeCoordinates.js
function resizeCoordinates(state, settings, anchor, directions, options) {
  var minimumSize = getMinimumSize(state);
  var sizeRestrictions2 = getSizeRestrictions(state, settings);
  return isInitializedState(state) ? __assign(__assign({}, copyState(state)), { coordinates: anchoredResizeCoordinatesAlgorithm(state.coordinates, anchor, directions, options, {
    positionRestrictions: mergePositionRestrictions(getPositionRestrictions(state, settings), coordinatesToPositionRestrictions(state.visibleArea)),
    sizeRestrictions: {
      maxWidth: Math.min(sizeRestrictions2.maxWidth, state.visibleArea.width),
      maxHeight: Math.min(sizeRestrictions2.maxHeight, state.visibleArea.height),
      minWidth: Math.max(Math.min(sizeRestrictions2.minWidth, state.visibleArea.width), minimumSize),
      minHeight: Math.max(Math.min(sizeRestrictions2.minHeight, state.visibleArea.height), minimumSize)
    },
    aspectRatio: getAspectRatio(state, settings)
  }) }) : state;
}

// node_modules/advanced-cropper/state/setBoundary.js
function setBoundary(state, settings, boundary) {
  var result = __assign(__assign({}, copyState(state)), { boundary });
  if (result.visibleArea && result.coordinates && state.visibleArea) {
    result.visibleArea.height = result.visibleArea.width / ratio(boundary);
    result.visibleArea.top += (state.visibleArea.height - result.visibleArea.height) / 2;
    var sizeRestrictions2 = getSizeRestrictions(result, settings);
    if (Math.max(sizeRestrictions2.minHeight, result.coordinates.height) - result.visibleArea.height > 0 || Math.max(sizeRestrictions2.minWidth, result.coordinates.width) - result.visibleArea.width > 0) {
      result.visibleArea = applyScale(result.visibleArea, Math.max(Math.max(sizeRestrictions2.minHeight, result.coordinates.height) / result.visibleArea.height, Math.max(sizeRestrictions2.minWidth, result.coordinates.width) / result.visibleArea.width));
    }
    result.visibleArea = resizeToSizeRestrictions(result.visibleArea, getAreaSizeRestrictions(result, settings));
    var move = inverseMove(fitToPositionRestrictions(result.coordinates, coordinatesToPositionRestrictions(result.visibleArea)));
    if (result.visibleArea.width < result.coordinates.width) {
      move.left = 0;
    }
    if (result.visibleArea.height < result.coordinates.height) {
      move.top = 0;
    }
    result.visibleArea = applyMove(result.visibleArea, move);
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
    result = fitCoordinates(result, settings);
  }
  return result;
}

// node_modules/advanced-cropper/algorithms/rotateImageAlgorithm.js
function rotateImageAlgorithm(state, settings, rotate) {
  if (isInitializedState(state)) {
    var result = copyState(state);
    var angle = isNumber(rotate) ? rotate : rotate.angle;
    var imageCenter = rotatePoint(getCenter(__assign({ left: 0, top: 0 }, getTransformedImageSize(state))), angle);
    result.transforms.rotate += angle;
    result.coordinates = __assign(__assign({}, approximateSize({
      sizeRestrictions: getSizeRestrictions(result, settings),
      aspectRatio: getAspectRatio(result, settings),
      width: result.coordinates.width,
      height: result.coordinates.height
    })), rotatePoint(getCenter(result.coordinates), angle));
    var center = !isNumber(rotate) && rotate.center ? rotate.center : getCenter(state.coordinates);
    var shift = diff(getCenter(state.coordinates), rotatePoint(getCenter(state.coordinates), angle, center));
    var imageSize = getTransformedImageSize(result);
    result.coordinates.left -= imageCenter.left - imageSize.width / 2 + result.coordinates.width / 2 - shift.left;
    result.coordinates.top -= imageCenter.top - imageSize.height / 2 + result.coordinates.height / 2 - shift.top;
    result.visibleArea = resizeToSizeRestrictions(result.visibleArea, mergeSizeRestrictions(getAreaSizeRestrictions(result, settings), {
      minWidth: result.coordinates.width,
      minHeight: result.coordinates.height
    }));
    result.coordinates = moveToPositionRestrictions(result.coordinates, getPositionRestrictions(result, settings));
    result.visibleArea = applyMove(result.visibleArea, diff(getCenter(result.coordinates), getCenter(state.coordinates)));
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
    return result;
  }
  return state;
}

// node_modules/advanced-cropper/algorithms/flipImageAlgorithm.js
function flipImageAlgorithm(state, settings, horizontal, vertical) {
  if (isInitializedState(state)) {
    var result = copyState(state);
    var rotate = state.transforms.rotate;
    var imageSize = getTransformedImageSize(state);
    var changed = {
      horizontal,
      vertical
    };
    if (changed.horizontal || changed.vertical) {
      var imageCenter = rotatePoint({
        left: imageSize.width / 2,
        top: imageSize.height / 2
      }, -rotate);
      var oldCenter = rotatePoint(getCenter(result.coordinates), -rotate);
      var newCenter = rotatePoint({
        left: changed.horizontal ? imageCenter.left - (oldCenter.left - imageCenter.left) : oldCenter.left,
        top: changed.vertical ? imageCenter.top - (oldCenter.top - imageCenter.top) : oldCenter.top
      }, rotate);
      result.coordinates = applyMove(result.coordinates, diff(newCenter, getCenter(result.coordinates)));
      oldCenter = rotatePoint(getCenter(result.visibleArea), -rotate);
      newCenter = rotatePoint({
        left: changed.horizontal ? imageCenter.left - (oldCenter.left - imageCenter.left) : oldCenter.left,
        top: changed.vertical ? imageCenter.top - (oldCenter.top - imageCenter.top) : oldCenter.top
      }, rotate);
      result.visibleArea = applyMove(result.visibleArea, diff(newCenter, getCenter(result.visibleArea)));
      result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
    }
    if (changed.horizontal) {
      result.transforms.flip.horizontal = !state.transforms.flip.horizontal;
    }
    if (changed.vertical) {
      result.transforms.flip.vertical = !state.transforms.flip.vertical;
    }
    return result;
  }
  return state;
}

// node_modules/advanced-cropper/algorithms/transformImageAlgorithm.js
function transformImageAlgorithm(state, settings, transform) {
  var _a;
  var _b = transform.scale, scale = _b === void 0 ? 1 : _b, _c = transform.move, move = _c === void 0 ? {} : _c;
  if (isInitializedState(state)) {
    var result = copyState(state);
    var scaleFactor = 1 / (isNumber(scale) ? scale : scale.factor);
    var scaleCenter = !isNumber(scale) ? scale.center : void 0;
    var isAllowedScale = !isRoughlyEqual(scaleFactor, 1);
    var areaScale = 1;
    var stencilScale = 1;
    result.visibleArea = applyMove(result.visibleArea, {
      left: move.left || 0,
      top: move.top || 0
    });
    var sizeRestrictions2 = getSizeRestrictions(result, settings);
    var positionRestrictions = getPositionRestrictions(result, settings);
    var areaSizeRestrictions = getAreaSizeRestrictions(result, settings);
    var scaleRestrictions = {
      stencil: {
        minimum: Math.max(sizeRestrictions2.minWidth ? sizeRestrictions2.minWidth / result.coordinates.width : 0, sizeRestrictions2.minHeight ? sizeRestrictions2.minHeight / result.coordinates.height : 0),
        maximum: Math.min(sizeRestrictions2.maxWidth ? sizeRestrictions2.maxWidth / result.coordinates.width : Infinity, sizeRestrictions2.maxHeight ? sizeRestrictions2.maxHeight / result.coordinates.height : Infinity, maxScale(result.coordinates, positionToSizeRestrictions(positionRestrictions)))
      },
      area: {
        maximum: maxScale(result.visibleArea, areaSizeRestrictions),
        minimum: minScale(result.visibleArea, areaSizeRestrictions)
      }
    };
    if (scaleFactor && isAllowedScale) {
      if (scaleFactor < 1) {
        stencilScale = Math.max(scaleFactor, Math.max(scaleRestrictions.area.minimum, scaleRestrictions.stencil.minimum));
        if (stencilScale > 1) {
          stencilScale = 1;
        }
      } else if (scaleFactor > 1) {
        stencilScale = Math.min(scaleFactor, Math.min(scaleRestrictions.area.maximum, scaleRestrictions.stencil.maximum));
        if (stencilScale < 1) {
          stencilScale = 1;
        }
      }
    }
    if (stencilScale) {
      result.visibleArea = applyScale(result.visibleArea, stencilScale, scaleCenter);
    }
    var relativeCoordinates = {
      left: state.coordinates.left - state.visibleArea.left,
      right: state.visibleArea.width + state.visibleArea.left - (state.coordinates.width + state.coordinates.left),
      top: state.coordinates.top - state.visibleArea.top,
      bottom: state.visibleArea.height + state.visibleArea.top - (state.coordinates.height + state.coordinates.top)
    };
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, {
      left: positionRestrictions.left !== void 0 ? positionRestrictions.left - relativeCoordinates.left * stencilScale : void 0,
      top: positionRestrictions.top !== void 0 ? positionRestrictions.top - relativeCoordinates.top * stencilScale : void 0,
      bottom: positionRestrictions.bottom !== void 0 ? positionRestrictions.bottom + relativeCoordinates.bottom * stencilScale : void 0,
      right: positionRestrictions.right !== void 0 ? positionRestrictions.right + relativeCoordinates.right * stencilScale : void 0
    });
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
    result.coordinates.width = result.coordinates.width * stencilScale;
    result.coordinates.height = result.coordinates.height * stencilScale;
    result.coordinates.left = result.visibleArea.left + relativeCoordinates.left * stencilScale;
    result.coordinates.top = result.visibleArea.top + relativeCoordinates.top * stencilScale;
    result.coordinates = moveToPositionRestrictions(result.coordinates, mergePositionRestrictions(coordinatesToPositionRestrictions(result.visibleArea), positionRestrictions));
    if (isAllowedScale && scaleFactor && ((_a = settings.transformImage) === null || _a === void 0 ? void 0 : _a.adjustStencil)) {
      if (scaleFactor > 1) {
        areaScale = Math.min(scaleRestrictions.area.maximum, scaleFactor) / stencilScale;
      } else if (scaleFactor < 1) {
        areaScale = Math.max(result.coordinates.height / result.visibleArea.height, result.coordinates.width / result.visibleArea.width, scaleFactor / stencilScale);
      }
      if (areaScale !== 1) {
        result.visibleArea = applyScale(result.visibleArea, areaScale, scaleFactor > 1 ? scaleCenter : getCenter(result.coordinates));
        result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
        result.visibleArea = applyMove(result.visibleArea, inverseMove(fitToPositionRestrictions(result.coordinates, coordinatesToPositionRestrictions(result.visibleArea))));
      }
    }
    return result;
  }
  return state;
}

// node_modules/advanced-cropper/state/transformImage.js
function transformImage(state, settings, transform) {
  if (transform.rotate) {
    state = rotateImageAlgorithm(state, settings, transform.rotate);
  }
  if (transform.flip) {
    state = flipImageAlgorithm(state, settings, transform.flip.horizontal, transform.flip.vertical);
  }
  if (transform.move || transform.scale) {
    state = transformImageAlgorithm(state, settings, transform);
  }
  return state;
}

// node_modules/advanced-cropper/state/reconcileState.js
function reconcileState(state, settings) {
  if (isInitializedState(state)) {
    var result = copyState(state);
    var aspectRatio2 = getAspectRatio(state, settings);
    var sizeRestrictions2 = getSizeRestrictions(state, settings);
    var areaSizeRestrictions = getAreaSizeRestrictions(state, settings);
    var brokenRatio = getBrokenRatio(ratio(state.coordinates), aspectRatio2);
    var desiredSize = brokenRatio ? {
      height: state.coordinates.height,
      width: state.coordinates.height * brokenRatio
    } : state.coordinates;
    result.coordinates = __assign(__assign({}, result.coordinates), approximateSize({
      width: desiredSize.width,
      height: desiredSize.height,
      aspectRatio: aspectRatio2,
      sizeRestrictions: mergeSizeRestrictions(areaSizeRestrictions, sizeRestrictions2)
    }));
    result.coordinates = applyMove(result.coordinates, diff(getCenter(state.coordinates), getCenter(result.coordinates)));
    var scaleModifier = Math.max(result.coordinates.width / result.visibleArea.width, result.coordinates.height / result.visibleArea.height, 1);
    result.visibleArea = __assign(__assign({}, state.visibleArea), approximateSize({
      width: state.visibleArea.width * scaleModifier,
      height: state.visibleArea.height * scaleModifier,
      aspectRatio: {
        minimum: ratio(result.boundary),
        maximum: ratio(result.boundary)
      },
      sizeRestrictions: areaSizeRestrictions
    }));
    result.visibleArea = applyMove(result.visibleArea, diff(getCenter(state.visibleArea), getCenter(result.visibleArea)));
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
    result.coordinates = moveToPositionRestrictions(result.coordinates, mergePositionRestrictions(coordinatesToPositionRestrictions(result.visibleArea), getPositionRestrictions(result, settings)));
    return result;
  }
  return state;
}

// node_modules/advanced-cropper/service/normalize.js
function normalizeResizeDirections(state, directions) {
  var coefficient = getCoefficient(state);
  return {
    left: isNumber(directions.left) ? directions.left * coefficient : 0,
    top: isNumber(directions.top) ? directions.top * coefficient : 0,
    right: isNumber(directions.right) ? directions.right * coefficient : 0,
    bottom: isNumber(directions.bottom) ? directions.bottom * coefficient : 0
  };
}
function normalizeCenter(state, center) {
  var coefficient = getCoefficient(state);
  if (state.visibleArea) {
    return {
      left: center.left * coefficient + state.visibleArea.left,
      top: center.top * coefficient + state.visibleArea.top
    };
  }
  return center;
}
function normalizeFlip(state, flip) {
  if (state) {
    var normalizedAngle = Math.abs(state.transforms.rotate % 180);
    if (normalizedAngle <= 45 || normalizedAngle >= 135) {
      return flip;
    } else {
      return {
        horizontal: flip.vertical,
        vertical: flip.horizontal
      };
    }
  }
  return flip;
}
function fillMoveDirections(directions) {
  return {
    left: isNumber(directions.left) ? directions.left : 0,
    top: isNumber(directions.top) ? directions.top : 0
  };
}
function fillResizeDirections(directions) {
  return {
    left: isNumber(directions.left) ? directions.left : 0,
    top: isNumber(directions.top) ? directions.top : 0,
    right: isNumber(directions.right) ? directions.right : 0,
    bottom: isNumber(directions.bottom) ? directions.bottom : 0
  };
}
function normalizeMoveDirections(state, directions) {
  var coefficient = getCoefficient(state);
  return {
    left: isNumber(directions.left) ? directions.left * coefficient : 0,
    top: isNumber(directions.top) ? directions.top * coefficient : 0
  };
}
function normalizeImageTransform(state, transform) {
  if (transform.scale) {
    transform.scale = {
      factor: isNumber(transform.scale) ? transform.scale : transform.scale.factor,
      center: !isNumber(transform.scale) && transform.scale.center ? normalizeCenter(state, transform.scale.center) : void 0
    };
  }
  if (transform.rotate) {
    transform.rotate = {
      angle: isNumber(transform.rotate) ? transform.rotate : transform.rotate.angle,
      center: !isNumber(transform.rotate) && transform.rotate.center ? normalizeCenter(state, transform.rotate.center) : void 0
    };
  }
  if (transform.move) {
    transform.move = normalizeMoveDirections(state, transform.move);
  }
  return transform;
}

// node_modules/advanced-cropper/service/imageTransforms.js
function calculateGeometricProperties(touches, container) {
  var _a = container.getBoundingClientRect(), left = _a.left, top = _a.top;
  var centerMass = { left: 0, top: 0 };
  var spread = 0;
  touches.forEach(function(touch) {
    centerMass.left += (touch.clientX - left) / touches.length;
    centerMass.top += (touch.clientY - top) / touches.length;
  });
  touches.forEach(function(touch) {
    spread += distance({ left: centerMass.left, top: centerMass.top }, { left: touch.clientX - left, top: touch.clientY - top });
  });
  return { centerMass, spread, count: touches.length };
}
function touchesToImageTransform(touches, previousTouches, container, options) {
  if (options === void 0) {
    options = {};
  }
  var move, scale, rotate;
  if (previousTouches.length === 1 && touches.length === 1) {
    if (options.move) {
      move = {
        left: previousTouches[0].clientX - touches[0].clientX,
        top: previousTouches[0].clientY - touches[0].clientY
      };
    }
  } else if (touches.length > 1) {
    var previousProperties = calculateGeometricProperties(previousTouches, container);
    var properties = calculateGeometricProperties(touches, container);
    if (options.rotate && previousTouches.length === 2 && touches.length === 2) {
      var diffs = {
        left: [
          touches[0].clientX - touches[1].clientX,
          previousTouches[0].clientX - previousTouches[1].clientX
        ],
        top: [touches[0].clientY - touches[1].clientY, previousTouches[0].clientY - previousTouches[1].clientY]
      };
      var y = diffs.left[0] * diffs.top[1] - diffs.left[1] * diffs.top[0];
      var x = diffs.left[0] * diffs.left[1] + diffs.top[0] * diffs.top[1];
      if (!isRoughlyEqual(x, 0) && !isRoughlyEqual(y, 0)) {
        var radians = Math.atan2(y, x);
        var angle = -(radians * 180) / Math.PI;
        rotate = {
          center: properties.centerMass,
          angle
        };
      }
    }
    if (options.move) {
      move = {
        left: previousProperties.centerMass.left - properties.centerMass.left,
        top: previousProperties.centerMass.top - properties.centerMass.top
      };
    }
    if (options.scale) {
      scale = {
        factor: properties.spread / previousProperties.spread,
        center: properties.centerMass
      };
    }
  }
  return {
    move,
    scale,
    rotate
  };
}
function wheelEventToImageTransform(event, container, ratio2) {
  if (ratio2 === void 0) {
    ratio2 = 0.1;
  }
  var _a = container.getBoundingClientRect(), left = _a.left, top = _a.top;
  var factor = 1 - ratio2 * sign(event.deltaY || event.detail || event.wheelDelta);
  var center = {
    left: event.clientX - left,
    top: event.clientY - top
  };
  return { scale: { factor, center } };
}

// node_modules/advanced-cropper/service/interactions.js
function hasInteractions(interactions) {
  return interactions.moveCoordinates || interactions.resizeCoordinates || interactions.transformImage.move || interactions.transformImage.rotate || interactions.transformImage.flip || interactions.transformImage.scale;
}
function getEmptyInteractions() {
  return {
    moveCoordinates: false,
    resizeCoordinates: false,
    transformImage: {
      rotate: false,
      move: false,
      scale: false,
      flip: false
    }
  };
}

// node_modules/advanced-cropper/service/isEqualState.js
function isEqualState(a, b) {
  return deepCompare(a, b);
}

// node_modules/advanced-cropper/defaults/defaultPositionRestrictions.js
function defaultPositionRestrictions(state, settings) {
  var imageSize = getTransformedImageSize(state);
  var limits = {};
  if (settings.imageRestriction && settings.imageRestriction !== "none") {
    limits = {
      left: 0,
      top: 0,
      right: imageSize.width,
      bottom: imageSize.height
    };
  }
  return limits;
}

// node_modules/advanced-cropper/defaults/defaultVisibleArea.js
function defaultVisibleArea(state, settings) {
  var coordinates = state.coordinates, boundary = state.boundary;
  var imageSize = getTransformedImageSize(state);
  var boundaryRatio = ratio(boundary);
  if (coordinates) {
    var reference = {
      height: Math.max(coordinates.height, imageSize.height),
      width: Math.max(coordinates.width, imageSize.width)
    };
    var visibleArea = {
      left: 0,
      top: 0,
      width: ratio(reference) > boundaryRatio ? reference.width : reference.height * boundaryRatio,
      height: ratio(reference) > boundaryRatio ? reference.width / boundaryRatio : reference.height
    };
    visibleArea = resizeToSizeRestrictions(visibleArea, getAreaSizeRestrictions(state, settings));
    visibleArea.left = coordinates.left + coordinates.width / 2 - visibleArea.width / 2;
    visibleArea.top = coordinates.top + coordinates.height / 2 - visibleArea.height / 2;
    var coordinatesIntersection = getIntersections(coordinates, coordinatesToPositionRestrictions(__assign({ left: 0, top: 0 }, imageSize)));
    var restrictions = {};
    if (!coordinatesIntersection.left && !coordinatesIntersection.right && visibleArea.width <= imageSize.width) {
      restrictions.left = 0;
      restrictions.right = imageSize.width;
    }
    if (!coordinatesIntersection.top && !coordinatesIntersection.bottom && visibleArea.height <= imageSize.height) {
      restrictions.top = 0;
      restrictions.bottom = imageSize.height;
    }
    return moveToPositionRestrictions(visibleArea, restrictions);
  } else {
    var imageRatio = ratio(imageSize);
    var areaProperties = {
      height: imageRatio < boundaryRatio ? imageSize.height : imageSize.width / boundaryRatio,
      width: imageRatio < boundaryRatio ? imageSize.height * boundaryRatio : imageSize.width
    };
    return {
      left: imageSize.width / 2 - areaProperties.width / 2,
      top: imageSize.height / 2 - areaProperties.height / 2,
      width: areaProperties.width,
      height: areaProperties.height
    };
  }
}

// node_modules/advanced-cropper/defaults/defaultSize.js
function defaultSize(state, settings) {
  var imageSize = state.imageSize, visibleArea = state.visibleArea;
  var sizeRestrictions2 = getSizeRestrictions(state, settings);
  var aspectRatio2 = getAspectRatio(state, settings);
  var area;
  if (visibleArea) {
    area = visibleArea;
  } else {
    var sizeRestrictions_1 = positionToSizeRestrictions(getPositionRestrictions(state, settings));
    area = {
      width: Math.max(sizeRestrictions_1.minWidth, Math.min(sizeRestrictions_1.maxWidth, imageSize.width)),
      height: Math.max(sizeRestrictions_1.minHeight, Math.min(sizeRestrictions_1.maxHeight, imageSize.height))
    };
  }
  var optimalRatio = Math.min(aspectRatio2.maximum || Infinity, Math.max(aspectRatio2.minimum || 0, ratio(area)));
  var size = area.width < area.height ? {
    width: area.width * 0.8,
    height: area.width * 0.8 / optimalRatio
  } : {
    height: area.height * 0.8,
    width: area.height * 0.8 * optimalRatio
  };
  return approximateSize(__assign(__assign({}, size), { aspectRatio: aspectRatio2, sizeRestrictions: sizeRestrictions2 }));
}

// node_modules/advanced-cropper/defaults/defaultSizeRestrictions.js
function retrieveSizeRestrictions(settings) {
  return {
    minWidth: isNumeric(settings.minWidth) ? parseNumber(settings.minWidth) : 0,
    minHeight: isNumeric(settings.minHeight) ? parseNumber(settings.minHeight) : 0,
    maxWidth: isNumeric(settings.maxWidth) ? parseNumber(settings.maxWidth) : Infinity,
    maxHeight: isNumeric(settings.maxHeight) ? parseNumber(settings.maxHeight) : Infinity
  };
}
function pixelsRestrictions(state, settings) {
  return retrieveSizeRestrictions(settings);
}

// node_modules/advanced-cropper/defaults/defaultPosition.js
function defaultPosition(state) {
  var visibleArea = state.visibleArea, coordinates = state.coordinates;
  var area = visibleArea || getTransformedImageSize(state);
  return {
    left: (visibleArea ? visibleArea.left : 0) + area.width / 2 - (coordinates ? coordinates.width / 2 : 0),
    top: (visibleArea ? visibleArea.top : 0) + area.height / 2 - (coordinates ? coordinates.height / 2 : 0)
  };
}

// node_modules/advanced-cropper/defaults/defaultAreaPositionRestrictions.js
function defaultAreaPositionRestrictions(state, settings) {
  var visibleArea = state.visibleArea, boundary = state.boundary;
  var imageRestriction = settings.imageRestriction;
  var imageSize = getTransformedImageSize(state);
  var restrictions = {};
  if (imageRestriction === ImageRestriction.fillArea) {
    restrictions = {
      left: 0,
      top: 0,
      right: imageSize.width,
      bottom: imageSize.height
    };
  } else if (imageRestriction === ImageRestriction.fitArea) {
    if (ratio(boundary) > ratio(imageSize)) {
      restrictions = {
        top: 0,
        bottom: imageSize.height
      };
      if (visibleArea) {
        if (visibleArea.width > imageSize.width) {
          restrictions.left = -(visibleArea.width - imageSize.width) / 2;
          restrictions.right = imageSize.width - restrictions.left;
        } else {
          restrictions.left = 0;
          restrictions.right = imageSize.width;
        }
      }
    } else {
      restrictions = {
        left: 0,
        right: imageSize.width
      };
      if (visibleArea) {
        if (visibleArea.height > imageSize.height) {
          restrictions.top = -(visibleArea.height - imageSize.height) / 2;
          restrictions.bottom = imageSize.height - restrictions.top;
        } else {
          restrictions.top = 0;
          restrictions.bottom = imageSize.height;
        }
      }
    }
  }
  return restrictions;
}

// node_modules/advanced-cropper/defaults/defaultAreaSizeRestrictions.js
function defaultAreaSizeRestrictions(state, settings) {
  var boundary = state.boundary;
  var imageRestriction = settings.imageRestriction;
  var imageSize = getTransformedImageSize(state);
  var restrictions = {
    minWidth: 0,
    minHeight: 0,
    maxWidth: Infinity,
    maxHeight: Infinity
  };
  if (imageRestriction === ImageRestriction.fillArea) {
    restrictions.maxWidth = imageSize.width;
    restrictions.maxHeight = imageSize.height;
  } else if (imageRestriction === ImageRestriction.fitArea) {
    if (ratio(boundary) > ratio(imageSize)) {
      restrictions.maxHeight = imageSize.height;
      restrictions.maxWidth = imageSize.height * ratio(boundary);
    } else {
      restrictions.maxWidth = imageSize.width;
      restrictions.maxHeight = imageSize.width / ratio(boundary);
    }
  }
  return restrictions;
}

// node_modules/advanced-cropper/defaults/defaultBoundary.js
function fitBoundary(boundary, size) {
  var areaHeight = boundary.clientHeight;
  var areaWidth = boundary.clientWidth;
  var currentHeight = areaHeight;
  var currentWidth = size.width * areaHeight / size.height;
  if (currentWidth > areaWidth) {
    currentWidth = areaWidth;
    currentHeight = size.height * areaWidth / size.width;
  }
  return {
    width: currentWidth,
    height: currentHeight
  };
}
function fillBoundary(boundary) {
  var _a = boundary.getBoundingClientRect(), width = _a.width, height = _a.height;
  return {
    width,
    height
  };
}

// node_modules/advanced-cropper/defaults/defaultStencilConstraints.js
function defaultStencilConstraints(rawSettings, stencilProps) {
  if (!rawSettings.aspectRatio) {
    return {
      aspectRatio: createAspectRatio(isFunction(stencilProps.aspectRatio) ? stencilProps.aspectRatio() : stencilProps.aspectRatio)
    };
  }
  return {};
}

// node_modules/advanced-cropper/defaults/index.js
function withDefaultSizeRestrictions(sizeRestrictions2) {
  return function(state, basicSettings) {
    var value = isFunction(sizeRestrictions2) ? sizeRestrictions2(state, basicSettings) : sizeRestrictions2;
    return mergeSizeRestrictions(pixelsRestrictions(state, basicSettings), value);
  };
}
function withDefaultPositionRestrictions(positionRestrictions) {
  return function(state, basicSettings) {
    var value = isFunction(positionRestrictions) ? positionRestrictions(state, basicSettings) : positionRestrictions;
    return mergePositionRestrictions(defaultPositionRestrictions(state, basicSettings), value);
  };
}
function withDefaultAreaPositionRestrictions(areaPositionRestrictions) {
  return function(state, basicSettings) {
    var value = isFunction(areaPositionRestrictions) ? areaPositionRestrictions(state, basicSettings) : areaPositionRestrictions;
    return mergePositionRestrictions(defaultAreaPositionRestrictions(state, basicSettings), value);
  };
}
function withDefaultAreaSizeRestrictions(areaSizeRestrictions) {
  return function(state, basicSettings) {
    var value = isFunction(areaSizeRestrictions) ? areaSizeRestrictions(state, basicSettings) : areaSizeRestrictions;
    return mergeSizeRestrictions(defaultAreaSizeRestrictions(state, basicSettings), value);
  };
}
function createDefaultSettings(params) {
  return __assign(__assign({}, params), { sizeRestrictions: function(state, basicSettings) {
    var restrictions;
    if (params.sizeRestrictions) {
      restrictions = isFunction(params.sizeRestrictions) ? params.sizeRestrictions(state, basicSettings) : params.sizeRestrictions;
    } else {
      restrictions = pixelsRestrictions(state, basicSettings);
    }
    return restrictions;
  }, areaPositionRestrictions: function(state, basicSettings) {
    if (params.areaPositionRestrictions) {
      return isFunction(params.areaPositionRestrictions) ? params.areaPositionRestrictions(state, basicSettings) : params.areaPositionRestrictions;
    } else {
      return defaultAreaPositionRestrictions(state, basicSettings);
    }
  }, areaSizeRestrictions: function(state, basicSettings) {
    if (params.areaSizeRestrictions) {
      return isFunction(params.areaSizeRestrictions) ? params.areaSizeRestrictions(state, basicSettings) : params.areaSizeRestrictions;
    } else {
      return defaultAreaSizeRestrictions(state, basicSettings);
    }
  }, positionRestrictions: function(state, basicSettings) {
    if (params.positionRestrictions) {
      return isFunction(params.positionRestrictions) ? params.positionRestrictions(state, basicSettings) : params.positionRestrictions;
    } else {
      return defaultPositionRestrictions(state, basicSettings);
    }
  }, defaultCoordinates: function(state, basicSettings) {
    if (params.defaultCoordinates) {
      return isFunction(params.defaultCoordinates) ? params.defaultCoordinates(state, basicSettings) : params.defaultCoordinates;
    } else {
      var defaultSizeAlgorithm = params.defaultSize;
      if (!defaultSizeAlgorithm) {
        defaultSizeAlgorithm = defaultSize;
      }
      var size = isFunction(defaultSizeAlgorithm) ? defaultSizeAlgorithm(state, basicSettings) : defaultSizeAlgorithm;
      var defaultPositionAlgorithm_1 = params.defaultPosition || defaultPosition;
      return [
        size,
        function(state2) {
          return __assign({}, isFunction(defaultPositionAlgorithm_1) ? defaultPositionAlgorithm_1(state2, basicSettings) : defaultPositionAlgorithm_1);
        }
      ];
    }
  }, defaultVisibleArea: function(state, basicSettings) {
    if (params.defaultVisibleArea) {
      return isFunction(params.defaultVisibleArea) ? params.defaultVisibleArea(state, basicSettings) : params.defaultVisibleArea;
    } else {
      return defaultVisibleArea(state, basicSettings);
    }
  }, aspectRatio: function(state, basicSettings) {
    return createAspectRatio(isFunction(params.aspectRatio) ? params.aspectRatio(state, basicSettings) : params.aspectRatio);
  } });
}

// node_modules/advanced-cropper/instance/AbstractCropperInstance.js
function runCallback(callback, getInstance) {
  if (callback && getInstance) {
    var instance = getInstance();
    if (instance) {
      callback(instance);
    }
  }
}
function runCallbacks(callbacks, getInstance) {
  callbacks.forEach(function(callback) {
    runCallback(callback, getInstance);
  });
}
var AbstractCropperInstance = (
  /** @class */
  /* @__PURE__ */ function() {
    function AbstractCropperInstance2() {
      var _this = this;
      this.getTransitions = function() {
        var data = _this.getData();
        var transitions = _this.getProps().transitions;
        return __assign(__assign({}, getOptions(transitions, {
          timingFunction: "ease-in-out",
          duration: 350
        })), { active: data.transitions });
      };
      this.getInteractions = function() {
        var interactions = _this.getData().interactions;
        return deepClone(interactions);
      };
      this.hasInteractions = function() {
        var interactions = _this.getData().interactions;
        return hasInteractions(interactions);
      };
      this.startTransitions = function() {
        var _a = _this.getProps(), onTransitionsStart = _a.onTransitionsStart, onUpdate = _a.onUpdate, getInstance = _a.getInstance;
        var _b = _this.getData(), transitions = _b.transitions, data = __rest(_b, ["transitions"]);
        _this.setData(__assign(__assign({}, data), { transitions: true }));
        if (!transitions) {
          runCallbacks([onTransitionsStart, onUpdate], getInstance);
        }
        _this.endTransitions();
      };
      this.endTransitions = debounce(function() {
        var _a = _this.getProps(), onTransitionsEnd = _a.onTransitionsEnd, onUpdate = _a.onUpdate, getInstance = _a.getInstance;
        _this.setData(__assign(__assign({}, _this.getData()), { transitions: false }));
        runCallbacks([onTransitionsEnd, onUpdate], getInstance);
      }, function() {
        return _this.getTransitions().duration;
      });
      this.applyPostProcess = function(action, state) {
        var _a = _this.getProps(), settings = _a.settings, postProcess = _a.postProcess;
        var name = action.name, _b = action.interaction, interaction = _b === void 0 ? false : _b, _c = action.transitions, transitions = _c === void 0 ? false : _c, _d = action.immediately, immediately = _d === void 0 ? false : _d;
        var preparedAction = {
          name,
          interaction,
          transitions,
          immediately
        };
        if (isArray(postProcess)) {
          return postProcess.reduce(function(processedState, p) {
            return p(processedState, settings, preparedAction);
          }, state);
        } else if (isFunction(postProcess)) {
          return postProcess(state, settings, preparedAction);
        } else {
          return state;
        }
      };
      this.updateState = function(modifier, options, callbacks) {
        if (options === void 0) {
          options = {};
        }
        if (callbacks === void 0) {
          callbacks = [];
        }
        var _a = options.transitions, transitions = _a === void 0 ? false : _a;
        var _b = _this.getProps(), onTransitionsStart = _b.onTransitionsStart, getInstance = _b.getInstance, onChange = _b.onChange, onUpdate = _b.onUpdate, settings = _b.settings;
        var previousData = _this.getData();
        var state = isFunction(modifier) ? modifier(previousData.state, settings) : modifier;
        var tolerance = state ? 1e-3 * getCoefficient(state) : 1e-3;
        var somethingChanged = !deepCompare(previousData.state, state, tolerance);
        var affectTransitionProperties = [
          "coordinates",
          "boundary",
          "visibleArea",
          "imageSize",
          "transforms"
        ].some(function(property) {
          var _a2;
          return !deepCompare((_a2 = previousData.state) === null || _a2 === void 0 ? void 0 : _a2[property], state === null || state === void 0 ? void 0 : state[property], tolerance);
        });
        var currentData = previousData;
        if (somethingChanged) {
          if (transitions && affectTransitionProperties) {
            _this.endTransitions();
          }
          currentData = __assign(__assign({}, currentData), { state: copyState(state), transitions: transitions && affectTransitionProperties });
          _this.setData(currentData);
          runCallback(onChange, getInstance);
        }
        if (currentData.transitions && !previousData.transitions) {
          runCallback(onTransitionsStart, getInstance);
        }
        runCallbacks(__spreadArrays(callbacks, [onUpdate]), getInstance);
      };
      this.setInteractions = function(interactions) {
        var _a = _this.getProps(), onInteractionStart = _a.onInteractionStart, onInteractionEnd = _a.onInteractionEnd, getInstance = _a.getInstance;
        var previousInteractions = _this.getInteractions();
        var currentInteractions = __assign(__assign({}, previousInteractions), interactions);
        if (!deepCompare(previousInteractions, currentInteractions)) {
          _this.setData(__assign(__assign({}, _this.getData()), { interactions: currentInteractions }));
        }
        if (hasInteractions(previousInteractions) !== hasInteractions(currentInteractions)) {
          if (!hasInteractions(previousInteractions)) {
            runCallback(onInteractionStart, getInstance);
          } else {
            var state_1 = _this.getData().state;
            _this.updateState(function() {
              return state_1 && _this.applyPostProcess({
                name: "interactionEnd",
                immediately: true,
                transitions: true
              }, state_1);
            }, {
              transitions: true
            }, [onInteractionEnd]);
          }
        }
      };
      this.resetState = function(boundary, image) {
        _this.updateState(_this.createDefaultState(boundary, image));
      };
      this.clear = function() {
        _this.updateState(null);
      };
      this.reconcileState = function(options) {
        if (options === void 0) {
          options = {};
        }
        var _a = _this.getProps(), reconcileStateAlgorithm = _a.reconcileStateAlgorithm, settings = _a.settings;
        var state = _this.getData().state;
        var _b = options.transitions, transitions = _b === void 0 ? false : _b;
        if (state && !isConsistentState(state, settings)) {
          var reconciledState = (reconcileStateAlgorithm || reconcileState)(state, settings);
          reconciledState = _this.applyPostProcess({
            name: "reconcileState",
            immediately: true,
            transitions
          }, reconciledState);
          _this.updateState(reconciledState, {
            transitions
          });
        }
      };
      this.transformImage = function(transform, options) {
        if (options === void 0) {
          options = {};
        }
        var _a = options.transitions, transitions = _a === void 0 ? true : _a, _b = options.interaction, interaction = _b === void 0 ? true : _b, _c = options.immediately, immediately = _c === void 0 ? false : _c, _d = options.normalize, normalize = _d === void 0 ? true : _d;
        var _e = _this.getProps(), transformImageAlgorithm2 = _e.transformImageAlgorithm, onTransformImage = _e.onTransformImage, onTransformImageEnd = _e.onTransformImageEnd, settings = _e.settings;
        var state = _this.getData().state;
        var callbacks = [];
        if (state) {
          if (normalize) {
            transform = normalizeImageTransform(state, transform);
          }
          var result = _this.applyPostProcess({
            name: "transformImage",
            transitions,
            immediately
          }, (transformImageAlgorithm2 || transformImage)(state, settings, transform));
          callbacks.push(onTransformImage);
          if (interaction) {
            _this.setInteractions({
              transformImage: {
                rotate: !isUndefined(transform.rotate),
                flip: !isUndefined(transform.flip),
                scale: !isUndefined(transform.scale),
                move: !isUndefined(transform.move)
              }
            });
          } else {
            result = _this.applyPostProcess({
              name: "transformImageEnd",
              transitions,
              immediately: true
            }, result);
            callbacks.push(onTransformImageEnd);
          }
          _this.updateState(result, {
            transitions: immediately && transitions
          }, callbacks);
        }
      };
      this.transformImageEnd = function(options) {
        if (options === void 0) {
          options = {};
        }
        var _a = options.immediately, immediately = _a === void 0 ? true : _a, _b = options.transitions, transitions = _b === void 0 ? true : _b;
        var state = _this.getData().state;
        var onTransformImageEnd = _this.getProps().onTransformImageEnd;
        _this.updateState(function() {
          return state && _this.applyPostProcess({ name: "transformImageEnd", immediately, transitions }, state);
        }, {
          transitions
        }, [onTransformImageEnd]);
        _this.setInteractions({
          transformImage: {
            rotate: false,
            flip: false,
            scale: false,
            move: false
          }
        });
      };
      this.zoomImage = function(scale, options) {
        if (options === void 0) {
          options = {};
        }
        var _a = options.interaction, interaction = _a === void 0 ? false : _a, _b = options.immediately, immediately = _b === void 0 ? true : _b, _c = options.transitions, transitions = _c === void 0 ? true : _c, _d = options.normalize, normalize = _d === void 0 ? false : _d;
        _this.transformImage({
          scale
        }, { interaction, immediately, transitions, normalize });
      };
      this.moveImage = function(left, top, options) {
        if (options === void 0) {
          options = {};
        }
        var _a = options.interaction, interaction = _a === void 0 ? false : _a, _b = options.immediately, immediately = _b === void 0 ? true : _b, _c = options.transitions, transitions = _c === void 0 ? true : _c, _d = options.normalize, normalize = _d === void 0 ? false : _d;
        _this.transformImage({
          move: {
            left,
            top
          }
        }, { interaction, immediately, transitions, normalize });
      };
      this.flipImage = function(horizontal, vertical, options) {
        if (options === void 0) {
          options = {};
        }
        var _a = options.interaction, interaction = _a === void 0 ? false : _a, _b = options.immediately, immediately = _b === void 0 ? true : _b, _c = options.transitions, transitions = _c === void 0 ? true : _c, _d = options.normalize, normalize = _d === void 0 ? true : _d;
        var state = _this.getState();
        var flip = {
          horizontal,
          vertical
        };
        _this.transformImage({
          flip: state && normalize ? normalizeFlip(state, flip) : flip
        }, { interaction, immediately, transitions });
      };
      this.rotateImage = function(rotate, options) {
        if (options === void 0) {
          options = {};
        }
        var _a = options.interaction, interaction = _a === void 0 ? false : _a, _b = options.immediately, immediately = _b === void 0 ? true : _b, _c = options.transitions, transitions = _c === void 0 ? true : _c, _d = options.normalize, normalize = _d === void 0 ? false : _d;
        _this.transformImage({
          rotate
        }, { interaction, immediately, transitions, normalize });
      };
      this.reset = function(boundary, image) {
        _this.resetState(boundary, image);
      };
      this.setState = function(modifier, options) {
        if (options === void 0) {
          options = {};
        }
        var settings = _this.getSettings();
        var state = _this.getData().state;
        var _a = options.transitions, transitions = _a === void 0 ? true : _a, _b = options.immediately, immediately = _b === void 0 ? false : _b, _c = options.interaction, interaction = _c === void 0 ? false : _c, _d = options.postprocess, postprocess = _d === void 0 ? false : _d;
        var newState = modifier && (isFunction(modifier) ? modifier(state, settings) : __assign(__assign({}, state), modifier));
        _this.updateState(function() {
          return postprocess ? newState && _this.applyPostProcess({
            name: "setState",
            immediately,
            transitions,
            interaction
          }, newState) : newState;
        }, {
          transitions
        });
      };
      this.setCoordinates = function(transforms, options) {
        if (options === void 0) {
          options = {};
        }
        var state = _this.getData().state;
        var _a = _this.getProps(), setCoordinatesAlgorithm = _a.setCoordinatesAlgorithm, settings = _a.settings;
        var _b = options.transitions, transitions = _b === void 0 ? true : _b, _c = options.immediately, immediately = _c === void 0 ? true : _c;
        _this.updateState(function() {
          return state && _this.applyPostProcess({
            name: "setCoordinates",
            immediately,
            transitions
          }, (setCoordinatesAlgorithm || setCoordinates)(state, settings, transforms, SetCoordinatesMode.zoom));
        }, {
          transitions
        });
      };
      this.setVisibleArea = function(visibleArea, options) {
        if (options === void 0) {
          options = {};
        }
        var _a = options.transitions, transitions = _a === void 0 ? true : _a, _b = options.immediately, immediately = _b === void 0 ? true : _b;
        var state = _this.getData().state;
        var _c = _this.getProps(), setVisibleAreaAlgorithm = _c.setVisibleAreaAlgorithm, settings = _c.settings;
        _this.updateState(function() {
          return state && _this.applyPostProcess({ name: "setVisibleArea", immediately, transitions }, (setVisibleAreaAlgorithm || setVisibleArea)(state, settings, visibleArea));
        }, {
          transitions
        });
      };
      this.setBoundary = function(boundary, options) {
        if (options === void 0) {
          options = {};
        }
        var state = _this.getData().state;
        var _a = _this.getProps(), setBoundaryAlgorithm = _a.setBoundaryAlgorithm, settings = _a.settings;
        var _b = options.transitions, transitions = _b === void 0 ? false : _b, _c = options.immediately, immediately = _c === void 0 ? true : _c;
        if (boundary) {
          _this.updateState(function() {
            return state && _this.applyPostProcess({ name: "setBoundary", immediately, transitions }, (setBoundaryAlgorithm || setBoundary)(state, settings, boundary));
          });
        } else {
          _this.updateState(null);
        }
      };
      this.moveCoordinates = function(directions, options) {
        if (options === void 0) {
          options = {};
        }
        var data = _this.getData();
        var _a = _this.getProps(), moveCoordinatesAlgorithm2 = _a.moveCoordinatesAlgorithm, onMove = _a.onMove, onMoveEnd = _a.onMoveEnd, settings = _a.settings;
        var _b = options.interaction, interaction = _b === void 0 ? true : _b, _c = options.transitions, transitions = _c === void 0 ? false : _c, _d = options.immediately, immediately = _d === void 0 ? false : _d, _e = options.normalize, normalize = _e === void 0 ? true : _e;
        var callbacks = [];
        if (!data.transitions && data.state) {
          var normalizedDirections = normalize ? normalizeMoveDirections(data.state, directions) : fillMoveDirections(directions);
          var result = _this.applyPostProcess({ name: "moveCoordinates", interaction, immediately, transitions }, (moveCoordinatesAlgorithm2 || moveCoordinates)(data.state, settings, normalizedDirections));
          callbacks.push(onMove);
          if (interaction) {
            _this.setInteractions({
              moveCoordinates: true
            });
          } else {
            result = _this.applyPostProcess({ name: "moveCoordinatesEnd", interaction, immediately, transitions }, result);
            callbacks.push(onMoveEnd);
          }
          _this.updateState(result, {
            transitions: immediately && transitions
          }, callbacks);
        }
      };
      this.moveCoordinatesEnd = function(options) {
        if (options === void 0) {
          options = {};
        }
        var state = _this.getData().state;
        var onMoveEnd = _this.getProps().onMoveEnd;
        var _a = options.transitions, transitions = _a === void 0 ? true : _a, _b = options.immediately, immediately = _b === void 0 ? false : _b;
        _this.updateState(function() {
          return state && _this.applyPostProcess({ name: "moveCoordinatesEnd", transitions, immediately }, state);
        }, {
          transitions
        }, [onMoveEnd]);
        _this.setInteractions({
          moveCoordinates: false
        });
      };
      this.resizeCoordinates = function(anchor, directions, parameters, options) {
        if (options === void 0) {
          options = {};
        }
        var state = _this.getData().state;
        var _a = _this.getProps(), resizeCoordinatesAlgorithm2 = _a.resizeCoordinatesAlgorithm, onResize = _a.onResize, onResizeEnd = _a.onResizeEnd, settings = _a.settings;
        var _b = options.interaction, interaction = _b === void 0 ? true : _b, _c = options.transitions, transitions = _c === void 0 ? false : _c, _d = options.immediately, immediately = _d === void 0 ? false : _d, _e = options.normalize, normalize = _e === void 0 ? true : _e;
        var transitionsOptions = _this.getTransitions();
        if (!transitionsOptions.active && state) {
          var callbacks = [];
          var normalizedDirections = normalize ? normalizeResizeDirections(state, directions) : fillResizeDirections(directions);
          var result = _this.applyPostProcess({ name: "resizeCoordinates", interaction, immediately, transitions }, (resizeCoordinatesAlgorithm2 || resizeCoordinates)(state, settings, anchor, normalizedDirections, isObject(parameters) ? parameters : {}));
          callbacks.push(onResize);
          if (interaction) {
            _this.setInteractions({
              resizeCoordinates: true
            });
          } else {
            result = _this.applyPostProcess({ name: "resizeCoordinatesEnd", interaction, immediately, transitions }, result);
            callbacks.push(onResizeEnd);
          }
          _this.updateState(result, {
            transitions: immediately && transitions
          }, callbacks);
        }
      };
      this.resizeCoordinatesEnd = function(options) {
        if (options === void 0) {
          options = {};
        }
        var onResizeEnd = _this.getProps().onResizeEnd;
        var state = _this.getData().state;
        var _a = options.transitions, transitions = _a === void 0 ? true : _a, _b = options.immediately, immediately = _b === void 0 ? false : _b;
        _this.updateState(function() {
          return state && _this.applyPostProcess({ name: "resizeCoordinatesEnd", transitions, immediately }, state);
        }, {
          transitions
        }, [onResizeEnd]);
        _this.setInteractions({
          resizeCoordinates: false
        });
      };
      this.getStencilCoordinates = function() {
        var state = _this.getData().state;
        return getStencilCoordinates(state);
      };
      this.getCoordinates = function(options) {
        if (options === void 0) {
          options = {};
        }
        var state = _this.getData().state;
        var settings = _this.getProps().settings;
        if (state && state.coordinates) {
          var _a = options.round, round = _a === void 0 ? true : _a;
          if (round) {
            return getRoundedCoordinates(state, settings);
          } else {
            return __assign({}, state.coordinates);
          }
        } else {
          return null;
        }
      };
      this.getVisibleArea = function() {
        var state = _this.getData().state;
        if (state === null || state === void 0 ? void 0 : state.visibleArea) {
          return __assign({}, state.visibleArea);
        } else {
          return null;
        }
      };
      this.getSettings = function() {
        var settings = _this.getProps().settings;
        return __assign({}, settings);
      };
      this.getState = function() {
        var state = _this.getData().state;
        return copyState(state);
      };
      this.getTransforms = function() {
        var state = _this.getData().state;
        return state ? deepClone(state.transforms) : {
          rotate: 0,
          flip: {
            horizontal: false,
            vertical: false
          }
        };
      };
      this.createDefaultState = function(boundary, image) {
        var _a = _this.getProps(), createStateAlgorithm = _a.createStateAlgorithm, settings = _a.settings;
        return _this.applyPostProcess({
          name: "createState",
          immediately: true,
          transitions: false
        }, (createStateAlgorithm || createState)({
          image,
          boundary
        }, settings));
      };
      this.isConsistent = function() {
        var state = _this.getData().state;
        var settings = _this.getProps().settings;
        return state ? isConsistentState(state, settings) : true;
      };
    }
    return AbstractCropperInstance2;
  }()
);

// node_modules/advanced-cropper/animation/index.js
var timingFunctions = {
  linear: function(t) {
    return t;
  },
  "ease-in": function(t) {
    return Math.pow(t, 1.675);
  },
  "ease-out": function(t) {
    return 1 - Math.pow(1 - t, 1.675);
  },
  "ease-in-out": function(t) {
    return 0.5 * (Math.sin((t - 0.5) * Math.PI) + 1);
  }
};
var Animation = (
  /** @class */
  function() {
    function Animation2() {
      this.active = false;
    }
    Animation2.prototype.start = function(animation) {
      var _a;
      this.onStart = animation.onStart;
      this.onProgress = animation.onProgress;
      this.onStop = animation.onStop;
      if (!this.active) {
        (_a = this.onStart) === null || _a === void 0 ? void 0 : _a.call(this);
      }
      if (this.id) {
        window.cancelAnimationFrame(this.id);
      }
      this.startTime = performance.now();
      this.timingFunction = animation.timingFunction;
      this.endTime = this.startTime + animation.duration;
      this.active = true;
      this.animate();
    };
    Animation2.prototype.animate = function() {
      var _this = this;
      if (this.startTime && this.endTime) {
        var timingFunction = timingFunctions[this.timingFunction];
        if (!timingFunction) {
          if (true) {
            console.warn("[Animation] The timing function '" + timingFunction + "' is not supported. Available timing function: 'linear', 'ease-in', 'ease-in-out', 'ease-out'. Reset to 'ease-out'.");
          }
          timingFunction = timingFunctions["ease-out"];
        }
        var percent = 1 - (this.endTime - performance.now()) / (this.endTime - this.startTime);
        var progress = Math.min(1, timingFunction(percent));
        if (this.onProgress) {
          this.onProgress(progress);
        }
        if (percent < 1) {
          this.id = window.requestAnimationFrame(function() {
            return _this.animate();
          });
        } else {
          this.stop();
        }
      } else {
        this.stop();
      }
    };
    Animation2.prototype.stop = function() {
      this.active = false;
      if (this.id) {
        window.cancelAnimationFrame(this.id);
      }
      if (this.onStop) {
        this.onStop();
      }
    };
    return Animation2;
  }()
);

// node_modules/advanced-cropper/canvas/index.js
function prepareSource(canvas, image, _a) {
  var rotate = _a.rotate, flip = _a.flip;
  var originalSize = {
    width: "naturalWidth" in image ? image.naturalWidth : image.width,
    height: "naturalHeight" in image ? image.naturalHeight : image.height
  };
  var transformedSize = rotateSize(originalSize, rotate);
  var ctx = canvas.getContext("2d");
  canvas.height = transformedSize.height;
  canvas.width = transformedSize.width;
  if (ctx) {
    ctx.save();
    var canvasCenter = rotatePoint(getCenter(__assign({ left: 0, top: 0 }, originalSize)), rotate);
    ctx.translate(-(canvasCenter.left - transformedSize.width / 2), -(canvasCenter.top - transformedSize.height / 2));
    ctx.rotate(rotate * Math.PI / 180);
    ctx.translate(flip.horizontal ? originalSize.width : 0, flip.vertical ? originalSize.height : 0);
    ctx.scale(flip.horizontal ? -1 : 1, flip.vertical ? -1 : 1);
    ctx.drawImage(image, 0, 0, originalSize.width, originalSize.height);
    ctx.restore();
  }
  return canvas;
}
function updateCanvas(canvas, source, coordinates, resultSize, options) {
  canvas.width = resultSize ? resultSize.width : coordinates.width;
  canvas.height = resultSize ? resultSize.height : coordinates.height;
  var ctx = canvas.getContext("2d");
  if (ctx) {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    if (options) {
      if (options.imageSmoothingEnabled) {
        ctx.imageSmoothingEnabled = options.imageSmoothingEnabled;
      }
      if (options.imageSmoothingQuality) {
        ctx.imageSmoothingQuality = options.imageSmoothingQuality;
      }
      if (options.fillColor) {
        ctx.fillStyle = options.fillColor;
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        ctx.save();
      }
    }
    var offsetX = coordinates.left < 0 ? -coordinates.left : 0;
    var offsetY = coordinates.top < 0 ? -coordinates.top : 0;
    ctx.drawImage(source, coordinates.left + offsetX, coordinates.top + offsetY, coordinates.width, coordinates.height, offsetX * (canvas.width / coordinates.width), offsetY * (canvas.height / coordinates.height), canvas.width, canvas.height);
  }
  return canvas;
}
function drawCroppedArea(state, image, resultCanvas, spareCanvas, options) {
  if (isInitializedState(state)) {
    var transforms = state.transforms, coordinates = state.coordinates;
    var imageTransformed = transforms.rotate !== 0 || transforms.flip.horizontal || transforms.flip.vertical;
    var source = imageTransformed ? prepareSource(spareCanvas, image, transforms) : image;
    var params = __assign({ minWidth: 0, minHeight: 0, maxWidth: Infinity, maxHeight: Infinity, maxArea: Infinity, imageSmoothingEnabled: true, imageSmoothingQuality: "high", fillColor: "transparent" }, options);
    var firstNumeric = function(array) {
      return array.find(function(el) {
        return isNumeric(el);
      });
    };
    var size = approximateSize({
      sizeRestrictions: {
        minWidth: firstNumeric([params.width, params.minWidth]) || 0,
        minHeight: firstNumeric([params.height, params.minHeight]) || 0,
        maxWidth: firstNumeric([params.width, params.maxWidth]) || Infinity,
        maxHeight: firstNumeric([params.height, params.maxHeight]) || Infinity
      },
      width: coordinates.width,
      height: coordinates.height,
      aspectRatio: {
        minimum: coordinates.width / coordinates.height,
        maximum: coordinates.width / coordinates.height
      }
    });
    if (params.maxArea && size.width * size.height > params.maxArea) {
      var scale = Math.sqrt(params.maxArea / (size.width * size.height));
      size = {
        width: Math.round(scale * size.width),
        height: Math.round(scale * size.height)
      };
    }
    return updateCanvas(resultCanvas, source, coordinates, size, params);
  } else {
    return null;
  }
}

// node_modules/advanced-cropper/boundary/index.js
function stretchCropperBoundary(boundary, stretcher, size) {
  stretcher.style.width = "0px";
  stretcher.style.height = "0px";
  stretcher.style.width = Math.max(boundary.clientWidth, size.width) + "px";
  var ratio2 = size.width / size.height;
  stretcher.style.height = Math.max(boundary.clientHeight, stretcher.clientWidth / ratio2) + "px";
  stretcher.style.width = stretcher.clientHeight * ratio2 + "px";
}
function stretchPreviewBoundary(boundary, stretcher, size) {
  stretcher.style.width = "0px";
  stretcher.style.height = "0px";
  stretcher.style.width = Math.max(boundary.getBoundingClientRect().width, size.width) + "px";
  var ratio2 = size.width / size.height;
  stretcher.style.height = Math.max(boundary.getBoundingClientRect().height, stretcher.getBoundingClientRect().width / ratio2) + "px";
  stretcher.style.width = stretcher.getBoundingClientRect().height * ratio2 + "px";
  if (stretcher.clientWidth / stretcher.clientHeight > boundary.clientWidth / boundary.clientHeight) {
    if (stretcher.clientWidth > boundary.clientWidth) {
      stretcher.style.width = boundary.clientWidth + "px";
      stretcher.style.height = boundary.clientWidth / ratio2 + "px";
    }
  } else {
    if (stretcher.clientHeight > boundary.clientHeight) {
      stretcher.style.height = boundary.clientHeight + "px";
      stretcher.style.width = boundary.clientHeight * ratio2 + "px";
    }
  }
}

// node_modules/advanced-cropper/image/index.js
var XHR_DONE = 4;
function base64ToArrayBuffer(base64) {
  base64 = base64.replace(/^data:([^;]+);base64,/gim, "");
  var binary = atob(base64);
  var len = binary.length;
  var buffer = new ArrayBuffer(len);
  var view = new Uint8Array(buffer);
  for (var i = 0; i < len; i++) {
    view[i] = binary.charCodeAt(i);
  }
  return buffer;
}
function objectURLToBlob(url, callback) {
  var http = new XMLHttpRequest();
  http.open("GET", url, true);
  http.responseType = "blob";
  http.onload = function() {
    if (this.status == 200 || this.status === 0) {
      callback(this.response);
    }
  };
  http.send();
}
function getTransforms(orientation) {
  var result = {
    flip: {
      horizontal: false,
      vertical: false
    },
    rotate: 0
  };
  if (orientation) {
    switch (orientation) {
      case 2:
        result.flip.horizontal = true;
        break;
      case 3:
        result.rotate = -180;
        break;
      case 4:
        result.flip.vertical = true;
        break;
      case 5:
        result.rotate = 90;
        result.flip.vertical = true;
        break;
      case 6:
        result.rotate = 90;
        break;
      case 7:
        result.rotate = 90;
        result.flip.horizontal = true;
        break;
      case 8:
        result.rotate = -90;
        break;
    }
  }
  return result;
}
function getImageData(img) {
  return new Promise(function(resolve, reject) {
    try {
      if (img) {
        if (/^data:/i.test(img)) {
          resolve(base64ToArrayBuffer(img));
        } else if (/^blob:/i.test(img)) {
          var fileReader_1 = new FileReader();
          fileReader_1.onload = function(e) {
            var _a;
            resolve((_a = e.target) === null || _a === void 0 ? void 0 : _a.result);
          };
          objectURLToBlob(img, function(blob) {
            fileReader_1.readAsArrayBuffer(blob);
          });
        } else {
          var http_1 = new XMLHttpRequest();
          http_1.onreadystatechange = function() {
            if (http_1.readyState !== XHR_DONE)
              return;
            if (http_1.status === 200 || http_1.status === 0) {
              resolve(http_1.response);
            } else {
              reject("Warning: could not load an image to parse its orientation");
            }
          };
          http_1.onprogress = function() {
            if (http_1.getResponseHeader("content-type") !== "image/jpeg") {
              http_1.abort();
            }
          };
          http_1.withCredentials = false;
          http_1.open("GET", img, true);
          http_1.responseType = "arraybuffer";
          http_1.send(null);
        }
      } else {
        reject("Error: the image is empty");
      }
    } catch (e) {
      reject(e);
    }
  });
}
function getStyleTransforms(transforms) {
  var _a = transforms.rotate, rotate = _a === void 0 ? 0 : _a, _b = transforms.flip, flip = _b === void 0 ? { horizontal: false, vertical: false } : _b, _c = transforms.scale, scale = _c === void 0 ? 1 : _c;
  return " rotate(" + rotate + "deg) scaleX(" + scale * (flip.horizontal ? -1 : 1) + ") scaleY(" + scale * (flip.vertical ? -1 : 1) + ")";
}
function getStringFromCharCode(dataView, start, length) {
  var str = "";
  var i;
  for (i = start, length += start; i < length; i++) {
    str += String.fromCharCode(dataView.getUint8(i));
  }
  return str;
}
function resetAndGetOrientation(arrayBuffer) {
  try {
    var dataView = new DataView(arrayBuffer);
    var orientation_1;
    var exifIDCode = void 0;
    var tiffOffset = void 0;
    var littleEndian = void 0;
    var app1Start = void 0;
    var ifdStart = void 0;
    if (dataView.getUint8(0) === 255 && dataView.getUint8(1) === 216) {
      var length_1 = dataView.byteLength;
      var offset = 2;
      while (offset + 1 < length_1) {
        if (dataView.getUint8(offset) === 255 && dataView.getUint8(offset + 1) === 225) {
          app1Start = offset;
          break;
        }
        offset++;
      }
    }
    if (app1Start) {
      exifIDCode = app1Start + 4;
      tiffOffset = app1Start + 10;
      if (getStringFromCharCode(dataView, exifIDCode, 4) === "Exif") {
        var endianness = dataView.getUint16(tiffOffset);
        littleEndian = endianness === 18761;
        if (littleEndian || endianness === 19789) {
          if (dataView.getUint16(tiffOffset + 2, littleEndian) === 42) {
            var firstIFDOffset = dataView.getUint32(tiffOffset + 4, littleEndian);
            if (firstIFDOffset >= 8) {
              ifdStart = tiffOffset + firstIFDOffset;
            }
          }
        }
      }
    }
    if (ifdStart) {
      var length_2 = dataView.getUint16(ifdStart, littleEndian);
      for (var i = 0; i < length_2; i++) {
        var offset = ifdStart + i * 12 + 2;
        if (dataView.getUint16(offset, littleEndian) === 274) {
          offset += 8;
          orientation_1 = dataView.getUint16(offset, littleEndian);
          dataView.setUint16(offset, 1, littleEndian);
          break;
        }
      }
    }
    return orientation_1;
  } catch (error) {
    return null;
  }
}
function arrayBufferToDataURL(arrayBuffer) {
  var chunks = [];
  var chunkSize = 8192;
  var uint8 = new Uint8Array(arrayBuffer);
  while (uint8.length > 0) {
    var value = uint8.subarray(0, chunkSize);
    chunks.push(String.fromCharCode.apply(null, Array.from ? Array.from(value) : value.slice()));
    uint8 = uint8.subarray(chunkSize);
  }
  return "data:image/jpeg;base64," + btoa(chunks.join(""));
}
function getImage(_a) {
  var src = _a.src, _b = _a.arrayBuffer, arrayBuffer = _b === void 0 ? null : _b, _c = _a.orientation, orientation = _c === void 0 ? null : _c;
  var options = {
    src,
    arrayBuffer,
    revoke: false,
    transforms: {
      flip: {
        horizontal: false,
        vertical: false
      },
      rotate: 0
    }
  };
  if (arrayBuffer && orientation && orientation > 1) {
    if (isBlob(src) || !isLocal(src)) {
      options.src = URL.createObjectURL(new Blob([arrayBuffer]));
      options.revoke = true;
    } else {
      options.src = arrayBufferToDataURL(arrayBuffer);
    }
  } else {
    options.src = src;
  }
  if (orientation) {
    options.transforms = getTransforms(orientation);
  }
  return options;
}
function parseImage(src, settings) {
  if (settings === void 0) {
    settings = {};
  }
  var checkOrientation = settings.checkOrientation, parse = settings.parse;
  return new Promise(function(resolve) {
    if (checkOrientation || parse) {
      getImageData(src).then(function(data) {
        var orientation = resetAndGetOrientation(data);
        resolve(getImage(data ? { src, arrayBuffer: data, orientation } : { src, arrayBuffer: null, orientation: null }));
      }).catch(function(error) {
        console.warn(error);
        resolve(getImage({ src }));
      });
    } else {
      resolve(getImage({ src }));
    }
  });
}
function createImage(src, settings) {
  if (settings === void 0) {
    settings = {};
  }
  return new Promise(function(resolve, reject) {
    var image = document.createElement("img");
    if (settings.crossOrigin) {
      image.crossOrigin = settings.crossOrigin !== true ? settings.crossOrigin : "anonymous";
    }
    image.src = src;
    image.style.visibility = "hidden";
    image.style.position = "fixed";
    document.body.appendChild(image);
    if (image.complete) {
      resolve(image);
      document.body.removeChild(image);
    } else {
      image.addEventListener("load", function() {
        resolve(image);
        document.body.removeChild(image);
      });
      image.addEventListener("error", function() {
        reject(null);
        document.body.removeChild(image);
      });
    }
  });
}
function loadImage(src, settings) {
  if (settings === void 0) {
    settings = {};
  }
  return parseImage(src, __assign(__assign({}, settings), { crossOrigin: isCrossOriginURL(src) && settings.crossOrigin })).then(function(options) {
    return new Promise(function(resolve, reject) {
      createImage(options.src, settings).then(function(image) {
        resolve(__assign(__assign({}, options), { width: image.naturalWidth, height: image.naturalHeight }));
      }).catch(function() {
        reject(null);
      });
    });
  });
}
function getImageStyle(image, state, area, coefficient, transitions) {
  if (transitions === void 0) {
    transitions = null;
  }
  var optimalImageSize = image.width > image.height ? {
    width: Math.min(512, image.width),
    height: Math.min(512, image.width) / (image.width / image.height)
  } : {
    height: Math.min(512, image.height),
    width: Math.min(512, image.height) * (image.width / image.height)
  };
  var actualImageSize = getTransformedImageSize(state);
  var imageTransforms = {
    rotate: state.transforms.rotate,
    flip: {
      horizontal: state.transforms.flip.horizontal,
      vertical: state.transforms.flip.vertical
    },
    translateX: area.left / coefficient,
    translateY: area.top / coefficient,
    scale: 1 / coefficient
  };
  var compensations = {
    rotate: {
      left: (optimalImageSize.width - actualImageSize.width) / (2 * coefficient),
      top: (optimalImageSize.height - actualImageSize.height) / (2 * coefficient)
    },
    scale: {
      left: (1 - 1 / coefficient) * optimalImageSize.width / 2,
      top: (1 - 1 / coefficient) * optimalImageSize.height / 2
    }
  };
  var transforms = __assign(__assign({}, imageTransforms), { scale: imageTransforms.scale * (image.width / optimalImageSize.width) });
  var result = {
    width: optimalImageSize.width + "px",
    height: optimalImageSize.height + "px",
    left: "0px",
    top: "0px",
    transition: "none",
    transform: "translate3d(" + (-compensations.rotate.left - compensations.scale.left - imageTransforms.translateX) + "px, " + (-compensations.rotate.top - compensations.scale.top - imageTransforms.translateY) + "px, 0px)" + getStyleTransforms(transforms),
    willChange: "none"
  };
  if (transitions && transitions.active) {
    result.willChange = "transform";
    result.transition = transitions.duration + "ms " + transitions.timingFunction;
  }
  return result;
}
function getBackgroundStyle(image, state, transitions) {
  if (transitions === void 0) {
    transitions = null;
  }
  if (image && state && state.visibleArea) {
    return getImageStyle(image, state, state.visibleArea, getCoefficient(state), transitions);
  } else {
    return {};
  }
}
function getPreviewStyle(image, state, size, transitions) {
  if (transitions === void 0) {
    transitions = null;
  }
  if (image && state && state.visibleArea && state.coordinates) {
    return getImageStyle(image, state, state.coordinates, ratio(state.coordinates) > ratio(size) ? state.coordinates.width / size.width : state.coordinates.height / size.height, transitions);
  } else {
    return {};
  }
}

// node_modules/react-advanced-cropper/dist/index.esm-bundler.js
var import_classnames = __toESM(require_classnames());

// node_modules/advanced-cropper/extensions/stencil-size/index.js
function fixedStencilConstraints(rawSettings, stencilOptions) {
  var defaultConstraints = defaultStencilConstraints({}, stencilOptions);
  return {
    stencilSize: function(state, settings) {
      var previousSize = isFunction(rawSettings.stencilSize) ? rawSettings.stencilSize(state, settings) : rawSettings.stencilSize;
      return approximateSize(__assign(__assign({}, previousSize), { aspectRatio: aspectRatioIntersection(defaultConstraints.aspectRatio, createAspectRatio(ratio(previousSize))) }));
    }
  };
}
function getStencilSize(state, settings) {
  var boundary = state.boundary;
  var size = isFunction(settings.stencilSize) ? settings.stencilSize(state, settings) : settings.stencilSize;
  if (size.width > boundary.width || size.height > boundary.height) {
    size = approximateSize({
      sizeRestrictions: {
        maxWidth: boundary.width,
        maxHeight: boundary.height,
        minWidth: 0,
        minHeight: 0
      },
      width: size.width,
      height: size.height,
      aspectRatio: {
        minimum: ratio(size),
        maximum: ratio(size)
      }
    });
  }
  return size;
}
function sizeRestrictions(state, settings) {
  var stencilSize = getStencilSize(state, __assign(__assign({}, settings), { stencilSize: settings.stencilSize }));
  var areaRestrictions = getAreaSizeRestrictions(state, settings);
  return {
    maxWidth: areaRestrictions.maxWidth * stencilSize.width / state.boundary.width,
    maxHeight: areaRestrictions.maxHeight * stencilSize.height / state.boundary.height,
    minWidth: 0,
    minHeight: 0
  };
}
function defaultSize2(state, settings) {
  var imageSize = state.imageSize, visibleArea = state.visibleArea, boundary = state.boundary;
  var sizeRestrictions2 = getSizeRestrictions(state, settings);
  var aspectRatio2 = getAspectRatio(state, settings);
  var stencilSize = isFunction(settings.stencilSize) ? settings.stencilSize(state, settings) : settings.stencilSize;
  var area = visibleArea || imageSize;
  var height, width;
  if (ratio(area) > ratio(boundary)) {
    height = stencilSize.height * area.height / boundary.height;
    width = height * ratio(stencilSize);
  } else {
    width = stencilSize.width * area.width / boundary.width;
    height = width / ratio(stencilSize);
  }
  return approximateSize({
    width,
    height,
    aspectRatio: aspectRatio2,
    sizeRestrictions: sizeRestrictions2
  });
}
function aspectRatio(state, settings) {
  var value = ratio(getStencilSize(state, settings));
  return {
    minimum: value,
    maximum: value
  };
}
function fixedStencilAlgorithm(state, settings) {
  if (isInitializedState(state)) {
    var result = copyState(state);
    var stencil = getStencilSize(state, settings);
    result.visibleArea = applyScale(result.visibleArea, result.coordinates.width * result.boundary.width / (result.visibleArea.width * stencil.width));
    var scale = fitToSizeRestrictions(result.visibleArea, getAreaSizeRestrictions(result, settings));
    if (scale !== 1) {
      result.visibleArea = applyScale(result.visibleArea, scale);
      result.coordinates = applyScale(result.coordinates, scale);
    }
    result.visibleArea = applyMove(result.visibleArea, diff(getCenter(result.coordinates), getCenter(result.visibleArea)));
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
    result.coordinates = moveToPositionRestrictions(result.coordinates, mergePositionRestrictions(coordinatesToPositionRestrictions(result.visibleArea), getAreaPositionRestrictions(result, settings)));
    return result;
  }
  return state;
}
function fixedStencil(state, settings, action) {
  if (action && action.immediately) {
    return fixedStencilAlgorithm(state, settings);
  }
  return state;
}

// node_modules/react-advanced-cropper/dist/index.esm-bundler.js
function hybridStencilAutoZoomAlgorithm(state, settings) {
  if (isInitializedState(state)) {
    var result = copyState(state);
    var stencil = {
      width: 0,
      height: 0
    };
    if (ratio(result.boundary) > ratio(result.coordinates)) {
      stencil.height = result.boundary.height * 0.8;
      stencil.width = stencil.height * ratio(result.coordinates);
    } else {
      stencil.width = result.boundary.width * 0.8;
      stencil.height = stencil.width * ratio(result.coordinates);
    }
    result.visibleArea = applyScale(result.visibleArea, result.coordinates.width * result.boundary.width / (result.visibleArea.width * stencil.width));
    var scale = fitToSizeRestrictions(result.visibleArea, getAreaSizeRestrictions(result, settings));
    result.visibleArea = applyScale(result.visibleArea, scale);
    if (scale !== 1) {
      stencil.height /= scale;
      stencil.width /= scale;
    }
    result.visibleArea = applyMove(result.visibleArea, diff(getCenter(result.coordinates), getCenter(result.visibleArea)));
    result.visibleArea = moveToPositionRestrictions(result.visibleArea, getAreaPositionRestrictions(result, settings));
    result.coordinates = moveToPositionRestrictions(result.coordinates, mergePositionRestrictions(coordinatesToPositionRestrictions(result.visibleArea), getPositionRestrictions(result, settings)));
    return result;
  }
  return state;
}
function hybridStencilAutoZoom(state, settings, action) {
  if (action.immediately) {
    return hybridStencilAutoZoomAlgorithm(state, settings);
  }
  return state;
}
function deprecationWarning(text) {
  if (true) {
    console.warn("Deprecation warning: ".concat(text));
  }
}
function useDeprecationWarning() {
  var fired = (0, import_react.useRef)([]);
  return function(message) {
    if (fired.current.indexOf(message) === -1) {
      deprecationWarning(message);
      fired.current.push(message);
    }
  };
}
function createCropper(render) {
  return (0, import_react.forwardRef)(render);
}
function useFirstMountState() {
  var isFirst = (0, import_react.useRef)(true);
  if (isFirst.current) {
    isFirst.current = false;
    return true;
  }
  return isFirst.current;
}
var useUpdateEffect = function(effect, deps) {
  var isFirstMount = useFirstMountState();
  (0, import_react.useEffect)(function() {
    if (!isFirstMount) {
      return effect();
    }
  }, deps);
};
function useStateWithCallback(initialState) {
  var _a = __read((0, import_react.useState)(initialState), 2), state = _a[0], setState = _a[1];
  var _b = __read((0, import_react.useState)(null), 2), callback = _b[0], setCallback = _b[1];
  var previousState = (0, import_react.useRef)(initialState);
  useUpdateEffect(function() {
    if (callback) {
      callback(state, previousState.current);
    }
  }, [callback]);
  return [
    state,
    function(value, callback2) {
      previousState.current = state;
      setState(value);
      setCallback(function() {
        return callback2;
      });
    }
  ];
}
var CropperInstance = (
  /** @class */
  function(_super) {
    __extends(CropperInstance2, _super);
    function CropperInstance2(props, onChange) {
      var _this = _super.call(this) || this;
      _this.props = props;
      _this.notify = onChange;
      _this.data = {
        state: null,
        transitions: false,
        interactions: getEmptyInteractions()
      };
      return _this;
    }
    CropperInstance2.prototype.getProps = function() {
      return this.props();
    };
    CropperInstance2.prototype.setData = function(data) {
      this.data = data;
      this.notify();
    };
    CropperInstance2.prototype.getData = function() {
      return this.data;
    };
    return CropperInstance2;
  }(AbstractCropperInstance)
);
function useForceRerender() {
  var _a = __read((0, import_react.useState)({}), 2);
  _a[0];
  var setTick = _a[1];
  return function() {
    setTick({});
  };
}
function usePersistentFunction(props) {
  var propsRef = (0, import_react.useRef)(props);
  propsRef.current = props;
  return function() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    return propsRef.current.apply(propsRef, __spreadArray([], __read(args), false));
  };
}
function useCropperInstance(props) {
  var rerender = useForceRerender();
  var getProps = usePersistentFunction(function() {
    var _a = props(), settings = _a.settings, parameters = __rest(_a, ["settings"]);
    var extendedSettings = __assign({ imageRestriction: ImageRestriction.fitArea, transformImage: {
      adjustStencil: true
    } }, settings);
    var extendedParameters = __assign({ transitions: true }, parameters);
    return __assign({ settings: __assign(__assign({}, extendedSettings), createDefaultSettings(extendedSettings)) }, extendedParameters);
  });
  var cropper = (0, import_react.useRef)(new CropperInstance(getProps, rerender));
  return cropper.current;
}
function useCropperImage(options) {
  var src = options.src, onLoadingStart = options.onLoadingStart, onLoadingEnd = options.onLoadingEnd, onError = options.onError, onLoad = options.onLoad, crossOrigin = options.crossOrigin, checkOrientation = options.checkOrientation, canvas = options.canvas, unloadTime = options.unloadTime;
  var _a = __read((0, import_react.useState)(null), 2), image = _a[0], setImage = _a[1];
  var _b = __read((0, import_react.useState)(false), 2), loading = _b[0], setLoading = _b[1];
  var _c = __read(useStateWithCallback(false), 2), loaded = _c[0], setLoaded = _c[1];
  var currentSrc = (0, import_react.useRef)(null);
  (0, import_react.useEffect)(function() {
    if (currentSrc.current !== src) {
      currentSrc.current = src || null;
      setLoaded(false);
      if (src) {
        setLoading(true);
        onLoadingStart === null || onLoadingStart === void 0 ? void 0 : onLoadingStart();
        var promises = [
          loadImage(src, {
            crossOrigin: isUndefined(crossOrigin) ? canvas : crossOrigin,
            checkOrientation
          })
        ];
        if (loaded && unloadTime) {
          promises.push(promiseTimeout(unloadTime));
        }
        Promise.all(promises).then(function(responses) {
          var _a2 = __read(responses, 1), image2 = _a2[0];
          if (currentSrc.current === src) {
            setImage(image2);
          }
        }).catch(function() {
          if (currentSrc.current === src) {
            onError === null || onError === void 0 ? void 0 : onError();
          }
        }).finally(function() {
          if (currentSrc.current === src) {
            onLoadingEnd === null || onLoadingEnd === void 0 ? void 0 : onLoadingEnd();
            setLoading(false);
          }
        });
      } else {
        if (unloadTime) {
          promiseTimeout(unloadTime).then(function() {
            if (currentSrc.current === src) {
              setImage(null);
            }
          });
        } else {
          setImage(null);
        }
      }
    }
  }, [src, image]);
  (0, import_react.useEffect)(function() {
    if (image) {
      setLoaded(true, function() {
        onLoad === null || onLoad === void 0 ? void 0 : onLoad(image);
      });
    }
  }, [image]);
  return {
    isLoading: function() {
      return loading;
    },
    isLoaded: function() {
      return loaded;
    },
    getImage: function() {
      return image;
    },
    setImage
  };
}
var useWindowResize = function(callback) {
  var callbackRef = (0, import_react.useRef)(callback);
  var internalCallback = function() {
    if (callbackRef.current) {
      callbackRef.current();
    }
  };
  (0, import_react.useEffect)(function() {
    callbackRef.current = callback;
  }, [callback]);
  (0, import_react.useEffect)(function() {
    window.addEventListener("resize", internalCallback);
    window.addEventListener("orientationchange", internalCallback);
    return function() {
      window.removeEventListener("resize", internalCallback);
      window.removeEventListener("orientationchange", internalCallback);
    };
  }, []);
};
function useCropperAutoReconcile(cropper, enabled) {
  if (enabled === void 0) {
    enabled = true;
  }
  var _a = __read((0, import_react.useState)(enabled), 2), active = _a[0], setActive = _a[1];
  (0, import_react.useLayoutEffect)(function() {
    if (active && !cropper.hasInteractions()) {
      cropper.reconcileState();
    }
  });
  return {
    pause: function() {
      setActive(false);
    },
    resume: function() {
      setActive(true);
    }
  };
}
function useAbstractCropper(props) {
  var _this = this;
  var _a = props(), src = _a.src, onReady = _a.onReady, onError = _a.onError, onUpdate = _a.onUpdate, _b = _a.canvas, canvas = _b === void 0 ? true : _b, _c = _a.unloadTime, unloadTime = _c === void 0 ? 500 : _c, _d = _a.crossOrigin, crossOrigin = _d === void 0 ? true : _d, _e = _a.checkOrientation, checkOrientation = _e === void 0 ? true : _e, _f = _a.autoReconcileState, autoReconcileState = _f === void 0 ? true : _f;
  var imageRef = (0, import_react.useRef)(null);
  var boundaryRef = (0, import_react.useRef)(null);
  var canvasRef = (0, import_react.useRef)(null);
  var cropperRef = (0, import_react.useRef)(null);
  var _g = __read(useStateWithCallback(null), 2), currentImage = _g[0], setCurrentImage = _g[1];
  var cropper = useCropperInstance(function() {
    return __assign(__assign({}, props()), { getInstance: function() {
      return cropperRef.current;
    } });
  });
  var cropperImage = useCropperImage({
    src,
    crossOrigin,
    checkOrientation,
    unloadTime,
    canvas,
    onError: function() {
      if (cropperRef.current) {
        onError === null || onError === void 0 ? void 0 : onError(cropperRef.current);
      }
    }
  });
  var autoReconcile = useCropperAutoReconcile(cropper, autoReconcileState);
  var resetCropper = function() {
    return __awaiter(_this, void 0, void 0, function() {
      var image_1, boundary_1;
      var _a2;
      return __generator(this, function(_b2) {
        switch (_b2.label) {
          case 0:
            if (!boundaryRef.current) return [3, 2];
            autoReconcile.pause();
            image_1 = cropperImage.getImage();
            return [4, (_a2 = boundaryRef.current) === null || _a2 === void 0 ? void 0 : _a2.stretchTo(image_1)];
          case 1:
            boundary_1 = _b2.sent();
            setCurrentImage(image_1, function() {
              if (boundary_1 && image_1) {
                cropper.reset(boundary_1, image_1);
              } else {
                cropper.clear();
              }
            });
            autoReconcile.resume();
            _b2.label = 2;
          case 2:
            return [
              2
              /*return*/
            ];
        }
      });
    });
  };
  var refreshCropper = function() {
    return __awaiter(_this, void 0, void 0, function() {
      var image, boundary, state;
      var _a2;
      return __generator(this, function(_b2) {
        switch (_b2.label) {
          case 0:
            if (!boundaryRef.current) return [3, 2];
            autoReconcile.pause();
            image = cropperImage.getImage();
            return [4, (_a2 = boundaryRef.current) === null || _a2 === void 0 ? void 0 : _a2.stretchTo(image)];
          case 1:
            boundary = _b2.sent();
            if (boundary && image) {
              state = cropper.getState();
              if (state) {
                if (boundary.width !== state.boundary.width || boundary.height !== state.boundary.height) {
                  cropper.setBoundary(boundary);
                  cropper.reconcileState();
                }
              } else {
                cropper.reset(boundary, image);
              }
            } else {
              cropper.clear();
            }
            autoReconcile.resume();
            _b2.label = 2;
          case 2:
            return [
              2
              /*return*/
            ];
        }
      });
    });
  };
  var cropperInterface = {
    reset: function() {
      return resetCropper();
    },
    refresh: function() {
      return refreshCropper();
    },
    setImage: function(image) {
      return setCurrentImage(image);
    },
    reconcileState: cropper.reconcileState,
    moveCoordinates: cropper.moveCoordinates,
    moveCoordinatesEnd: cropper.moveCoordinatesEnd,
    resizeCoordinates: cropper.resizeCoordinates,
    clear: cropper.clear,
    resizeCoordinatesEnd: cropper.resizeCoordinatesEnd,
    moveImage: cropper.moveImage,
    flipImage: cropper.flipImage,
    zoomImage: cropper.zoomImage,
    rotateImage: cropper.rotateImage,
    transformImage: cropper.transformImage,
    transformImageEnd: cropper.transformImageEnd,
    setCoordinates: cropper.setCoordinates,
    setVisibleArea: cropper.setVisibleArea,
    startTransitions: cropper.startTransitions,
    setState: cropper.setState,
    hasInteractions: cropper.hasInteractions,
    getStencilCoordinates: cropper.getStencilCoordinates,
    getCoordinates: cropper.getCoordinates,
    getVisibleArea: cropper.getVisibleArea,
    getTransforms: cropper.getTransforms,
    getTransitions: cropper.getTransitions,
    getInteractions: cropper.getInteractions,
    getSettings: cropper.getSettings,
    getState: cropper.getState,
    getDefaultState: function() {
      var state = cropper.getState();
      var image = cropperImage.getImage();
      if (state && image) {
        return cropper.createDefaultState(state.boundary, image);
      } else {
        return null;
      }
    },
    getCanvas: function(options) {
      var state = cropper.getState();
      if (imageRef.current && canvasRef.current && state) {
        return canvasRef.current.draw(state, imageRef.current, options);
      } else {
        return null;
      }
    },
    getImage: function() {
      return currentImage ? __assign({}, currentImage) : null;
    },
    isLoading: cropperImage.isLoading,
    isLoaded: cropperImage.isLoaded
  };
  useWindowResize(function() {
    refreshCropper();
  });
  useUpdateEffect(function() {
    resetCropper();
  }, [cropperImage.getImage()]);
  useUpdateEffect(function() {
    if (cropperRef.current && currentImage) {
      onReady === null || onReady === void 0 ? void 0 : onReady(cropperRef.current);
    }
  }, [currentImage]);
  useUpdateEffect(function() {
    if (cropperRef.current) {
      onUpdate === null || onUpdate === void 0 ? void 0 : onUpdate(cropperRef.current);
    }
  }, [cropperImage.isLoaded(), cropperImage.isLoading()]);
  (0, import_react.useImperativeHandle)(cropperRef, function() {
    return cropperInterface;
  });
  return {
    cropper: cropperInterface,
    refs: {
      image: imageRef,
      boundary: boundaryRef,
      canvas: canvasRef
    },
    image: currentImage
  };
}
var StretchableBoundary = (0, import_react.forwardRef)(function(_a, ref) {
  var className = _a.className, style = _a.style, stretcherClassName = _a.stretcherClassName, contentClassName = _a.contentClassName, _b = _a.stretchAlgorithm, stretchAlgorithm = _b === void 0 ? stretchCropperBoundary : _b, _c = _a.sizeAlgorithm, sizeAlgorithm = _c === void 0 ? fillBoundary : _c, children = _a.children;
  var stretcherRef = (0, import_react.useRef)(null);
  var boundaryRef = (0, import_react.useRef)(null);
  (0, import_react.useImperativeHandle)(ref, function() {
    return {
      reset: function() {
        var stretcher = stretcherRef.current;
        if (stretcher) {
          stretcher.style.height = "";
          stretcher.style.width = "";
        }
      },
      stretchTo: function(size) {
        var stretcher = stretcherRef.current;
        var boundary = boundaryRef.current;
        if ((size === null || size === void 0 ? void 0 : size.width) && (size === null || size === void 0 ? void 0 : size.height) && stretcher && boundary) {
          stretchAlgorithm(boundary, stretcher, size);
          var result = sizeAlgorithm(boundary, size);
          return Promise.resolve(result.width && result.height ? result : null);
        } else {
          if (stretcher) {
            stretcher.style.height = "";
            stretcher.style.width = "";
          }
          return Promise.resolve(null);
        }
      }
    };
  });
  return import_react.default.createElement(
    "div",
    { ref: boundaryRef, style, className: (0, import_classnames.default)("advanced-cropper-boundary", className) },
    import_react.default.createElement("div", { ref: stretcherRef, className: (0, import_classnames.default)(["advanced-cropper-boundary__stretcher", stretcherClassName]) }),
    import_react.default.createElement("div", { className: (0, import_classnames.default)(["advanced-cropper-boundary__content", contentClassName]) }, children)
  );
});
StretchableBoundary.displayName = "StretchableBoundary";
var CropperFade = function(_a) {
  var visible = _a.visible, className = _a.className, style = _a.style, children = _a.children;
  return import_react.default.createElement("div", { style, className: (0, import_classnames.default)(className, "advanced-cropper-fade", Boolean(visible) && "advanced-cropper-fade--visible") }, children);
};
var CropperWrapper = function(_a) {
  var cropper = _a.cropper, children = _a.children, className = _a.className, style = _a.style;
  var state = cropper ? cropper.getState() : null;
  var loaded = cropper ? cropper.isLoaded() : false;
  return import_react.default.createElement(
    "div",
    { className: (0, import_classnames.default)(className, "advanced-cropper-wrapper"), style },
    import_react.default.createElement(CropperFade, { visible: state && loaded, className: "advanced-cropper-wrapper__fade" }, children)
  );
};
function preventDefault(e) {
  e.preventDefault();
}
var CropperBackgroundImage = (0, import_react.forwardRef)(function(_a, ref) {
  var className = _a.className, style = _a.style, cropper = _a.cropper, _b = _a.crossOrigin, crossOrigin = _b === void 0 ? true : _b;
  var state = cropper.getState();
  var transitions = cropper.getTransitions();
  var image = cropper.getImage();
  var transformStyles = image && state ? getBackgroundStyle(image, state, transitions) : {};
  var src = image ? image.src : void 0;
  return src ? import_react.default.createElement("img", { key: src, ref, className: (0, import_classnames.default)("advanced-cropper-background-image", className), src, crossOrigin: crossOrigin === true ? "anonymous" : crossOrigin || void 0, style: __assign(__assign({}, transformStyles), style), onMouseDown: preventDefault }) : null;
});
CropperBackgroundImage.displayName = "CropperBackgroundImage";
var CropperCanvas = (0, import_react.forwardRef)(function(_, ref) {
  var canvasRef = (0, import_react.useRef)(null);
  var spareCanvasRef = (0, import_react.useRef)(null);
  (0, import_react.useImperativeHandle)(ref, function() {
    return {
      draw: function(state, image, options) {
        if (options === void 0) {
          options = {};
        }
        if (image && canvasRef.current && spareCanvasRef.current) {
          return drawCroppedArea(state, image, canvasRef.current, spareCanvasRef.current, options);
        } else {
          return null;
        }
      }
    };
  });
  return import_react.default.createElement(
    import_react.default.Fragment,
    null,
    import_react.default.createElement("canvas", { className: "advanced-cropper-canvas", ref: canvasRef }),
    import_react.default.createElement("canvas", { className: "advanced-cropper-canvas", ref: spareCanvasRef })
  );
});
CropperCanvas.displayName = "CropperCanvas";
var DraggableElement = (
  /** @class */
  function(_super) {
    __extends(DraggableElement2, _super);
    function DraggableElement2(props) {
      var _this = _super.call(this, props) || this;
      _this.processMove = function(e, newTouches) {
        var _a;
        var container = _this.container.current;
        if (container && _this.touches.length) {
          var _b = container.getBoundingClientRect(), left = _b.left, top_1 = _b.top;
          if (_this.touches.length === 1 && newTouches.length === 1) {
            if (_this.props.onMove) {
              var movingToAnchor = {
                left: Math.abs(newTouches[0].clientX - _this.anchor.left - left) < Math.abs(_this.touches[0].clientX - _this.anchor.left - left),
                top: Math.abs(newTouches[0].clientY - _this.anchor.top - top_1) < Math.abs(_this.touches[0].clientY - _this.anchor.top - top_1)
              };
              var direction = {
                left: 0,
                top: 0
              };
              if (!_this.props.useAnchor || !movingToAnchor.left) {
                direction.left = newTouches[0].clientX - _this.touches[0].clientX;
              }
              if (!_this.props.useAnchor || !movingToAnchor.top) {
                direction.top = newTouches[0].clientY - _this.touches[0].clientY;
              }
              (_a = _this.props) === null || _a === void 0 ? void 0 : _a.onMove(direction, e);
              _this.touches = __spreadArray([], __read(newTouches), false);
            }
          }
        }
      };
      _this.processEnd = function() {
        var _a = _this.props, onMoveEnd = _a.onMoveEnd, onLeave = _a.onLeave;
        if (!_this.props.disabled && _this.touches.length) {
          onMoveEnd === null || onMoveEnd === void 0 ? void 0 : onMoveEnd();
        }
        if (_this.hovered) {
          onLeave === null || onLeave === void 0 ? void 0 : onLeave();
          _this.hovered = false;
        }
        _this.touches = [];
      };
      _this.initAnchor = function(touch) {
        var container = _this.container.current;
        if (container) {
          var _a = container.getBoundingClientRect(), left = _a.left, top_2 = _a.top;
          _this.anchor = {
            left: touch.clientX - left,
            top: touch.clientY - top_2
          };
        }
      };
      _this.onMouseOver = function() {
        var _a = _this.props, onEnter = _a.onEnter, disabled = _a.disabled;
        if (!_this.hovered && !disabled) {
          _this.hovered = true;
          onEnter === null || onEnter === void 0 ? void 0 : onEnter();
        }
      };
      _this.onMouseLeave = function() {
        var onLeave = _this.props.onLeave;
        if (_this.hovered && !_this.touches.length) {
          _this.hovered = false;
          onLeave === null || onLeave === void 0 ? void 0 : onLeave();
        }
      };
      _this.onTouchStart = function(e) {
        var _a = _this.props, onEnter = _a.onEnter, onMoveStart = _a.onMoveStart, disabled = _a.disabled;
        if (e.cancelable) {
          _this.touches = Array.from(e.touches);
          var shouldStartMove = !disabled && e.touches.length === 1;
          if (shouldStartMove) {
            _this.touches = Array.from(e.touches);
            onMoveStart === null || onMoveStart === void 0 ? void 0 : onMoveStart();
          }
          if (!_this.hovered && !disabled) {
            _this.hovered = true;
            onEnter === null || onEnter === void 0 ? void 0 : onEnter();
          }
          if (_this.started || shouldStartMove) {
            e.preventDefault();
            e.stopPropagation();
          }
        }
      };
      _this.onTouchEnd = function() {
        _this.started = false;
        _this.processEnd();
      };
      _this.onTouchMove = function(e) {
        if (_this.touches.length >= 1) {
          if (_this.started) {
            _this.processMove(e, Array.from(e.touches));
            e.preventDefault();
            e.stopPropagation();
          } else if (distance({ left: _this.touches[0].clientX, top: _this.touches[0].clientY }, { left: e.touches[0].clientX, top: e.touches[0].clientY }) > (_this.props.activationDistance || 0)) {
            _this.initAnchor({
              clientX: e.touches[0].clientX,
              clientY: e.touches[0].clientY
            });
            _this.started = true;
          }
        }
      };
      _this.onMouseDown = function(e) {
        var _a = _this.props, onMoveStart = _a.onMoveStart, disabled = _a.disabled;
        if (!disabled && e.button === 0) {
          var touch = {
            clientX: e.clientX,
            clientY: e.clientY
          };
          _this.touches = [touch];
          _this.initAnchor(touch);
          e.stopPropagation();
          onMoveStart === null || onMoveStart === void 0 ? void 0 : onMoveStart();
        }
      };
      _this.onMouseMove = function(e) {
        if (!_this.props.disabled && _this.touches.length) {
          _this.processMove(e, [
            {
              clientX: e.clientX,
              clientY: e.clientY
            }
          ]);
          if (e.preventDefault && e.cancelable) {
            e.preventDefault();
          }
          e.stopPropagation();
        }
      };
      _this.onMouseUp = function() {
        _this.processEnd();
      };
      _this.touches = [];
      _this.hovered = false;
      _this.started = false;
      _this.anchor = {
        left: 0,
        top: 0
      };
      _this.container = (0, import_react.createRef)();
      return _this;
    }
    DraggableElement2.prototype.componentWillUnmount = function() {
      window.removeEventListener("mouseup", this.onMouseUp);
      window.removeEventListener("mousemove", this.onMouseMove);
      window.removeEventListener("touchmove", this.onTouchMove);
      window.removeEventListener("touchend", this.onTouchEnd);
      var container = this.container.current;
      if (container) {
        container.removeEventListener("touchstart", this.onTouchStart);
        container.removeEventListener("mousedown", this.onMouseDown);
      }
    };
    DraggableElement2.prototype.componentDidMount = function() {
      window.addEventListener("mouseup", this.onMouseUp, { passive: false });
      window.addEventListener("mousemove", this.onMouseMove, { passive: false });
      window.addEventListener("touchmove", this.onTouchMove, { passive: false });
      window.addEventListener("touchend", this.onTouchEnd, { passive: false });
      var container = this.container.current;
      if (container) {
        container.addEventListener("touchstart", this.onTouchStart, {
          passive: false
        });
        container.addEventListener("mousedown", this.onMouseDown, {
          passive: false
        });
      }
    };
    DraggableElement2.prototype.componentDidUpdate = function(prevProps) {
      if (this.props.disabled && !prevProps.disabled) {
        this.touches = [];
      }
    };
    DraggableElement2.prototype.render = function() {
      var _a = this.props, children = _a.children, className = _a.className;
      return import_react.default.createElement("div", { className: (0, import_classnames.default)("advanced-cropper-draggable-element", className), ref: this.container, onMouseOver: this.onMouseOver, onMouseLeave: this.onMouseLeave }, children);
    };
    DraggableElement2.defaultProps = {
      disabled: false,
      activationDistance: 30,
      useAnchor: true,
      rerender: true
    };
    return DraggableElement2;
  }(import_react.Component)
);
var LineWrapper = function(_a) {
  var position = _a.position, className = _a.className, disabled = _a.disabled, onDrag = _a.onDrag, onDragEnd = _a.onDragEnd, onLeave = _a.onLeave, onEnter = _a.onEnter, children = _a.children;
  return import_react.default.createElement(
    DraggableElement,
    { className: (0, import_classnames.default)([
      "advanced-cropper-line-wrapper",
      position && "advanced-cropper-line-wrapper--".concat(position),
      disabled && "advanced-cropper-line-wrapper--disabled",
      className
    ]), disabled, onMove: onDrag, onMoveEnd: onDragEnd, onLeave, onEnter, activationDistance: 0 },
    import_react.default.createElement("div", { className: (0, import_classnames.default)([
      "advanced-cropper-line-wrapper__content",
      position && "advanced-cropper-line-wrapper__content--".concat(position)
    ]) }, children)
  );
};
var SimpleLine = function(_a) {
  var _b, _c;
  var position = _a.position, hoverClassName = _a.hoverClassName, wrapperClassName = _a.wrapperClassName, defaultClassName = _a.defaultClassName, disabled = _a.disabled, onMove = _a.onMove, onMoveEnd = _a.onMoveEnd;
  var _d = __read((0, import_react.useState)(false), 2), hover = _d[0], setHover = _d[1];
  var onEnter = function() {
    setHover(true);
  };
  var onLeave = function() {
    setHover(false);
  };
  return import_react.default.createElement(
    LineWrapper,
    { className: (0, import_classnames.default)("advanced-cropper-simple-line-wrapper", wrapperClassName, (_b = {}, _b["advanced-cropper-simple-line-wrapper--".concat(position)] = !!position, _b)), position, disabled, onDrag: onMove, onDragEnd: onMoveEnd, onLeave, onEnter },
    import_react.default.createElement("div", { className: (0, import_classnames.default)("advanced-cropper-simple-line", hover && "advanced-cropper-simple-line--hover", defaultClassName, hover && hoverClassName, (_c = {}, _c["advanced-cropper-simple-line--".concat(position)] = !!position, _c)) })
  );
};
var HandlerWrapper = function(_a) {
  var horizontalPosition = _a.horizontalPosition, verticalPosition = _a.verticalPosition, className = _a.className, disabled = _a.disabled, onDrag = _a.onDrag, onDragEnd = _a.onDragEnd, onLeave = _a.onLeave, onEnter = _a.onEnter, children = _a.children, style = _a.style;
  var position = horizontalPosition || verticalPosition ? getDirectionNames(horizontalPosition, verticalPosition).snakeCase : null;
  return import_react.default.createElement(
    "div",
    { style, className: (0, import_classnames.default)(className, "advanced-cropper-handler-wrapper", position && "advanced-cropper-handler-wrapper--".concat(position), disabled && "advanced-cropper-handler-wrapper--disabled") },
    import_react.default.createElement(DraggableElement, { className: "advanced-cropper-handler-wrapper__draggable", disabled, onMove: onDrag, onMoveEnd: onDragEnd, onLeave, onEnter, activationDistance: 0 }, children)
  );
};
var SimpleHandler = function(_a) {
  var _b;
  var verticalPosition = _a.verticalPosition, horizontalPosition = _a.horizontalPosition, hoverClassName = _a.hoverClassName, wrapperClassName = _a.wrapperClassName, defaultClassName = _a.defaultClassName, wrapperStyle = _a.wrapperStyle, disabled = _a.disabled, onMove = _a.onMove, onMoveEnd = _a.onMoveEnd;
  var _c = __read((0, import_react.useState)(false), 2), hover = _c[0], setHover = _c[1];
  var onEnter = function() {
    setHover(true);
  };
  var onLeave = function() {
    setHover(false);
  };
  return import_react.default.createElement(
    HandlerWrapper,
    { style: wrapperStyle, className: (0, import_classnames.default)("advanced-cropper-simple-handler-wrapper", wrapperClassName, verticalPosition && "advanced-cropper-simple-handler-wrapper--".concat(verticalPosition), horizontalPosition && "advanced-cropper-simple-handler-wrapper--".concat(horizontalPosition), horizontalPosition && verticalPosition && "advanced-cropper-simple-handler-wrapper--".concat(horizontalPosition, "-").concat(verticalPosition), hover && "advanced-cropper-simple-handler-wrapper--hover"), verticalPosition, horizontalPosition, disabled, onDrag: onMove, onDragEnd: onMoveEnd, onLeave, onEnter },
    import_react.default.createElement("div", { className: (0, import_classnames.default)("advanced-cropper-simple-handler", hover && "advanced-cropper-simple-handler--hover", defaultClassName, hover && hoverClassName, (_b = {}, _b["advanced-cropper-simple-handler--".concat(verticalPosition)] = !!verticalPosition, _b["advanced-cropper-simple-handler--".concat(horizontalPosition)] = !!horizontalPosition, _b["advanced-cropper-simple-handler--".concat(horizontalPosition, "-").concat(verticalPosition)] = horizontalPosition && verticalPosition, _b)) })
  );
};
var HORIZONTAL_DIRECTIONS2 = ["east", "west", null];
var VERTICAL_DIRECTIONS2 = ["south", "north", null];
var BoundingBox = function(_a) {
  var style = _a.style, className = _a.className, children = _a.children, onResize = _a.onResize, onResizeEnd = _a.onResizeEnd, _b = _a.handlerComponent, handlerComponent = _b === void 0 ? SimpleHandler : _b, _c = _a.handlers, handlers = _c === void 0 ? {
    eastNorth: true,
    north: true,
    westNorth: true,
    west: true,
    westSouth: true,
    south: true,
    eastSouth: true,
    east: true
  } : _c, _d = _a.handlerClassNames, handlerClassNames = _d === void 0 ? {} : _d, _e = _a.handlerWrapperClassNames, handlerWrapperClassNames = _e === void 0 ? {} : _e, _f = _a.lines, lines = _f === void 0 ? {
    west: true,
    north: true,
    east: true,
    south: true
  } : _f, _g = _a.lineComponent, lineComponent = _g === void 0 ? SimpleLine : _g, _h = _a.lineClassNames, lineClassNames = _h === void 0 ? {} : _h, _j = _a.lineWrapperClassNames, lineWrapperClassNames = _j === void 0 ? {} : _j, _k = _a.disabled, disabled = _k === void 0 ? false : _k, _l = _a.reference, reference = _l === void 0 ? null : _l;
  var _m = __read((0, import_react.useState)(null), 2), lastReference = _m[0], setLastReference = _m[1];
  var points = (0, import_react.useMemo)(function() {
    var result = [];
    HORIZONTAL_DIRECTIONS2.forEach(function(hDirection) {
      VERTICAL_DIRECTIONS2.forEach(function(vDirection) {
        if (hDirection !== vDirection) {
          var _a2 = getDirectionNames(hDirection, vDirection), snakeCase = _a2.snakeCase, camelCase = _a2.camelCase;
          if (snakeCase && camelCase) {
            result.push({
              name: camelCase,
              className: snakeCase,
              verticalPosition: vDirection,
              horizontalPosition: hDirection
            });
          }
        }
      });
    });
    return result;
  }, []);
  var lineNodes = (0, import_react.useMemo)(function() {
    var result = [];
    points.forEach(function(point) {
      if (isCardinalDirection(point.name) && (isObject(lines) ? lines[point.name] : lines)) {
        result.push({
          name: point.name,
          component: lineComponent,
          className: (0, import_classnames.default)(lineClassNames.default, lineClassNames[point.name], disabled && lineClassNames.disabled),
          wrapperClassName: (0, import_classnames.default)("advanced-cropper-bounding-box__line", "advanced-cropper-bounding-box__line--".concat(point.name), lineWrapperClassNames.default, lineWrapperClassNames[point.name], disabled && lineWrapperClassNames.disabled),
          hoverClassName: lineClassNames.hover,
          verticalPosition: point.verticalPosition,
          horizontalPosition: point.horizontalPosition,
          disabled
        });
      }
    });
    return result;
  }, [points, lines, lineComponent, lineClassNames, lineWrapperClassNames, disabled]);
  var handlerNodes = (0, import_react.useMemo)(function() {
    var result = [];
    points.forEach(function(point) {
      if (isObject(handlers) ? handlers[point.name] : handlers) {
        result.push({
          name: point.name,
          component: handlerComponent,
          className: (0, import_classnames.default)(handlerClassNames.default, handlerClassNames[point.name]),
          containerClassName: (0, import_classnames.default)("advanced-cropper-bounding-box__handler-wrapper", "advanced-cropper-bounding-box__handler-wrapper--".concat(point.className)),
          wrapperClassName: (0, import_classnames.default)("advanced-cropper-bounding-box__handler", "advanced-cropper-bounding-box__handler--".concat(point.className), handlerWrapperClassNames.default, handlerWrapperClassNames[point.name]),
          hoverClassName: handlerClassNames.hover,
          verticalPosition: point.verticalPosition,
          horizontalPosition: point.horizontalPosition,
          disabled
        });
      }
    });
    return result;
  }, [points, handlers, handlerComponent, handlerClassNames, handlerWrapperClassNames, disabled]);
  var onHandlerMove = function(horizontalPosition, verticalPosition) {
    return function(_a2, nativeEvent) {
      var left = _a2.left, top = _a2.top;
      var directions = {
        left,
        top
      };
      var respectDirection;
      if (!verticalPosition && horizontalPosition) {
        respectDirection = "width";
      } else if (verticalPosition && !horizontalPosition) {
        respectDirection = "height";
      }
      if (!disabled) {
        if (onResize) {
          var anchor = getDirectionNames(horizontalPosition, verticalPosition).camelCase;
          if (anchor) {
            onResize(anchor, directions, {
              reference: lastReference || reference,
              preserveAspectRatio: nativeEvent && nativeEvent.shiftKey,
              respectDirection,
              compensate: true
            });
          }
        }
        if (!lastReference) {
          setLastReference(reference);
        }
      }
    };
  };
  var onHandlerMoveEnd = function() {
    onResizeEnd === null || onResizeEnd === void 0 ? void 0 : onResizeEnd();
    setLastReference(null);
  };
  return import_react.default.createElement(
    "div",
    { className: (0, import_classnames.default)("advanced-cropper-bounding-box", className), style },
    children,
    import_react.default.createElement("div", null, lineNodes.map(function(line) {
      return import_react.default.createElement(line.component, { key: line.name, defaultClassName: line.className, hoverClassName: line.hoverClassName, wrapperClassName: line.wrapperClassName, position: line.name, disabled: line.disabled, onMove: onHandlerMove(line.horizontalPosition, line.verticalPosition), onMoveEnd: onHandlerMoveEnd });
    })),
    import_react.default.createElement("div", null, handlerNodes.map(function(handler) {
      var handlerElement = import_react.default.createElement(handler.component, { defaultClassName: handler.className, hoverClassName: handler.hoverClassName, wrapperClassName: handler.wrapperClassName, horizontalPosition: handler.horizontalPosition, verticalPosition: handler.verticalPosition, disabled: handler.disabled, onMove: onHandlerMove(handler.horizontalPosition, handler.verticalPosition), onMoveEnd: onHandlerMoveEnd });
      return import_react.default.createElement("div", { key: handler.name, className: handler.containerClassName }, handlerElement);
    }))
  );
};
var StencilOverlay = function(_a) {
  var className = _a.className, children = _a.children;
  return import_react.default.createElement("div", { className: (0, import_classnames.default)("advanced-cropper-stencil-overlay", className) }, children);
};
function useTransition(transitions) {
  if (transitions === void 0) {
    transitions = null;
  }
  var animation = (0, import_react.useRef)(new Animation());
  var _a = __read((0, import_react.useState)(false), 2), active = _a[0], setActive = _a[1];
  return [
    function(callback) {
      if (transitions && transitions.active) {
        animation.current.start(__assign(__assign({}, transitions), { onStart: function() {
          setActive(true);
        }, onProgress: function(progress) {
          callback(progress);
        }, onStop: function() {
          setActive(false);
        } }));
      } else if (!animation.current.active) {
        callback(1);
      }
    },
    active
  ];
}
var ArtificialTransition = function(_a) {
  var className = _a.className, transitions = _a.transitions, children = _a.children, values = __rest(_a, ["className", "transitions", "children"]);
  var root = (0, import_react.useRef)(null);
  var transitionValues = (0, import_react.useRef)(values);
  var _b = __read((0, import_react.useState)(values), 2), rememberedValues = _b[0], setRememberedValues = _b[1];
  var _c = __read(useTransition(transitions), 2), runTransitions = _c[0], transitionsActive = _c[1];
  (0, import_react.useLayoutEffect)(function() {
    if (!deepCompare(rememberedValues, values)) {
      setRememberedValues(values);
      var startValues_1 = transitionsActive ? __assign({}, transitionValues.current) : rememberedValues;
      runTransitions(function(progress) {
        var properties = ["left", "top", "height", "width"];
        properties.forEach(function(property) {
          var desiredValue = values[property];
          var startValue = startValues_1[property];
          transitionValues.current[property] = isNumber(startValue) && isNumber(desiredValue) ? startValue + (desiredValue - startValue) * progress : desiredValue;
        });
        if (root.current) {
          root.current.style.width = "".concat(transitionValues.current.width, "px");
          root.current.style.height = "".concat(transitionValues.current.height, "px");
          root.current.style.transform = "translate3d(".concat(transitionValues.current.left, "px, ").concat(transitionValues.current.top, "px, 0px)");
        }
      });
    }
  }, [rememberedValues, transitionsActive, values.width, values.height, values.top, values.left]);
  var currentValues = transitionsActive ? transitionValues.current : values;
  var rootStyle = {
    left: 0,
    top: 0,
    width: "".concat(currentValues.width, "px"),
    height: "".concat(currentValues.height, "px"),
    transform: "translate3d(".concat(currentValues.left, "px, ").concat(currentValues.top, "px, 0px)")
  };
  return import_react.default.createElement("div", { ref: root, className: (0, import_classnames.default)("advanced-cropper-artificial-transition", className), style: rootStyle }, children);
};
var StencilWrapper = function(_a) {
  var className = _a.className, transitions = _a.transitions, width = _a.width, height = _a.height, left = _a.left, top = _a.top, children = _a.children;
  return import_react.default.createElement(ArtificialTransition, { className: (0, import_classnames.default)("advanced-cropper-stencil-wrapper", className), transitions, width, height, top, left }, children);
};
var StencilGrid = function(_a) {
  var _b = _a.columns, columns = _b === void 0 ? 3 : _b, _c = _a.rows, rows = _c === void 0 ? 3 : _c, _d = _a.visible, visible = _d === void 0 ? false : _d, className = _a.className;
  var nodes = [];
  var _e = __read((0, import_react.useState)(columns), 2), currentColumns = _e[0], setCurrentColumns = _e[1];
  var _f = __read((0, import_react.useState)(rows), 2), currentRows = _f[0], setCurrentRows = _f[1];
  useUpdateEffect(function() {
    if (visible) {
      setCurrentRows(rows);
      setCurrentColumns(columns);
    }
  }, [visible, columns, rows]);
  for (var i = 0; i < currentRows; i++) {
    var cells = [];
    for (var j = 0; j < currentColumns; j++) {
      cells.push(import_react.default.createElement("div", { key: j, className: (0, import_classnames.default)("advanced-cropper-stencil-grid__cell", i === 0 && "advanced-cropper-stencil-grid__cell--top", i === currentRows - 1 && "advanced-cropper-stencil-grid__cell--bottom", j === 0 && "advanced-cropper-stencil-grid__cell--left", j === currentColumns - 1 && "advanced-cropper-stencil-grid__cell--right") }));
    }
    nodes.push(import_react.default.createElement("div", { key: i, className: "advanced-cropper-stencil-grid__row" }, cells));
  }
  return import_react.default.createElement("div", { className: (0, import_classnames.default)("advanced-cropper-stencil-grid", visible && "advanced-cropper-stencil-grid--visible", className) }, nodes);
};
var RectangleStencil = (0, import_react.forwardRef)(function(_a, ref) {
  var cropper = _a.cropper, coordinates = _a.coordinates, aspectRatio2 = _a.aspectRatio, minAspectRatio = _a.minAspectRatio, maxAspectRatio = _a.maxAspectRatio, _b = _a.handlerComponent, handlerComponent = _b === void 0 ? SimpleHandler : _b, _c = _a.handlers, handlers = _c === void 0 ? {
    eastNorth: true,
    north: true,
    westNorth: true,
    west: true,
    westSouth: true,
    south: true,
    eastSouth: true,
    east: true
  } : _c, _d = _a.handlerClassNames, handlerClassNames = _d === void 0 ? {} : _d, _e = _a.handlerWrapperClassNames, handlerWrapperClassNames = _e === void 0 ? {} : _e, _f = _a.lines, lines = _f === void 0 ? {
    west: true,
    north: true,
    east: true,
    south: true
  } : _f, _g = _a.lineComponent, lineComponent = _g === void 0 ? SimpleLine : _g, _h = _a.lineClassNames, lineClassNames = _h === void 0 ? {} : _h, _j = _a.lineWrapperClassNames, lineWrapperClassNames = _j === void 0 ? {} : _j, _k = _a.resizable, resizable = _k === void 0 ? true : _k, _l = _a.movable, movable = _l === void 0 ? true : _l, grid = _a.grid, gridClassName = _a.gridClassName, className = _a.className, movingClassName = _a.movingClassName, resizingClassName = _a.resizingClassName, previewClassName = _a.previewClassName, boundingBoxClassName = _a.boundingBoxClassName, overlayClassName = _a.overlayClassName, draggableAreaClassName = _a.draggableAreaClassName, disabled = _a.disabled;
  var state = cropper.getState();
  var transitions = cropper.getTransitions();
  var interactions = cropper.getInteractions();
  var resizeAllowed = resizable && !disabled;
  var moveAllowed = movable && !disabled;
  (0, import_react.useImperativeHandle)(ref, function() {
    return {
      aspectRatio: createAspectRatio(aspectRatio2 || {
        minimum: minAspectRatio,
        maximum: maxAspectRatio
      })
    };
  });
  var onMove = function(directions) {
    if (cropper && moveAllowed) {
      cropper.moveCoordinates(directions);
    }
  };
  var onMoveEnd = function() {
    if (cropper) {
      cropper.moveCoordinatesEnd();
    }
  };
  var onResize = function(anchor, directions, options) {
    if (cropper && resizeAllowed) {
      cropper.resizeCoordinates(anchor, directions, options);
    }
  };
  var onResizeEnd = function() {
    if (cropper) {
      cropper.resizeCoordinatesEnd();
    }
  };
  var _m = coordinates ? isFunction(coordinates) ? coordinates(state) : coordinates : getStencilCoordinates(state), width = _m.width, height = _m.height, left = _m.left, top = _m.top;
  return state && import_react.default.createElement(
    StencilWrapper,
    { className: (0, import_classnames.default)("advanced-cropper-rectangle-stencil", className, interactions.moveCoordinates && movingClassName, interactions.resizeCoordinates && resizingClassName, {
      "advanced-cropper-rectangle-stencil--movable": moveAllowed,
      "advanced-cropper-rectangle-stencil--moving": interactions.moveCoordinates,
      "advanced-cropper-rectangle-stencil--resizable": resizeAllowed,
      "advanced-cropper-rectangle-stencil--resizing": interactions.resizeCoordinates,
      "advanced-cropper-rectangle-stencil--disabled": disabled
    }), width, height, left, top, transitions },
    import_react.default.createElement(
      BoundingBox,
      { reference: state.coordinates, className: (0, import_classnames.default)(boundingBoxClassName, "advanced-cropper-rectangle-stencil__bounding-box"), handlers, handlerComponent, handlerClassNames, handlerWrapperClassNames, lines, lineComponent, lineClassNames, lineWrapperClassNames, onResize, onResizeEnd, disabled: !resizeAllowed },
      import_react.default.createElement(
        DraggableElement,
        { disabled: !moveAllowed, onMove, onMoveEnd, className: (0, import_classnames.default)("advanced-cropper-rectangle-stencil__draggable-area", draggableAreaClassName) },
        import_react.default.createElement(
          StencilOverlay,
          { className: (0, import_classnames.default)("advanced-cropper-rectangle-stencil__overlay", overlayClassName) },
          grid && import_react.default.createElement(StencilGrid, { visible: cropper.hasInteractions(), columns: interactions.transformImage.rotate ? 9 : 3, rows: interactions.transformImage.rotate ? 9 : 3, className: (0, import_classnames.default)("advanced-cropper-rectangle-stencil__grid", gridClassName) }),
          import_react.default.createElement("div", { className: (0, import_classnames.default)("advanced-cropper-rectangle-stencil__preview", previewClassName) })
        )
      )
    )
  );
});
RectangleStencil.displayName = "RectangleStencil";
function useRotateImageOptions(rotateImage) {
  return (0, import_react.useMemo)(function() {
    return getOptions(rotateImage, {
      touch: true
    }, {
      touch: false
    });
  }, [rotateImage]);
}
function useScaleImageOptions(scaleImage) {
  return (0, import_react.useMemo)(function() {
    return getOptions(scaleImage, {
      touch: true,
      wheel: {
        ratio: 0.1
      }
    }, {
      touch: false,
      wheel: false
    });
  }, [scaleImage]);
}
function useMoveImageOptions(moveImage) {
  return (0, import_react.useMemo)(function() {
    return getOptions(moveImage, {
      touch: true,
      mouse: true
    }, {
      touch: false,
      mouse: false
    });
  }, [moveImage]);
}
var TransformableImageEvent = (
  /** @class */
  function() {
    function TransformableImageEvent2(_a) {
      var active = _a.active;
      this.active = active;
      this.defaultPrevented = false;
    }
    TransformableImageEvent2.prototype.preventDefault = function() {
      this.defaultPrevented = true;
    };
    return TransformableImageEvent2;
  }()
);
var TransformableImage = (
  /** @class */
  function(_super) {
    __extends(TransformableImage2, _super);
    function TransformableImage2(props) {
      var _this = _super.call(this, props) || this;
      _this.processMove = function(newTouches) {
        var _a = _this.props, onTransform = _a.onTransform, touchScale = _a.touchScale, touchMove = _a.touchMove, touchRotate = _a.touchRotate;
        var container = _this.container.current;
        if (container && onTransform) {
          onTransform(touchesToImageTransform(newTouches, _this.touches, container, {
            scale: touchScale,
            rotate: touchRotate,
            move: touchMove
          }));
          _this.touches = newTouches;
        }
      };
      _this.processEnd = function() {
        var onTransformEnd = _this.props.onTransformEnd;
        if (_this.transforming) {
          _this.transforming = false;
          if (onTransformEnd) {
            onTransformEnd();
          }
        }
      };
      _this.processStart = function() {
        _this.transforming = true;
        _this.debouncedProcessEnd.clear();
      };
      _this.processEvent = function(nativeEvent) {
        var _a = _this.props, onEvent = _a.onEvent, disabled = _a.disabled, _b = _a.preventDefault, preventDefault2 = _b === void 0 ? true : _b;
        var transformEvent = new TransformableImageEvent({ active: _this.transforming });
        if (onEvent) {
          onEvent(transformEvent, nativeEvent);
        } else if (preventDefault2) {
          nativeEvent.preventDefault();
          nativeEvent.stopPropagation();
        }
        return !disabled && !transformEvent.defaultPrevented;
      };
      _this.onWheel = function(event) {
        var _a = _this.props, onTransform = _a.onTransform, wheelScale = _a.wheelScale;
        var container = _this.container.current;
        if (wheelScale) {
          if (_this.processEvent(event)) {
            _this.processStart();
            if (onTransform && container) {
              onTransform(wheelEventToImageTransform(event, container, wheelScale === true ? 0.1 : wheelScale.ratio));
            }
            if (!_this.touches.length) {
              _this.debouncedProcessEnd();
            }
          }
        }
      };
      _this.onTouchStart = function(event) {
        var _a = _this.props, touchMove = _a.touchMove, touchScale = _a.touchScale, touchRotate = _a.touchRotate;
        if (event.cancelable && (touchMove || (touchScale || touchRotate) && event.touches.length > 1)) {
          if (_this.processEvent(event)) {
            var container = _this.container.current;
            if (container) {
              var _b = container.getBoundingClientRect(), left_1 = _b.left, top_1 = _b.top, bottom_1 = _b.bottom, right_1 = _b.right;
              _this.touches = Array.from(event.touches).filter(function(touch) {
                return touch.clientX > left_1 && touch.clientX < right_1 && touch.clientY > top_1 && touch.clientY < bottom_1;
              });
            }
          }
        }
      };
      _this.onTouchEnd = function(event) {
        if (event.touches.length === 0) {
          _this.touches = [];
          _this.processEnd();
        }
      };
      _this.onTouchMove = function(event) {
        if (_this.touches.length) {
          var touches = __spreadArray([], __read(event.touches), false).filter(function(touch) {
            return !touch.identifier || _this.touches.find(function(anotherTouch) {
              return anotherTouch.identifier === touch.identifier;
            });
          });
          if (_this.processEvent(event)) {
            _this.processMove(touches);
            _this.processStart();
          }
        }
      };
      _this.onMouseDown = function(event) {
        var mouseMove = _this.props.mouseMove;
        if (mouseMove && "buttons" in event && event.buttons === 1) {
          if (_this.processEvent(event)) {
            var touch = {
              clientX: event.clientX,
              clientY: event.clientY
            };
            _this.touches = [touch];
            _this.processStart();
          }
        }
      };
      _this.onMouseMove = function(event) {
        if (_this.touches.length) {
          if (_this.processEvent(event)) {
            _this.processMove([
              {
                clientX: event.clientX,
                clientY: event.clientY
              }
            ]);
          }
        }
      };
      _this.onMouseUp = function() {
        _this.touches = [];
        _this.processEnd();
      };
      _this.transforming = false;
      _this.touches = [];
      _this.anchor = {
        left: 0,
        top: 0
      };
      _this.container = (0, import_react.createRef)();
      _this.debouncedProcessEnd = debounce(_this.processEnd, props.timeout);
      return _this;
    }
    TransformableImage2.prototype.shouldComponentUpdate = function() {
      return true;
    };
    TransformableImage2.prototype.componentWillUnmount = function() {
      window.removeEventListener("mouseup", this.onMouseUp);
      window.removeEventListener("mousemove", this.onMouseMove);
      window.removeEventListener("touchmove", this.onTouchMove);
      window.removeEventListener("touchend", this.onTouchEnd);
      var container = this.container.current;
      if (container) {
        container.removeEventListener("touchstart", this.onTouchStart);
        container.removeEventListener("mousedown", this.onMouseDown);
        container.removeEventListener("wheel", this.onWheel);
      }
    };
    TransformableImage2.prototype.componentDidMount = function() {
      window.addEventListener("mouseup", this.onMouseUp, { passive: false });
      window.addEventListener("mousemove", this.onMouseMove, { passive: false });
      window.addEventListener("touchmove", this.onTouchMove, { passive: false });
      window.addEventListener("touchend", this.onTouchEnd, { passive: false });
      var container = this.container.current;
      if (container) {
        container.addEventListener("touchstart", this.onTouchStart, {
          passive: false
        });
        container.addEventListener("mousedown", this.onMouseDown, {
          passive: false
        });
        container.addEventListener("wheel", this.onWheel, {
          passive: false
        });
      }
    };
    TransformableImage2.prototype.render = function() {
      var _a = this.props, className = _a.className, children = _a.children, style = _a.style;
      return import_react.default.createElement("div", { className, style, ref: this.container }, children);
    };
    TransformableImage2.defaultProps = {
      touchMove: true,
      mouseMove: true,
      touchScale: true,
      touchRotate: false,
      wheelScale: true,
      timeout: 500
    };
    return TransformableImage2;
  }(import_react.Component)
);
var CropperBackgroundWrapper = function(_a) {
  var _b = _a.scaleImage, scaleImage = _b === void 0 ? true : _b, _c = _a.moveImage, moveImage = _c === void 0 ? true : _c, _d = _a.rotateImage, rotateImage = _d === void 0 ? false : _d, children = _a.children, className = _a.className, style = _a.style, cropper = _a.cropper, timeout = _a.timeout, disabled = _a.disabled;
  var transitions = cropper.getTransitions();
  var rotateImageOptions = useRotateImageOptions(rotateImage);
  var scaleImageOptions = useScaleImageOptions(scaleImage);
  var moveImageOptions = useMoveImageOptions(moveImage);
  return import_react.default.createElement(TransformableImage, { className, style, onTransform: cropper.transformImage, onTransformEnd: cropper.transformImageEnd, touchMove: moveImageOptions.touch, mouseMove: moveImageOptions.mouse, touchScale: scaleImageOptions.touch, wheelScale: scaleImageOptions.wheel, touchRotate: rotateImageOptions.touch, disabled: transitions.active || disabled, preventDefault: !disabled, timeout }, children);
};
var AbstractCropperComponent = function(props, ref) {
  var style = props.style, className = props.className, _a = props.stencilComponent, stencilComponent = _a === void 0 ? RectangleStencil : _a, _b = props.stencilConstraints, stencilConstraints = _b === void 0 ? defaultStencilConstraints : _b, _c = props.stencilProps, stencilProps = _c === void 0 ? {} : _c, _d = props.wrapperComponent, wrapperComponent = _d === void 0 ? CropperWrapper : _d, _e = props.wrapperProps, wrapperProps = _e === void 0 ? {} : _e, _f = props.backgroundComponent, backgroundComponent = _f === void 0 ? CropperBackgroundImage : _f, _g = props.backgroundProps, backgroundProps = _g === void 0 ? {} : _g, backgroundClassName = props.backgroundClassName, _h = props.backgroundWrapperComponent, backgroundWrapperComponent = _h === void 0 ? CropperBackgroundWrapper : _h, _j = props.backgroundWrapperProps, backgroundWrapperProps = _j === void 0 ? {} : _j, _k = props.boundaryComponent, boundaryComponent = _k === void 0 ? StretchableBoundary : _k, boundaryProps = props.boundaryProps, boundaryClassName = props.boundaryClassName, _l = props.canvas, canvas = _l === void 0 ? true : _l, _m = props.crossOrigin, crossOrigin = _m === void 0 ? true : _m, disabled = props.disabled, settings = props.settings, parameters = __rest(props, ["style", "className", "stencilComponent", "stencilConstraints", "stencilProps", "wrapperComponent", "wrapperProps", "backgroundComponent", "backgroundProps", "backgroundClassName", "backgroundWrapperComponent", "backgroundWrapperProps", "boundaryComponent", "boundaryProps", "boundaryClassName", "canvas", "crossOrigin", "disabled", "settings"]);
  var stencilRef = (0, import_react.useRef)(null);
  var _o = useAbstractCropper(function() {
    return __assign(__assign({}, parameters), { crossOrigin, stencilProps, canvas, settings: __assign(__assign({}, settings), stencilConstraints(settings, __assign(__assign({}, stencilProps), stencilRef.current))) });
  }), cropper = _o.cropper, image = _o.image, refs = _o.refs;
  var StencilComponent = stencilComponent;
  var WrapperComponent = wrapperComponent;
  var BackgroundWrapperComponent = backgroundWrapperComponent;
  var BackgroundComponent = backgroundComponent;
  var BoundaryComponent = boundaryComponent;
  (0, import_react.useImperativeHandle)(ref, function() {
    return cropper;
  });
  var deprecatedWrapperProps = {
    loading: cropper.isLoading(),
    loaded: cropper.isLoaded()
  };
  return import_react.default.createElement(
    WrapperComponent,
    __assign({}, wrapperProps, { disabled, className: (0, import_classnames.default)("advanced-cropper", className), cropper, style }, deprecatedWrapperProps),
    import_react.default.createElement(
      BoundaryComponent,
      __assign({}, boundaryProps, { ref: refs.boundary, className: (0, import_classnames.default)("advanced-cropper__boundary", boundaryClassName) }),
      import_react.default.createElement(
        BackgroundWrapperComponent,
        __assign({}, backgroundWrapperProps, { disabled, cropper, className: "advanced-cropper__background-wrapper" }),
        cropper.getState() && import_react.default.createElement(BackgroundComponent, __assign({}, backgroundProps, { ref: refs.image, crossOrigin, cropper, className: (0, import_classnames.default)("advanced-cropper__background", backgroundClassName) })),
        import_react.default.createElement(StencilComponent, __assign({}, stencilProps, { disabled, ref: stencilRef, cropper, image }))
      ),
      canvas && import_react.default.createElement(CropperCanvas, { ref: refs.canvas })
    )
  );
};
var AbstractCropper = createCropper(AbstractCropperComponent);
var defaultSettings = [
  "transformImage",
  "moveCoordinates",
  "resizeCoordinates",
  "defaultCoordinates",
  "defaultVisibleArea",
  "areaPositionRestrictions",
  "areaSizeRestrictions",
  "sizeRestrictions",
  "positionRestrictions",
  "aspectRatio",
  "minWidth",
  "minHeight",
  "maxWidth",
  "maxHeight",
  "defaultSize",
  "defaultPosition",
  "defaultTransforms",
  "imageRestriction",
  "priority"
];
function useAbstractCropperProps(props, settings) {
  if (settings === void 0) {
    settings = defaultSettings;
  }
  var result = { settings: {}, props: {} };
  Object.keys(props).forEach(function(key) {
    if (settings.some(function(setting) {
      return setting === key;
    })) {
      result.settings[key] = props[key];
    } else {
      result.props[key] = props[key];
    }
  });
  return result;
}
var FixedCropper = (0, import_react.forwardRef)(function(props, ref) {
  var cropperProps = useAbstractCropperProps(props, __spreadArray(__spreadArray([], __read(defaultSettings), false), ["stencilSize"], false));
  return import_react.default.createElement(AbstractCropper, __assign({ postProcess: fixedStencil, stencilConstraints: fixedStencilConstraints }, cropperProps.props, { settings: __assign(__assign({ defaultSize: defaultSize2, aspectRatio, sizeRestrictions: withDefaultSizeRestrictions(sizeRestrictions) }, cropperProps.settings), { transformImage: __assign(__assign({}, cropperProps.settings.transformImage), { adjustStencil: false }) }), ref }));
});
FixedCropper.displayName = "FixedCropper";
var Cropper = (0, import_react.forwardRef)(function(props, ref) {
  var cropperProps = useAbstractCropperProps(props);
  var intrinsicProps = cropperProps.props;
  var _a = cropperProps.settings, stencilSize = _a.stencilSize, autoZoom = _a.autoZoom, actualSettings = __rest(_a, ["stencilSize", "autoZoom"]);
  var deprecationWarning2 = useDeprecationWarning();
  if (!isUndefined(autoZoom)) {
    if (intrinsicProps.postProcess || stencilSize) {
      deprecationWarning2("prop 'autoZoom' is deprecated now and will be removed, use 'postProcess' prop to pass your auto zoom function.");
    } else {
      deprecationWarning2("prop 'autoZoom' is deprecated now and will be removed, use 'postProcess' prop to pass your auto zoom function. The 'postProcess' automatically set to 'hybridAutoZoom'");
      intrinsicProps.postProcess = hybridStencilAutoZoom;
    }
  }
  if (!isUndefined(stencilSize)) {
    deprecationWarning2("prop 'stencilSize' is deprecated for <Cropper/> component now and will be removed, use <FixedCropper/> component instead.");
    return import_react.default.createElement(FixedCropper, __assign({ ref, stencilSize }, actualSettings, intrinsicProps));
  }
  return import_react.default.createElement(AbstractCropper, __assign({}, intrinsicProps, { ref, settings: actualSettings }));
});
Cropper.displayName = "CropperComponent";
var CircleStencil = (0, import_react.forwardRef)(function(_a, ref) {
  var cropper = _a.cropper, coordinates = _a.coordinates, _b = _a.handlerComponent, handlerComponent = _b === void 0 ? SimpleHandler : _b, _c = _a.handlers, handlers = _c === void 0 ? {
    eastNorth: true,
    westNorth: true,
    westSouth: true,
    eastSouth: true
  } : _c, _d = _a.handlerClassNames, handlerClassNames = _d === void 0 ? {} : _d, _e = _a.handlerWrapperClassNames, handlerWrapperClassNames = _e === void 0 ? {} : _e, _f = _a.lines, lines = _f === void 0 ? {
    west: true,
    north: true,
    east: true,
    south: true
  } : _f, _g = _a.lineComponent, lineComponent = _g === void 0 ? SimpleLine : _g, _h = _a.lineClassNames, lineClassNames = _h === void 0 ? {} : _h, _j = _a.lineWrapperClassNames, lineWrapperClassNames = _j === void 0 ? {} : _j, _k = _a.resizable, resizable = _k === void 0 ? true : _k, _l = _a.movable, movable = _l === void 0 ? true : _l, grid = _a.grid, gridClassName = _a.gridClassName, className = _a.className, movingClassName = _a.movingClassName, resizingClassName = _a.resizingClassName, previewClassName = _a.previewClassName, boundingBoxClassName = _a.boundingBoxClassName, overlayClassName = _a.overlayClassName, draggableAreaClassName = _a.draggableAreaClassName, disabled = _a.disabled;
  var state = cropper.getState();
  var transitions = cropper.getTransitions();
  var interactions = cropper.getInteractions();
  var resizeAllowed = resizable && !disabled;
  var moveAllowed = movable && !disabled;
  (0, import_react.useImperativeHandle)(ref, function() {
    return {
      aspectRatio: 1,
      boundingBox: "circle"
    };
  });
  var onMove = function(directions) {
    if (cropper && moveAllowed) {
      cropper.moveCoordinates(directions);
    }
  };
  var onMoveEnd = function() {
    if (cropper) {
      cropper.moveCoordinatesEnd();
    }
  };
  var onResize = function(anchor, directions, options) {
    if (cropper && resizeAllowed) {
      cropper.resizeCoordinates(anchor, directions, options);
    }
  };
  var onResizeEnd = function() {
    if (cropper) {
      cropper.resizeCoordinatesEnd();
    }
  };
  var _m = coordinates ? isFunction(coordinates) ? coordinates(state) : coordinates : getStencilCoordinates(state), width = _m.width, height = _m.height, left = _m.left, top = _m.top;
  return state && import_react.default.createElement(
    StencilWrapper,
    { className: (0, import_classnames.default)("advanced-cropper-circle-stencil", className, interactions.moveCoordinates && movingClassName, interactions.resizeCoordinates && resizingClassName, {
      "advanced-cropper-circle-stencil--movable": moveAllowed,
      "advanced-cropper-circle-stencil--moving": interactions.moveCoordinates,
      "advanced-cropper-circle-stencil--resizable": resizeAllowed,
      "advanced-cropper-circle-stencil--resizing": interactions.resizeCoordinates,
      "advanced-cropper-circle-stencil--disabled": disabled
    }), width, height, left, top, transitions },
    import_react.default.createElement(
      BoundingBox,
      { reference: state.coordinates, className: (0, import_classnames.default)(boundingBoxClassName, "advanced-cropper-circle-stencil__bounding-box"), handlers, handlerComponent, handlerClassNames, handlerWrapperClassNames, lines, lineComponent, lineClassNames, lineWrapperClassNames, onResize, onResizeEnd, disabled: !resizeAllowed },
      import_react.default.createElement(
        DraggableElement,
        { disabled: !moveAllowed, onMove, onMoveEnd, className: (0, import_classnames.default)("advanced-cropper-circle-stencil__draggable-area", draggableAreaClassName) },
        import_react.default.createElement(
          StencilOverlay,
          { className: (0, import_classnames.default)("advanced-cropper-circle-stencil__overlay", overlayClassName) },
          grid && import_react.default.createElement(StencilGrid, { visible: cropper.hasInteractions(), columns: interactions.transformImage.rotate ? 9 : 3, rows: interactions.transformImage.rotate ? 9 : 3, className: (0, import_classnames.default)("advanced-cropper-circle-stencil__grid", gridClassName) }),
          import_react.default.createElement("div", { className: (0, import_classnames.default)("advanced-cropper-circle-stencil__preview", previewClassName) })
        )
      )
    )
  );
});
CircleStencil.displayName = "CircleStencil";
var CropperSource = (0, import_react.forwardRef)(function(_a, ref) {
  var src = _a.src, _b = _a.crossOrigin, crossOrigin = _b === void 0 ? true : _b, props = __rest(_a, ["src", "crossOrigin"]);
  return src ? import_react.default.createElement("img", __assign({ key: src, ref, src, className: (0, import_classnames.default)("advanced-cropper-source"), crossOrigin: crossOrigin === true ? "anonymous" : crossOrigin || void 0 }, props)) : null;
});
CropperSource.displayName = "CropperSource";
var CropperPreviewWrapper = function(_a) {
  var children = _a.children, cropper = _a.cropper, className = _a.className, style = _a.style;
  return import_react.default.createElement(
    "div",
    { className: (0, import_classnames.default)(className, "cropper-preview-wrapper"), style },
    import_react.default.createElement(CropperFade, { visible: cropper === null || cropper === void 0 ? void 0 : cropper.isLoaded(), className: "cropper-preview-wrapper__fade" }, children)
  );
};
var CropperPreviewBackground = function(_a) {
  var className = _a.className, cropper = _a.cropper, _b = _a.crossOrigin, crossOrigin = _b === void 0 ? true : _b, size = _a.size, style = _a.style;
  var state = cropper.getState();
  var transitions = cropper.getTransitions();
  var image = cropper.getImage();
  var transformStyles = size && image && (state === null || state === void 0 ? void 0 : state.coordinates) ? getPreviewStyle(image, state, size, transitions) : {};
  var src = image ? image.src : void 0;
  return src ? import_react.default.createElement("img", { key: src, className: (0, import_classnames.default)("advanced-cropper-background-image", className), src, crossOrigin: crossOrigin === true ? "anonymous" : crossOrigin || void 0, style: __assign(__assign({}, transformStyles), style), onMouseDown: preventDefault }) : null;
};
var CropperPreview = (0, import_react.forwardRef)(function(_a, ref) {
  var _b, _c, _d, _e;
  var className = _a.className, contentClassName = _a.contentClassName, _f = _a.state, state = _f === void 0 ? null : _f, _g = _a.image, image = _g === void 0 ? null : _g, _h = _a.transitions, transitions = _h === void 0 ? null : _h, _j = _a.backgroundComponent, backgroundComponent = _j === void 0 ? CropperPreviewBackground : _j, backgroundProps = _a.backgroundProps, backgroundClassName = _a.backgroundClassName, _k = _a.boundaryComponent, boundaryComponent = _k === void 0 ? StretchableBoundary : _k, boundaryProps = _a.boundaryProps, boundaryClassName = _a.boundaryClassName, _l = _a.wrapperComponent, wrapperComponent = _l === void 0 ? CropperPreviewWrapper : _l, wrapperProps = _a.wrapperProps, _m = _a.loaded, loaded = _m === void 0 ? true : _m, _o = _a.loading, loading = _o === void 0 ? false : _o, style = _a.style, cropper = _a.cropper;
  var rerender = useForceRerender();
  var boundaryRef = (0, import_react.useRef)(null);
  var internalInstance = (0, import_react.useRef)(null);
  var instance = cropper || (internalInstance.current ? internalInstance : {
    current: {
      getState: function() {
        return state;
      },
      getTransitions: function() {
        return transitions;
      },
      getImage: function() {
        return image;
      },
      isLoaded: function() {
        return loaded;
      },
      isLoading: function() {
        return loading;
      }
    }
  });
  var _p = __read((0, import_react.useState)(null), 2), size = _p[0], setSize = _p[1];
  var coordinates = (_c = (_b = instance.current) === null || _b === void 0 ? void 0 : _b.getState()) === null || _c === void 0 ? void 0 : _c.coordinates;
  var src = (_e = (_d = instance.current) === null || _d === void 0 ? void 0 : _d.getImage()) === null || _e === void 0 ? void 0 : _e.src;
  var contentStyle = size ? {
    width: "".concat(size.width, "px"),
    height: "".concat(size.height, "px")
  } : {};
  var refresh = function() {
    var _a2, _b2;
    var coordinates2 = (_b2 = (_a2 = instance.current) === null || _a2 === void 0 ? void 0 : _a2.getState()) === null || _b2 === void 0 ? void 0 : _b2.coordinates;
    if (boundaryRef.current && coordinates2) {
      boundaryRef.current.stretchTo(coordinates2).then(function(size2) {
        if (size2 && coordinates2) {
          if (isGreater(ratio(coordinates2), ratio(size2))) {
            setSize({
              width: size2.width,
              height: size2.width / ratio(coordinates2)
            });
          } else {
            setSize({
              width: size2.height * ratio(coordinates2),
              height: size2.height
            });
          }
        } else {
          setSize(null);
        }
      });
    }
    rerender();
  };
  useWindowResize(refresh);
  (0, import_react.useLayoutEffect)(refresh, [coordinates === null || coordinates === void 0 ? void 0 : coordinates.height, coordinates === null || coordinates === void 0 ? void 0 : coordinates.width]);
  (0, import_react.useImperativeHandle)(ref, function() {
    return {
      refresh,
      update: function(cropper2) {
        if (cropper2) {
          internalInstance.current = cropper2;
        } else {
          internalInstance.current = null;
        }
        refresh();
      }
    };
  });
  var WrapperComponent = wrapperComponent;
  var BackgroundComponent = backgroundComponent;
  var BoundaryComponent = boundaryComponent;
  return import_react.default.createElement(
    WrapperComponent,
    __assign({}, wrapperProps, { className: (0, import_classnames.default)(className, "advanced-cropper-preview"), cropper: instance.current, style }),
    import_react.default.createElement(
      BoundaryComponent,
      __assign({ ref: boundaryRef, stretchAlgorithm: stretchPreviewBoundary }, boundaryProps, { className: (0, import_classnames.default)("advanced-cropper-preview__boundary", boundaryClassName) }),
      import_react.default.createElement("div", { className: (0, import_classnames.default)(contentClassName, "advanced-cropper-preview__content"), style: contentStyle }, instance.current && import_react.default.createElement(BackgroundComponent, __assign({}, backgroundProps, { cropper: instance.current, size, className: (0, import_classnames.default)(backgroundClassName, "advanced-cropper-preview__image", src && "advanced-cropper-preview__image--visible") })))
    )
  );
});
function mergeRefs(refs) {
  return function(value) {
    refs.forEach(function(ref) {
      if (typeof ref === "function") {
        ref(value);
      } else if (ref != null) {
        ref.current = value;
      }
    });
  };
}
export {
  AbstractCropperInstance,
  Animation,
  BoundingBox,
  CircleStencil,
  Cropper,
  CropperBackgroundImage,
  CropperBackgroundWrapper,
  CropperCanvas,
  CropperFade,
  CropperPreview,
  CropperPreviewBackground,
  CropperPreviewWrapper,
  CropperSource,
  CropperWrapper,
  DraggableElement as DraggableArea,
  DraggableElement,
  FixedCropper,
  ImageRestriction,
  Priority,
  RectangleStencil,
  SetCoordinatesMode,
  SimpleHandler,
  SimpleLine,
  StencilGrid,
  StencilOverlay,
  StencilWrapper,
  StretchableBoundary,
  TransformableImage,
  TransformableImageEvent,
  anchorMoveToResizeDirections,
  anchorToAllowedDirections,
  anchoredResizeCoordinatesAlgorithm,
  applyDirections,
  applyMove,
  applyScale,
  approximateSize,
  aspectRatioIntersection,
  calculateAreaSizeRestrictions,
  calculateSizeRestrictions,
  coordinatesToPositionRestrictions,
  copyState,
  createAspectRatio,
  createDefaultSettings,
  createImage,
  createState,
  debounce,
  deepClone,
  deepCompare,
  defaultAreaPositionRestrictions,
  defaultAreaSizeRestrictions,
  defaultPosition,
  defaultPositionRestrictions,
  defaultSize,
  defaultStencilConstraints,
  defaultVisibleArea,
  diff,
  distance,
  drawCroppedArea,
  emptyCoordinates,
  fillBoundary,
  fillMoveDirections,
  fillResizeDirections,
  fitBoundary,
  fitCoordinates,
  fitDirections,
  fitToPositionRestrictions,
  fitToSizeRestrictions,
  fitVisibleArea,
  flipImageAlgorithm,
  getAreaPositionRestrictions,
  getAreaSizeRestrictions,
  getAspectRatio,
  getBackgroundStyle,
  getBrokenRatio,
  getCenter,
  getCloserAngle,
  getCloserSize,
  getCoefficient,
  getDefaultCoordinates,
  getDefaultTransforms,
  getDefaultVisibleArea,
  getDirectionNames,
  getEmptyInteractions,
  getImageStyle,
  getIntersections,
  getMinimumSize,
  getOppositeSide,
  getOptions,
  getPositionRestrictions,
  getPreviewStyle,
  getRoundedCoordinates,
  getSizeRestrictions,
  getStencilCoordinates,
  getStyleTransforms,
  getTransformedImageSize,
  getTransitionStyle,
  hasInteractions,
  inverseMove,
  isArray,
  isArrayBufferLike,
  isBlob,
  isBoolean,
  isCardinalDirection,
  isConsistentPosition,
  isConsistentSize,
  isConsistentState,
  isCrossOriginURL,
  isDataUrl,
  isEqualState,
  isFunction,
  isGreater,
  isInitializedState,
  isLocal,
  isLower,
  isMouseEvent,
  isNaN,
  isNumber,
  isNumeric,
  isObject,
  isOrdinalDirection,
  isRoughlyEqual,
  isString,
  isTouchEvent,
  isUndefined,
  isWheelEvent,
  loadImage,
  maxScale,
  mergePositionRestrictions,
  mergeRefs,
  mergeSizeRestrictions,
  minScale,
  moveCoordinates,
  moveCoordinatesAlgorithm,
  moveToPositionRestrictions,
  normalizeCenter,
  normalizeFlip,
  normalizeImageTransform,
  normalizeMoveDirections,
  normalizeResizeDirections,
  parseNumber,
  pixelsRestrictions,
  positionToSizeRestrictions,
  prepareSource,
  promiseTimeout,
  ratio,
  reconcileSizeRestrictions,
  reconcileState,
  resizeCoordinates,
  resizeCoordinatesAlgorithm,
  resizeToSizeRestrictions,
  retrieveSizeRestrictions,
  rotateImageAlgorithm,
  rotatePoint,
  rotateSize,
  setBoundary,
  setCoordinates,
  setVisibleArea,
  sign,
  sizeDistance,
  stretchCropperBoundary,
  stretchPreviewBoundary,
  touchesToImageTransform,
  transformImage,
  transformImageAlgorithm,
  updateCanvas,
  useAbstractCropper,
  useCropperImage,
  useCropperInstance,
  useMoveImageOptions,
  useRotateImageOptions,
  useScaleImageOptions,
  useUpdateEffect,
  useWindowResize,
  wheelEventToImageTransform,
  withDefaultAreaPositionRestrictions,
  withDefaultAreaSizeRestrictions,
  withDefaultPositionRestrictions,
  withDefaultSizeRestrictions
};
/*! Bundled license information:

classnames/index.js:
  (*!
  	Copyright (c) 2018 Jed Watson.
  	Licensed under the MIT License (MIT), see
  	http://jedwatson.github.io/classnames
  *)
*/
//# sourceMappingURL=react-advanced-cropper.js.map
